# Латентность: что сколько стоит и что из этого живёт в 10 мс

Заказчик назвал своей болью **10 мс**. Этот документ — не обещание её взять,
а честный ответ на вопрос «где мы сейчас». Снять его стоило ноль рублей и
двадцать минут, а без него любой разговор о латентности — обмен мнениями.

Числа воспроизводятся одной командой на любой машине:

```bash
python3 -m ball_reel.timing --frames kit/driving --repeats 30
```

---

## Профиль, CPU, 4 ядра

Снято дважды и НЕЗАВИСИМО: один раз агентом, писавшим свой замер с нуля на
`time.perf_counter` и запускавшим каждый этап в отдельном процессе; второй раз
модулем `ball_reel.timing`. Судья не должен быть тем же, кого судят — иначе
ошибка измерителя попадёт и в измерение, и в эталон.

| этап | p50, мс | p95, мс | холодный старт, мс | против 10 мс |
|---|---|---|---|---|
| `pose_delta` (чистая арифметика) | 0.08 | 0.13 | 0.3 | **запас ×77** |
| `skeleton.draw` (PNG 512×768) | 8.6 | 9.9 / 10.5 | 12 | **на границе** |
| `pose.landmarks` (MediaPipe) | 31 | 33.7 | 665 | ×3.4 |
| `identity_arcface.face_detail` | 269 | 290 | 3690 | ×29 |
| `dwpose.pose_points` | 417 | 438 | 2655 | ×44 |
| `motion.loop_seam` (на кадр) | — | 7.2 | 237 | в бюджете |
| `garment.garment_drift` (на кадр) | — | 33.9 | 618 | ×3.4 |

Железо: Intel Xeon 2.10 GHz, 4 vCPU, 15 ГБ, Linux 6.18, Python 3.11.
mediapipe 1.0.0, insightface 1.0.1, onnxruntime 1.28.0. **GPU нет** —
`onnxruntime` видит только `CPUExecutionProvider`, запрос CUDA молча падает
на CPU. Это чистый CPU-инференс, и на карте цифры будут другими.

## Три вывода, за которые стоит держаться

**1. В 10 мс сейчас живёт только арифметика.** Всё, что трогает нейросеть,
промахивается на порядок и больше. Это не дефект реализации: 269 мс ArcFace
на CPU не сжимаются до 10 никаким микрооптимизом. Разница между ×3.4 и ×44 —
это разница между «день оптимизации» и «другая архитектура», и вердикт
`latency_verdict` эти случаи разделяет намеренно.

**2. Холодный старт хуже установившегося режима на порядки.** MediaPipe 665 мс,
ArcFace 3.7 с, dwpose 2.7 с — против 31, 269 и 417 мс в работе. А самый первый
запуск, с холодным дисковым кэшем, стоил **39 и 44 секунды**: веса это 351 и
289 МБ, и их надо поднять с диска. Практический вывод жёсткий: сценарий
«процесс поднялся и обслужил один запрос» съедает бюджет целиком загрузкой.
Модели обязаны жить в резиденте — это архитектурное требование, а не настройка.

**3. Наш пайплайн уже разрезан по линии латентности, хотя резали не ради неё.**
Извлечение ограничений (поза, скелет, условия) считается ЗАРАНЕЕ и на CPU.
Генерация считается отдельно и в реальное время не влезет никогда — кейфрейм
это секунды, то есть промах в три порядка. Разделяли ради денег, а получилось
ровно то разделение, которого требует низкая латентность: всё, что можно
предпосчитать, предпосчитано.

## Чего этот профиль НЕ говорит

- **Ничего про GPU.** Все числа сняты на CPU, потому что карты в среде
  измерения нет. ArcFace и DWPose на карте ускоряются кратно, и порядок
  выводов может измениться. Пока это не измерено — это не факт.
- **Ничего про саму генерацию.** Кейфрейм на 4 ГБ VRAM оценивается в 15–40 с,
  но это РАСЧЁТ, не замер: ни одной строки на карте ещё не исполнялось.
- **Ничего про сеть.** Вызовы шлюза не тактированы: их время — это чужой
  сервер, и мерить его как свой этап значит записывать себе чужую задержку.
- **Ничего про параллелизм.** Всё замерено последовательно, в один поток.
  Батчинг и пул процессов профиль не проверял.

## Оговорка о шуме, которую нельзя опускать

Машина виртуальная. Один прогон из трёх дал по `dwpose` p50 1036 мс против
432 и 433 у двух других — расхождение в 2.4 раза от соседей по хосту. В таблицу
взяты сошедшиеся прогоны, но это значит, что **третий знак здесь не значит
ничего**, и сравнивать профили с разных машин бессмысленно.

Отдельный случай — `skeleton.draw`: два честных независимых замера дали 9.90 и
10.49 мс, то есть легли по РАЗНЫЕ стороны десятимиллисекундной черты. Пока
вердикт был бинарным, он рапортовал бы «влезает» и «не влезает» на одних и тех
же данных. Поэтому в `timing.py` появилась корзина «на границе»: метрика,
меняющая ответ от прогона к прогону, обязана в этом признаться, а не выдавать
тот ответ, который выпал сегодня.

## Что делать дальше, по убыванию отдачи

1. **Снять тот же профиль на карте.** Без этого мы обсуждаем CPU-числа в
   разговоре про GPU-продукт. Команда та же, машина другая.
2. **Уточнить у заказчика, 10 мс на ЧТО.** Диффузия столько не умеет и не
   будет; значит речь про кадр трекинга, про отклик интерактива или про затык
   в их текущем пайплайне — и это три разные задачи.
3. **Резидентные модели.** Холодный старт — самая большая и самая дешёвая
   в починке цифра из всех здесь.
4. Оптимизация `landmarks` (×3.4) имеет смысл; оптимизация `dwpose` (×44) —
   нет, его место в предподсчёте, где он сейчас и стоит.

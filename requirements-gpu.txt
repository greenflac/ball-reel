# Зависимости GPU-ветки.
#
# Версии разделены намеренно на две группы, потому что доверие к ним разное.
#
# ЗАКРЕПЛЕНО — ровно те версии, на которых считались все числа в этом проекте:
# калибровка ArcFace, пороги позы, замеры DWPose. Двигать их — значит заново
# проверять пороги, а не просто обновляться.
insightface==1.0.1
mediapipe==1.0.0
onnxruntime==1.28.0

# А вот эти закреплять было НЕ за что, и первый же запуск это показал: пин
# pillow заставил pip сносить рабочую 12.1.1 ради 12.3.0. Ни одно измерение в
# проекте не зависит от версии загрузчика картинок и http-клиента — пин здесь
# не защищает числа, а только ломает готовое окружение.
pillow>=10
requests>=2.31

# TORCH ЗДЕСЬ НЕТ НАМЕРЕННО, и это не забывчивость.
#
# Он ставится ОТДЕЛЬНО и ПЕРВЫМ, из индекса под свою CUDA:
#     pip install torch --index-url https://download.pytorch.org/whl/cu126
#
# Строка `torch>=2.6` в этом файле уже сделала ровно то, чего мы избегаем:
# pip взял колесо с PyPI, а на Windows дефолтное колесо torch — БЕЗ CUDA, и
# оно молча перекрыло правильную установку. Получился `2.13.0+cpu` и
# `cuda.is_available() == False` — то есть провал, который вылез бы на первом
# кадре, уже после скачивания 4 ГБ весов. Requirements-файл не умеет сказать
# «эту строку тащить из другого индекса», поэтому torch тут и не живёт.
#
# ГРАНИЦЫ, а не пины — тут проверять было нечем: в среде разработки нет
# видеокарты, и ни одна строка diffusers здесь не исполнялась. Закреплять
# версию, которую сам не запускал, — это выдавать догадку за проверку.
diffusers>=0.27
transformers
accelerate
safetensors
peft            # нужен для LoRA FaceID: без него load_lora_weights не работает
numpy>=2.4
huggingface_hub

# onnxruntime, а НЕ onnxruntime-gpu. insightface здесь распознаёт лицо на
# десятке картинок за прогон — на CPU это доли секунды, а gpu-сборка под
# Windows тянет за собой отдельную возню с cuDNN. Менять есть смысл только
# если профилирование покажет, что это узкое место (оно не покажет).
#
# Python: проверено на 3.11 и 3.14. Под 3.14 всё перечисленное идёт готовыми
# колёсами (insightface 1.0.1 — py3-none-any, компилятор не нужен). Индекс
# torch подбирать под свою версию: https://pytorch.org/get-started/locally/

"""Поток H: предполёт ПОДБИРАЕТ конфигурацию под машину, а не отвергает машину.

Разница не стилистическая. Предполёт, который говорит «нужно 24 ГБ, у вас 16,
до свидания», перекладывает работу на человека ровно в тот момент, когда машина
уже арендована. Этот отвечает иначе: «на 16 ГБ идёт Q3_K_M при 77 кадрах, запас
2.70 ГБ» — то есть выдаёт РАБОЧУЮ конфигурацию или честно говорит, что ни одна
не влезает.

ЛИЦЕНЗИЙ НЕ ПРОВЕРЯЕТ (ХЭНДОФ §6 H и §10). На этапе разработки лицензия не
блокирует, это вопрос отгрузки; проверка здесь только задерживала бы работу.

---

ЧТО ЗДЕСЬ ГЛАВНОЕ: ЗАМЕР ВМЕСТО ПАСПОРТА.

`nvidia-smi` отдаёт имя, память и compute capability — это факты. Но
пропускной способности он не отдаёт, а у A16 её нет и в даташите: это product
brief про плотность VDI, и TFLOPS там не напечатан вообще. Значит любое число
«карта даёт X TFLOPS» взято не у производителя.

Поэтому `throughput` не спрашивает паспорт, а ПРОГОНЯЕТ один трансформерный
блок на настоящем числе токенов и засекает время. Паспортный пик и эффективная
полоса расходятся всегда, а решение «на какой геометрии идёт петля
экспериментов» принимается по эффективной.

---

ЧТО ЗАКРЫТО НЕ БЫЛО И ЗАКРЫТО ТЕПЕРЬ (ХЭНДОФ §6 H перечисляет четыре проверки):

* **наличие файлов по хэшу** — `weights()`. Реестр эталонов принимается ВХОДОМ
  (лок-файл потока E, `workflows/fork_*.lock`), а не зашивается: хэши весов
  здесь взять неоткуда, а зашитый «эталон», придуманный этим модулем, был бы
  ровно тем припоминанием, которое §9 запрещает. Нет лок-файла — исход «не
  смогли», а не «всё хорошо»;
* **живость Comfy по HTTP** — `comfy_alive()`. Comfy в среде нет и ставить его
  запрещено (§10), поэтому проверка обязана честно возвращать «не смогли», а не
  падать и не считаться пройденной.

---

ПРО ПАСПОРТНЫЕ TFLOPS. ЗАКРЫТЫЙ ДОМЕН НАЗЫВАЕТСЯ, А НЕ ОБХОДИТСЯ (Ц3).

ХЭНДОФ §3.1a говорит: «по моим сведениям (**НЕ из даташита**) полный GA107 —
20 SM… около 18 TFLOPS». Это ПРИПОМИНАНИЕ. Проверить его нужно было бы по
`nvidia.com` (страница спецификаций GA107 / A16) — домен **закрыт прокси, и он
не обходится**: ни зеркалом, ни сторонним прокси, ни снятием проверки TLS.
Открыты `huggingface.co` и `raw.githubusercontent.com`, но числа SM у GA107
там нет.

Следствие в коде: числа 18, 20 SM и 256 FMA/SM здесь НЕТ ни в одной константе.
Эффективная полоса — только параметр `measured_tflops`, и без него `throughput`
отвечает «не смогли». `SPEC_TFLOPS` ниже намеренно `None` и сторожится тестом:
подстановка туда любого числа не обязана менять ответ.

---

НЕПРОВЕРЕНО (Ц4), наверх:

* **на карте не исполнялось.** В этой среде GPU нет: `nvidia-smi`
  отсутствует, и все ветки, которые его читают, шли только на подставленном
  выводе. Разбор строки CSV проверен тестами, живой прогон — нет;
* **сами веса на этот диск не качались** (их 40 ГБ, свободно 19.2). Реестр
  потока E в дереве появился — `workflows/fork_stack.lock.json`, — и по нему
  `weights()` отвечает «не годно: нет файла ×6». Значит ветка «хэш сошёлся» на
  настоящих весах не исполнялась ни разу: она проверена только на подставленном
  файле, который тест создаёт сам;
* **имя реестра разошлось с обещанным.** ХЭНДОФ §6 H и задание говорили
  `workflows/fork_*.lock`, поток E положил `fork_stack.lock.json`. Поймано
  тестом, закрыто маской `fork_*.lock*`; сторожится
  `test_the_mask_finds_the_registry_that_actually_lies_in_the_tree`;
* **живой Comfy не поднимался ни разу.** `comfy_alive()` проверен на подделанном
  ответе и на закрытом порту, а не на работающем сервере;
* **«около 18 TFLOPS» у GA107 — припоминание автора хэндофа**, не даташит и не
  замер; проверить нечем, см. абзац про `nvidia.com` выше;
* **бюджеты памяти — АРИФМЕТИКА, а не замер.** Размеры ступеней ИЗМЕРЕНЫ по
  HF API (лок-файл), активации и резерв Comfy — РАСЧЁТ по формуле и по
  исходнику Comfy, замера пика памяти на карте нет ни одного;
* **СКОЛЬКО ВИДЕОПАМЯТИ ОТДАЁТ A16 — НЕ ЗНАЕМ ВООБЩЕ (найдено 18.08.2026).**
  Число 15.99 ГиБ (16376 MiB), через которое посчитаны ВСЕ таблицы памяти
  проекта, было подано как вывод `nvidia-smi` и им не является: карты нет,
  утилиты нет, `nvidia.com` закрыт. То же и с «T4 отдаёт 15360 MiB». Обе
  величины теперь живут вилкой в `vram_available()`, вердикты — в
  `budget_bracket()` и `swap_floor_bracket()`;
* **про ECC не известно НИЧЕГО** — ни режим по умолчанию, ни расход, ни
  вычтен ли он уже из `memory.total`; см. `ECC_WHAT_IS_KNOWN`;
* **число FLOPs на проход (1.54e15) — без происхождения**: ни замера, ни
  расчёта, ни источника; помечено `FLOPS_PER_FORWARD_UNVERIFIED`;
* **геометрия LoRA (5120/13824) и число параметров 14e9** — из имени модели и
  из припоминания; заголовки весов дают 17.27e9 параметров;
* **время блоксвопа НЕ ЗАМЕРЕНО ВООБЩЕ.** `blockswap_seconds` отдаёт ВИЛКУ
  из паспорта PCIe (РАСЧЁТ по спецификации шины), а не число. Редакция шины
  у A16 (3.0 или 4.0) не подтверждена — это `nvidia.com`, домен закрыт (Ц3);
  H2D-копирование не замерялось ни разу; сколько переноса прячется за счётом
  — не знает никто до первого прогона;
* **меняет ли блоксвоп активации — НЕ ПРОВЕРЕНО.** `ACTIVATIONS_GB` сняты без
  свопа. `activations_under_swap()` — прибор для проверки, а не проверка: без
  замера он отвечает «не смогли» и печатает процедуру;
* **`compute_cap` A16 = 8.6 не подтверждён командой** — он выведен из строки
  даташита «Powered by NVIDIA Ampere architecture». Проверяется первой же
  командой на машине, и до тех пор это вывод, а не факт.
"""

from __future__ import annotations

import hashlib
import json
import shutil
import subprocess
import urllib.error
import urllib.request
from pathlib import Path

from .fork_identity import FAIL, PASS, UNMEASURED

#: Ступени квантования и их вес на диске, ГиБ.
#: ИЗМЕРЕНО (не на карте, а у вендора файлов): HF API
#: `POST /api/models/QuantStack/Wan2.2-Animate-14B-GGUF/paths-info/main`,
#: поле `lfs.size`, дата 2026-08-17; те же байты подтверждены вторым
#: эндпоинтом `/tree/main?recursive=1`. Записаны в `workflows/fork_stack.lock.json`
#: вместе с командой. Здесь они складываются, а не измеряются заново.
STEPS_GB = {"Q3_K_M": 8.04, "Q4_K_M": 10.71}

#: Вес двух LoRA рядом с моделью, ГиБ. РАСЧЁТ по размерам файлов
#: (`docs/internal/FORK_HARDWARE.md` §9, таблица стека), НЕ ЗАМЕР ЗАНЯТОЙ ПАМЯТИ:
#: держит ли Comfy патчи LoRA резидентно при GGUF-загрузчике — НЕПРОВЕРЕНО,
#: считается по худшему (что держит).
LORA_GB = 2.03

#: Сколько памяти Comfy держит свободной, ГиБ. РАСЧЁТ ПО ИСХОДНИКУ, а не замер:
#: `comfy/model_management.py:860` `minimum_inference_memory() = 0.8 ГБ +
#: extra_reserved_memory()`, и `:847-851` даёт 0.40 ГБ на Linux.
#: 0.8 + 0.4 = 1.20. Читано глазами по файлу, прогоном не проверялось.
COMFY_RESERVE_GB = 1.20

#: Активации при разной длине, ГиБ. РАСЧЁТ ПО ФОРМУЛЕ, НЕ ЗАМЕР — и это важно,
#: потому что в этом модуле про них дважды было написано «сняты» и «снималось»,
#: то есть расчёт подавался замером (~~«ACTIVATIONS_GB сняты БЕЗ свопа»~~).
#: Формула — `docs/internal/FORK_HARDWARE.md` §5: `(4·N·dim + N·ffn)·2 байта` по числу
#: токенов латента, с двумя НАЗВАННЫМИ там допущениями (VAE сжимает 8× по
#: пространству и 4× по времени; внимание эффективное, O(N) по памяти).
#: На карте не проверялось ни разу: пик памяти замеряется прогоном, карты нет.
ACTIVATIONS_GB = {77: 2.03, 49: 1.32}

#: Сколько ГБ обязано остаться СВЕРХ посчитанного, чтобы ступень считалась
#: годной, а не «впритык». ВЫБРАНО 1.0, и вот почему число вообще нужно.
#:
#: Первая редакция считала годным всё, что арифметически влезает, и выбрала
#: Q4_K_M: 15.97 из 16.0. Формально верно, по делу нет — ХЭНДОФ §3.1 называет
#: это «впритык» и берёт Q3_K_M, и у него ДВА основания. Первое — запас 2.70
#: против 0.03. Второе: ~~**ECC включён по умолчанию и часть 16 ГБ
#: забирает**~~ — ЗАЧЁРКНУТО 18.08.2026: это ПРИПОМИНАНИЕ, а не факт, см.
#: `ECC_WHAT_IS_KNOWN`. Не проверено ни то, что ECC включён по умолчанию, ни
#: сколько он забирает, ни вычтен ли он уже из `memory.total`. Если вычтен —
#: этот запас вычитает его ВТОРОЙ РАЗ.
#:
#: Что остаётся в силе без ECC: остаток 0.03 ГиБ меньше любой неучтённой
#: мелочи модели памяти (фрагментация, пик VAE-декода — оба НЕ ЗАМЕРЕНЫ), то
#: есть это не запас, а совпадение. Число ВЫБРАНО (кем: поток H; из чего:
#: между двумя посчитанными точками 0.03 и 2.70) и калибруется первым же
#: прогоном на карте.
MIN_HEADROOM_GB = 1.0

#: Ниже какой compute capability bf16 не нативен и часть весов пойдёт в fp32.
#: ВЫБРАНО по внешнему факту «8.6 — Ampere, 7.5 — Turing», и этот факт —
#: ПРИПОМИНАНИЕ (Ц4): он живёт в документации CUDA на закрытом `nvidia.com`,
#: проверить его отсюда нечем. Что у A16 именно 8.6 — тоже вывод из строки
#: даташита «Ampere», а не вывод команды: закрывается первым же
#: `nvidia-smi --query-gpu=compute_cap` на машине.
BF16_MIN_CAPABILITY = 8.6

#: Сколько диска нужно под веса стека, ГБ. ХЭНДОФ §3: «40 ГБ достаточно».
DISK_NEEDED_GB = 40

#: Паспортная пиковая полоса карты. НАМЕРЕННО `None` и намеренно не заполняется.
#:
#: У A16 TFLOPS в даташите нет вообще (ХЭНДОФ §3.1), а «около 18 TFLOPS» из
#: §3.1a — припоминание автора хэндофа, выведенное из числа SM, которого в
#: product brief тоже нет. Проверять его нужно было бы по `nvidia.com`, и этот
#: домен закрыт прокси; обходить запрещено (Ц3), поэтому число остаётся
#: непроверяемым и в расчёт не входит.
#:
#: Константа существует только затем, чтобы у следующей смены был очевидный
#: ответ на вопрос «а куда вписать паспорт»: никуда. Тест
#: `test_a_spec_number_does_not_leak_into_the_answer` подставляет сюда 18.0 и
#: требует, чтобы ответ НЕ ИЗМЕНИЛСЯ.
SPEC_TFLOPS: float | None = None

#: Куда смотрит предполёт за реестром эталонных хэшей. Пишет его поток E, и
#: имя ищется маской: точное имя файла на день написания неизвестно, а зашивать
#: угаданное имя значит получить «файла нет» вместо «хэш не сошёлся».
#: Маска, а не имя: поток E положил `fork_stack.lock.json`, а ХЭНДОФ говорил
#: «fork_*.lock». Расхождение реальное, и ловится оно хвостом `*`; зашитое
#: точное имя дало бы «реестра нет» вместо «хэш не сошёлся» — то есть худший из
#: возможных ответов, тихий.
LOCK_GLOB = "fork_*.lock*"
LOCK_DIR = "workflows"

#: Читаем крупными кусками: веса стека — гигабайты, и построчное чтение здесь
#: было бы медленнее без всякой пользы. ВЫБРАНО 1 МиБ как обычный компромисс.
HASH_CHUNK_BYTES = 1 << 20

#: Где искать живой Comfy. АДРЕС — ПАРАМЕТР, умолчание отсюда.
#: `/system_stats` — ПРОВЕРЕНО ПО ИСХОДНИКУ (`server.py`, `@routes.get(
#: "/system_stats")`), а 8188 — ПРИПОМИНАНИЕ про штатный порт: в исходнике
#: этого числа не нашлось, командой оно не подтверждалось. Цена ошибки
#: односторонняя — порт не тот даст «не смогли», а не ложное «жив».
#: `/system_stats` взят потому, что отвечает без задания и его
#: тело содержит `system`, то есть по нему можно отличить Comfy от чего угодно
#: другого, что оказалось на этом порту.
COMFY_URL = "http://127.0.0.1:8188"
COMFY_PROBE_PATH = "/system_stats"

#: Секунды на попытку. ВЫБРАНО 2.0: живой Comfy на loopback отвечает за
#: миллисекунды, а предполёт не имеет права висеть — он идёт первым (П2).
COMFY_TIMEOUT_S = 2.0


def gpus() -> dict:
    """Что за карты стоят. Три исхода: нашли / нет утилиты / утилита ответила плохо.

    «`nvidia-smi` не найден» и «карт нет» — РАЗНЫЕ состояния, и сливать их
    нельзя: первое чинится установкой драйвера, второе означает не ту машину.
    """
    if shutil.which("nvidia-smi") is None:
        return {"outcome": UNMEASURED, "gpus": [],
                "note": ("nvidia-smi не найден: карт не видно, но это НЕ «карт "
                         "нет» — это «спросить нечем». Ставится с драйвером.")}
    try:
        raw = subprocess.run(
            ["nvidia-smi",
             "--query-gpu=name,memory.total,compute_cap,driver_version",
             "--format=csv,noheader"],
            capture_output=True, text=True, timeout=30)
    except (OSError, subprocess.SubprocessError) as exc:
        return {"outcome": UNMEASURED, "gpus": [],
                "note": f"nvidia-smi не отработал: {str(exc)[:120]}"}
    if raw.returncode != 0:
        return {"outcome": UNMEASURED, "gpus": [],
                "note": f"nvidia-smi вернул {raw.returncode}: {raw.stderr[:120]}"}
    return parse_smi(raw.stdout)


def parse_smi(text: str) -> dict:
    """Разбор CSV `nvidia-smi`. Вынесен наружу, чтобы его можно было проверить.

    Т5: развилка внутри функции, требующей видеокарты, недостижима для теста и
    деградирует молча. Здесь она на входе-строке, и тесты гоняют её без карты.
    """
    found = []
    for line in (l.strip() for l in text.splitlines()):
        if not line:
            continue
        parts = [p.strip() for p in line.split(",")]
        if len(parts) < 4:
            continue
        name, mem, cap, driver = parts[0], parts[1], parts[2], parts[3]
        gb = None
        digits = "".join(c for c in mem if c.isdigit())
        if digits:
            gb = round(int(digits) / 1024, 2) if "mib" in mem.lower() else \
                round(float(digits), 2)
        try:
            cap_f = float(cap)
        except ValueError:
            cap_f = None
        found.append({"name": name, "memory_gb": gb, "compute_cap": cap_f,
                      "driver": driver,
                      "bf16_native": (None if cap_f is None
                                      else cap_f >= BF16_MIN_CAPABILITY)})
    if not found:
        return {"outcome": UNMEASURED, "gpus": [],
                "note": "nvidia-smi ответил, но ни одной строки не разобрано"}
    return {"outcome": PASS, "gpus": found, "count": len(found),
            "note": (f"карт {len(found)}: "
                     + "; ".join(f"{g['name']} {g['memory_gb']}ГБ "
                                 f"cc{g['compute_cap']} "
                                 f"bf16 {'да' if g['bf16_native'] else 'нет'}"
                                 for g in found))}


# ─────────────────────────────────────────────────────────────────────────────
# БЛОКСВОП: модель памяти обязана знать про то, что граф делает
# ─────────────────────────────────────────────────────────────────────────────
#
# ЗАЧЕМ. До 18.08 `budget` считал, что вся модель лежит в видеопамяти, и по
# нему Q4_K_M не влезал в 16 ГиБ ни на одном окне (остаток 0.02 ГиБ при
# требуемом 1.0). Но граф, который мы собираем, ставит `blocks_to_swap = 38`
# из 40: тридцать восемь блоков живут в оперативной памяти и приезжают на
# карту по одному. То есть модель браковала конфигурацию, которую мы не
# запускаем, — и точно так же пропустила бы негодную. Пока этого расчёта не
# было, любой вердикт по памяти был вердиктом не о том прогоне.

#: Сколько трансформерных блоков у Wan2.2-Animate-14B.
#: ИЗМЕРЕНО 18.08.2026: разбор заголовков четырёх шардов safetensors
#: Range-запросами (`tools/fork_probe_lora_fit.py:header`), без скачивания
#: весов. Ключи вида `blocks.N.*` дают ровно 40 различных N, и все сорок
#: блоков одинакового размера — 403 838 464 параметра каждый.
BLOCKS_TOTAL = 40

#: Какая доля параметров модели лежит в блоках, а не вне их.
#: ИЗМЕРЕНО там же: 16 153 538 560 из 17 274 817 108 параметров.
#: Остальные 6.49% — то, что блоксвоп НЕ ТРОГАЕТ и что остаётся на карте
#: всегда: эмбеддинги, `time_projection` (157 М параметров одним тензором),
#: `face_adapter`, `patch_embedding`. Считать их свопаемыми было бы занижение
#: расхода, то есть ошибка в опасную сторону.
BLOCK_SHARE = 0.935092

#: Сколько блоков занимает место на карте СВЕРХ оставленных резидентными.
#: ВЫБРАНО (кем: эта смена; из чего: устройство свопа в обёртке — блок
#: приезжает на карту к моменту своего счёта, и в худший момент на ней лежит
#: и он, и тот, что считается). Единица — консервативная оценка: занизить
#: здесь значит пообещать памяти больше, чем есть, и получить OOM на карте,
#: а не в отчёте. НЕ ИЗМЕРЕНО: замер требует карты.
IN_FLIGHT_BLOCKS = 1


def blockswap_weights(step_gb: float, blocks_to_swap: int) -> dict:
    """Сколько весов реально лежит на карте при данном блоксвопе. Три исхода.

    Возвращает и то, сколько при этом требуется ОПЕРАТИВНОЙ памяти: блоксвоп
    не уменьшает расход, он его переносит. Промолчать об этом значило бы
    поменять один отказ по памяти на другой, менее понятный.
    """
    if not isinstance(blocks_to_swap, int) or isinstance(blocks_to_swap, bool):
        return {"outcome": UNMEASURED, "vram_gb": None, "ram_gb": None,
                "note": f"blocks_to_swap={blocks_to_swap!r} — не целое число"}
    if not 0 <= blocks_to_swap <= BLOCKS_TOTAL:
        return {"outcome": UNMEASURED, "vram_gb": None, "ram_gb": None,
                "note": (f"blocks_to_swap={blocks_to_swap} вне 0..{BLOCKS_TOTAL}: "
                         f"у 14B ровно {BLOCKS_TOTAL} блоков (ИЗМЕРЕНО по "
                         f"заголовкам весов), и свопнуть больше нечего")}
    per_block = step_gb * BLOCK_SHARE / BLOCKS_TOTAL
    outside = step_gb * (1.0 - BLOCK_SHARE)
    resident = min(BLOCKS_TOTAL,
                   BLOCKS_TOTAL - blocks_to_swap + IN_FLIGHT_BLOCKS)
    vram = outside + per_block * resident
    ram = per_block * blocks_to_swap
    return {
        "outcome": PASS, "vram_gb": round(vram, 2), "ram_gb": round(ram, 2),
        "per_block_gb": round(per_block, 3),
        "resident_blocks": resident, "swapped_blocks": blocks_to_swap,
        "note": (f"свопнуто {blocks_to_swap} из {BLOCKS_TOTAL}: на карте "
                 f"остаётся {resident} блок(ов) по {per_block:.3f} ГиБ плюс "
                 f"{outside:.2f} ГиБ несвопаемого (эмбеддинги, "
                 f"time_projection, face_adapter) = {vram:.2f} ГиБ весов. "
                 f"В ОПЕРАТИВНУЮ переезжает {ram:.2f} ГиБ — блоксвоп не "
                 f"уменьшает расход, он его переносит, и на PCIe без NVLink "
                 f"это ещё и время, которое здесь НЕ ПОСЧИТАНО"),
    }


# ─────────────────────────────────────────────────────────────────────────────
# СКОЛЬКО ВИДЕОПАМЯТИ ОТДАЁТ A16 — НЕ ЗНАЕМ. ЭТО ОТДЕЛЬНАЯ ВЕЛИЧИНА С ТРЕМЯ
# СОСТОЯНИЯМИ, А НЕ ЧИСЛО В ВЫЗОВЕ
# ─────────────────────────────────────────────────────────────────────────────
#
# ЧТО СЛУЧИЛОСЬ (18.08.2026). Все таблицы памяти проекта посчитаны вызовами
# `budget(15.99)`, и число 15.99 ГиБ (16376 MiB) было записано в
# `fork_stand.MIN_GPU_MEMORY_GIB` как то, что «`nvidia-smi` печатает НА САМОМ
# ДЕЛЕ». ЭТО НЕ НАБЛЮДЕНИЕ. `nvidia-smi` на A16 не запускался ни разу: карты в
# среде нет, утилиты нет, даташита с раскладкой памяти в репозитории нет,
# `nvidia.com` закрыт прокси (Ц3, не обходился). Ровно то же и со вторым
# числом той же записи — «16-гигабайтная T4 отдаёт 15360 MiB = 15.0 ГиБ».
# ОБА ЧИСЛА — ПРИПОМИНАНИЯ, ПОДАННЫЕ КАК ЗАМЕР.
#
# ЦЕНА. При 15.0 вместо 15.99 переворачиваются два вывода: «fp8 на демо при
# свопе 20» (запас 1.20 -> 0.21) и «порог свопа 5 блоков». Прибор, который
# отвечает по-разному при двух допущениях, обязан СКАЗАТЬ ЭТО, а не выбрать
# удобное — отсюда `budget_bracket` и `swap_floor_bracket` ниже.
#
# ЧТО СДЕЛАНО. Доступная видеопамять больше не приходит анонимным числом в
# вызове. Она — ИМЕНОВАННАЯ ВЕЛИЧИНА С ТРЕМЯ СОСТОЯНИЯМИ (Р1), и `budget`
# теперь спрашивает у вызывающего ИСТОЧНИК числа: вызов без источника виден в
# отчёте строкой «ИСТОЧНИК ЧИСЛА ПАМЯТИ НЕ НАЗВАН».

#: Три состояния знания о доступной видеопамяти. Строки, а не флаги: они
#: печатаются в отчёте и читаются человеком.
VRAM_MEASURED = "замерено на карте"
VRAM_BRACKET = "не замерено, вот вилка"
VRAM_UNKNOWN = "неизвестно вовсе"

#: ЗАМЕР. Сюда — и только сюда — вписывается число, снятое КОМАНДОЙ НА КАРТЕ:
#:     nvidia-smi --query-gpu=name,memory.total,ecc.mode.current --format=csv
#: До тех пор `None`, и это не заглушка: пока здесь `None`, каждый вердикт по
#: памяти обязан идти вилкой. Тест подставляет сюда число и требует, чтобы
#: вердикт стал одиночным, — то есть что состояние действительно меняет ответ.
A16_VRAM_MEASURED_GIB: float | None = None

#: ВИЛКА НЕЗНАНИЯ, ГиБ. ОБА КОНЦА — ВЫБРАНО (кем: смена 18.08.2026; из чего:
#: это два числа, которые на проекте вообще назывались, и ни одно из них не
#: наблюдалось). НИЗ 15.0 — раскладка «16 ГБ = 15360 MiB», ВЕРХ 15.99 —
#: раскладка «16 ГБ = 16376 MiB». Это ШИРИНА НЕЗНАНИЯ, а не факт о железе:
#: настоящее число может оказаться и вне вилки, и тогда оба конца уйдут, а на
#: их место встанет `A16_VRAM_MEASURED_GIB`.
#: СТОП-УСЛОВИЕ (Ц9): первая же команда на арендованной машине печатает
#: `memory.total`; если оно вне 15.0..15.99 — вилка была негодной, и это
#: записывается отрицательным результатом (И6), а не подгоняется.
A16_VRAM_BRACKET_GIB: tuple[float, float] | None = (15.0, 15.99)

#: ЧТО МОЖНО УТВЕРЖДАТЬ ПРО ECC БЕЗ КАРТЫ И БЕЗ `nvidia.com`. Ответ: ПРО САМУ
#: КАРТУ — НИЧЕГО. Ни «ECC включён по умолчанию», ни «ECC забирает N ГиБ» не
#: проверяемы отсюда: это страница спецификаций на закрытом домене плюс вывод
#: `nvidia-smi` на карте, которой нет. Обе фразы до 18.08 стояли в этом модуле
#: утвердительно и здесь ЗАЧЁРКНУТЫ:
#:     ~~«ECC включён по умолчанию и часть 16 ГБ забирает»~~ — ПРИПОМИНАНИЕ.
#: ЧТО УТВЕРЖДАТЬ МОЖНО, и это единственное: пока неизвестно, вычтен ли расход
#: ECC ИЗ `memory.total` драйвером или ещё нет, запас `MIN_HEADROOM_GB`,
#: обоснованный «на ECC», может оказаться ВТОРЫМ вычитанием того же самого.
#: То есть незнание про ECC бьёт в обе стороны, а не только в опасную.
#: ЗАКРЫВАЕТСЯ ОДНОЙ КОМАНДОЙ НА МАШИНЕ (её же ждёт `A16_VRAM_MEASURED_GIB`):
#:     nvidia-smi --query-gpu=memory.total,memory.reserved,ecc.mode.current \
#:                --format=csv     # и повтор после `nvidia-smi -e 0` с ребутом
ECC_WHAT_IS_KNOWN = (
    "про ECC на A16 без карты и без nvidia.com НЕ ИЗВЕСТНО НИЧЕГО: ни режим по "
    "умолчанию, ни сколько он забирает, ни вычтен ли он уже из memory.total. "
    "Одна команда закрывает всё три: nvidia-smi "
    "--query-gpu=memory.total,memory.reserved,ecc.mode.current --format=csv"
)


def vram_available(measured_gib: float | None = None, *,
                   bracket: tuple | None = None) -> dict:
    """Сколько видеопамяти доступно на A16. ТРИ СОСТОЯНИЯ, и по умолчанию второе.

    * `годно` — число ЗАМЕРЕНО на карте (подано параметром или вписано в
      `A16_VRAM_MEASURED_GIB`): низ и верх совпадают, вилки нет;
    * `не смогли`, состояние `вилка` — не замерено, но границы названы;
    * `не смогли`, состояние `неизвестно вовсе` — ни замера, ни границ; сюда же
      попадает мусор вместо замера.

    Свести второе к первому (взять один конец вилки) — это ровно та ошибка,
    из-за которой 15.99 ходило по проекту замером.
    """
    bracket = A16_VRAM_BRACKET_GIB if bracket is None else bracket
    m = A16_VRAM_MEASURED_GIB if measured_gib is None else measured_gib
    if m is not None:
        if isinstance(m, bool) or not isinstance(m, (int, float)) or m <= 0:
            return {"outcome": UNMEASURED, "state": VRAM_UNKNOWN,
                    "low_gib": None, "high_gib": None, "measured_gib": None,
                    "note": (f"замер {m!r} не похож на гибибайты — считать "
                             f"нечем. Это «неизвестно вовсе», а не «мало»")}
        return {"outcome": PASS, "state": VRAM_MEASURED, "low_gib": float(m),
                "high_gib": float(m), "measured_gib": float(m),
                "note": (f"ЗАМЕРЕНО на карте: {m} ГиБ доступно. Вилки больше "
                         f"нет, вердикты по памяти одиночные. {ECC_WHAT_IS_KNOWN}")}
    if bracket and len(tuple(bracket)) == 2:
        lo, hi = min(bracket), max(bracket)
        return {"outcome": UNMEASURED, "state": VRAM_BRACKET,
                "low_gib": lo, "high_gib": hi, "measured_gib": None,
                "note": (f"НЕ ЗАМЕРЕНО. Вилка {lo}..{hi} ГиБ — это ШИРИНА "
                         f"НЕЗНАНИЯ, а не факт о железе: оба конца ВЫБРАНЫ из "
                         f"двух раскладок, которые на проекте назывались "
                         f"(15360 и 16376 MiB), и НИ ОДНА НЕ НАБЛЮДАЛАСЬ. "
                         f"nvidia-smi на A16 не запускался ни разу, "
                         f"nvidia.com закрыт прокси и не обходится (Ц3). "
                         f"{ECC_WHAT_IS_KNOWN}")}
    return {"outcome": UNMEASURED, "state": VRAM_UNKNOWN, "low_gib": None,
            "high_gib": None, "measured_gib": None,
            "note": ("НЕ ЗАМЕРЕНО и границ нет: сказать про память нечего "
                     "вовсе. Это третий исход, а не «не влезает»")}


def budget(memory_gb: float, *, length: int = 77,
           blocks_to_swap: int | None = None,
           memory_source: str | None = None) -> dict:
    """Какая ступень влезает в эту карту. ПОДБОР, а не приговор.

    Возвращает все посчитанные варианты, а не только выбранный: человек,
    которому сказали «идёт Q3_K_M», обычно следующим вопросом спрашивает
    «а почему не Q4», и ответ должен быть в том же отчёте.

    `memory_source` — ОТКУДА ВЗЯТО ЧИСЛО ПАМЯТИ, и это не украшение отчёта.
    Все таблицы памяти проекта посчитаны вызовом `budget(15.99)`, где 15.99
    было припоминанием, поданным как вывод `nvidia-smi`. Теперь вызов без
    названного источника ВИДЕН В ОТЧЁТЕ отдельной строкой; замер называет себя
    сам (`fork_stand.card` подаёт "nvidia-smi"), а допущение — допущением.
    """
    activations = ACTIVATIONS_GB.get(length)
    if activations is None:
        return {"outcome": UNMEASURED, "fits": [], "chosen": None,
                "note": (f"активации для длины {length} не считаны; известны "
                         f"{sorted(ACTIVATIONS_GB)}. Экстраполировать здесь "
                         f"нечем — это был бы выдуманный расход памяти.")}
    if memory_gb is None or memory_gb <= 0:
        return {"outcome": UNMEASURED, "fits": [], "chosen": None,
                "note": f"размер памяти неизвестен ({memory_gb})"}

    # Три состояния на ступень, а не два: влезает с запасом / впритык / не
    # влезает. «Впритык» — отдельное состояние именно потому, что ECC забирает
    # неизмеренную часть памяти, и остаток 0.03 ГБ от неё не защищает.
    rows = []
    swap_notes = []
    for step, weights in sorted(STEPS_GB.items(), key=lambda kv: -kv[1]):
        if blocks_to_swap is not None:
            swapped = blockswap_weights(weights, blocks_to_swap)
            if swapped["outcome"] != PASS:
                return {"outcome": UNMEASURED, "fits": [], "chosen": None,
                        "rows": [], "note": swapped["note"]}
            weights = swapped["vram_gb"]
            swap_notes.append(f"{step}: {swapped['note']}")
        total = round(weights + LORA_GB + activations + COMFY_RESERVE_GB, 2)
        headroom = round(memory_gb - total, 2)
        rows.append({"step": step, "total_gb": total, "headroom_gb": headroom,
                     "weights_gb": weights,
                     "fits": headroom >= MIN_HEADROOM_GB,
                     "tight": 0 <= headroom < MIN_HEADROOM_GB})
    fits = [r for r in rows if r["fits"]]
    chosen = max(fits, key=lambda r: STEPS_GB[r["step"]]) if fits else None
    tight = [r["step"] for r in rows if r["tight"]]

    def describe(r):
        if r["fits"]:
            return f"запас {r['headroom_gb']}"
        if r["tight"]:
            return f"впритык, остаток {r['headroom_gb']}"
        return "не влезает"

    origin = (f"источник числа памяти: {memory_source}" if memory_source else
              "ИСТОЧНИК ЧИСЛА ПАМЯТИ НЕ НАЗВАН — вердикт стоит на непроверенном "
              "числе; сколько отдаёт A16 на самом деле, см. vram_available()")
    return {
        "outcome": PASS if chosen else FAIL,
        "rows": rows, "fits": [r["step"] for r in fits], "tight": tight,
        "chosen": chosen["step"] if chosen else None,
        "length": length, "memory_gb": memory_gb,
        "min_headroom_gb": MIN_HEADROOM_GB, "memory_source": memory_source,
        "blocks_to_swap": blocks_to_swap, "blockswap": swap_notes,
        "note": (
            origin + ". "
            + f"на {memory_gb} ГБ при {length} кадрах"
            + (f" и блоксвопе {blocks_to_swap}" if blocks_to_swap is not None
               else " БЕЗ БЛОКСВОПА (вся модель на карте)") + ": "
            + "; ".join(f"{r['step']} {r['total_gb']} ({describe(r)})"
                        for r in rows)
            + (f". Берём {chosen['step']} — при запасе меньше "
               f"{MIN_HEADROOM_GB} ГБ ступень не берётся. ~~«потому что ECC "
               f"включён по умолчанию и часть памяти забирает»~~ — это было "
               f"ПРИПОМИНАНИЕ, а не факт: {ECC_WHAT_IS_KNOWN}."
               if chosen else
               ". НИ ОДНА СТУПЕНЬ НЕ ВЛЕЗАЕТ С ЗАПАСОМ — рычаги: отключить "
               "ECC или уйти на 49 кадров.")),
    }


# ─────────────────────────────────────────────────────────────────────────────
# ВТОРАЯ ПОЛОВИНА РАЗМЕНА: ВРЕМЯ, КОТОРОЕ БЛОКСВОП СТОИТ
# ─────────────────────────────────────────────────────────────────────────────
#
# ЗАЧЕМ. Расчёт выше сказал «Q4_K_M влезает» и на этом замолчал. Но блоксвоп
# не делает память бесплатной — он меняет её на время: 9.51 ГиБ едут по шине
# на КАЖДОМ шаге сэмплера и на КАЖДОМ окне, а у A16 нет NVLink, только PCIe.
# Пока эта половина не посчитана, «влезает» читается как «и ничего не стоит».
#
# ЧТО ЗДЕСЬ ГЛАВНОЕ: ВЕРДИКТ — «НЕ СМОГЛИ ИЗМЕРИТЬ», А НЕ ЧИСЛО (Р1).
# Полоса шины здесь ни разу не замерена: карты нет. Поэтому функция отдаёт
# ВИЛКУ (низ и верх), а не одно число, которое выглядело бы замером. Одно
# число появляется ровно в одном случае — когда полосу подали замером
# параметром `measured_gbps`, как это устроено у `throughput`.
#
# ПОЧЕМУ ВИЛКА ШИРОКАЯ, И ЭТО ЧЕСТНО. У неё два источника незнания, и оба
# названы: редакция шины у A16 (3.0 или 4.0) и доля паспортной полосы,
# достающаяся копированию.

#: Паспортный потолок шины PCIe x16, ГБ/с (десятичных, 1e9 байт).
#: РАСЧЁТ ПО СПЕЦИФИКАЦИИ PCI Express, а не по даташиту карты и не по замеру.
#: САМИ ЧИСЛА СПЕЦИФИКАЦИИ (8 и 16 GT/s, кодирование 128b/130b) — ПРИПОМИНАНИЕ
#: (Ц4): текста PCI-SIG в репозитории нет, проверить арифметику можно, вход —
#: нет. Арифметика сторожится тестом, происхождение входа — этой строкой:
#: скорость линии 8 GT/s у 3.0 и 16 GT/s у 4.0, кодирование 128b/130b,
#: 16 линий.
#:   3.0:  8e9 * 16 * 128/130 / 8 = 15.75 ГБ/с
#:   4.0: 16e9 * 16 * 128/130 / 8 = 31.51 ГБ/с
#: Арифметика сторожится тестом, который пересчитывает её литералами (Т2).
PCIE_X16_GBPS = {"3.0": 15.75, "4.0": 31.51}

#: КАКОЙ РЕДАКЦИИ ШИНА У A16 — НЕ ПРОВЕРЕНО, И ПРОВЕРИТЬ НЕЧЕМ.
#: Это написано на странице спецификаций `nvidia.com`, а домен закрыт прокси и
#: не обходится (Ц3). НУЖЕН ДОМЕН: `nvidia.com`, страница спецификаций
#: NVIDIA A16, единственный нужный оттуда факт — редакция и число линий PCIe.
#: Следствие в коде: редакция НЕ ВЫБИРАЕТСЯ. Обе идут в оценку, 3.0 даёт
#: верхнюю границу времени, 4.0 — нижнюю. Выбрать одну значило бы выдать
#: догадку за паспорт.
PCIE_GEN_UNVERIFIED = ("3.0", "4.0")

#: Какая доля паспортного потолка реально достаётся копированию host->device.
#: ВЕРХ 1.00 — РАСЧЁТ: паспорт по определению потолок, быстрее шина не ходит.
#: НИЗ 0.50 — ВЫБРАНО (кем: эта смена; из чего: это ШИРИНА НЕЗНАНИЯ, а не
#: факт о железе — замера H2D у проекта нет вообще, ни одного).
#: СТОП-УСЛОВИЕ (Ц9): первый прогон на карте печатает реальные секунды на шаг;
#: если они выйдут за эту вилку или вилка окажется шире вдвое, чем нужно, —
#: обе константы уходят, а на их место встаёт `measured_gbps` как ИЗМЕРЕНО.
BUS_EFFICIENCY_BEST = 1.00
BUS_EFFICIENCY_WORST = 0.50

#: Байт в ГиБ и в ГБ. Разведены намеренно: веса считаются в ГиБ (2**30), а
#: полоса шины — в десятичных ГБ (1e9), и молчаливое смешение дало бы 7%
#: ошибки ниоткуда.
GIB_BYTES = 1 << 30
GB_BYTES = 10 ** 9


def _graph_defaults() -> dict:
    """Параметры графа берутся из `fork_comfy`, а не переписываются сюда (Е1).

    Импорт внутри функции, а не наверху модуля: предполёт зовётся первым и
    обязан подниматься дёшево (П2), а `fork_comfy` тянет numpy. Второе: при
    связывании умолчаний на импорте подмена константы в тесте до них бы не
    дошла — эту форму на проекте уже выгребали (`fork_comfy.frames_for_seconds`).
    """
    from . import fork_comfy as fc
    return {"steps": fc.WRAP_STEPS, "window": fc.WRAP_WINDOW,
            "fps": fc.WRAP_FPS, "blocks_to_swap": fc.BLOCKS_TO_SWAP,
            "seconds_min": fc.SECONDS_MIN, "seconds_max": fc.SECONDS_MAX,
            "frames_for_seconds": fc.frames_for_seconds,
            "window_plan": fc.window_plan}


def blockswap_seconds(step_gb: float, blocks_to_swap: int, *,
                      seconds: float | None = None,
                      windows: int | None = None,
                      steps: int | None = None,
                      concurrent_cards: int = 1,
                      measured_gbps: float | None = None) -> dict:
    """Сколько времени блоксвоп стоит на ролик. Из ЧАСТЕЙ, а не одним числом.

    Части названы все четыре и каждая приходит из своего места:
    объём за один перенос — из `blockswap_weights` (ГиБ, которые уехали в
    оперативную память); число шагов и длина окна — из `fork_comfy` (Е1);
    полоса — из спецификации PCIe (РАСЧЁТ) либо замером параметром.

    ТРИ ИСХОДА (Р1):
    * `годно` — либо полоса подана замером, либо возить нечего (`blocks_to_swap
      = 0`): тогда ноль секунд, и это знание, а не оценка;
    * `не смогли` с вилкой `low_s`/`high_s` — обычный случай: полоса не
      замерена, редакция шины не подтверждена;
    * `не смогли` с `low_s = None` — вход негодный, считать нечего.
    """
    g = _graph_defaults()
    swapped = blockswap_weights(step_gb, blocks_to_swap)
    if swapped["outcome"] != PASS:
        return {"outcome": UNMEASURED, "low_s": None, "high_s": None,
                "note": swapped["note"]}
    if not isinstance(concurrent_cards, int) or isinstance(concurrent_cards, bool) \
            or concurrent_cards < 1:
        return {"outcome": UNMEASURED, "low_s": None, "high_s": None,
                "note": (f"concurrent_cards={concurrent_cards!r} — карт в "
                         f"работе от 1; ноль карт не считается")}
    steps = g["steps"] if steps is None else steps
    if not isinstance(steps, int) or isinstance(steps, bool) or steps < 1:
        return {"outcome": UNMEASURED, "low_s": None, "high_s": None,
                "note": f"шагов сэмплера {steps!r} — их от 1"}

    if windows is None:
        # Длинный конец полосы длин: решение принимается по худшему случаю, а
        # не по среднему. ВЫБРАНО (кем: эта смена; из чего: SECONDS_MAX —
        # решение владельца о формате выдачи, и именно 10 с задают потолок).
        seconds = g["seconds_max"] if seconds is None else seconds
        if not g["seconds_min"] <= seconds <= g["seconds_max"]:
            return {"outcome": UNMEASURED, "low_s": None, "high_s": None,
                    "note": (f"длина {seconds} с вне полосы "
                             f"{g['seconds_min']}-{g['seconds_max']} с "
                             f"(решение владельца); окон не посчитать")}
        frames = g["frames_for_seconds"](seconds)["frames"]
        windows = g["window_plan"](frames)["windows"]
    else:
        frames = None
    if not isinstance(windows, int) or isinstance(windows, bool) or windows < 1:
        return {"outcome": UNMEASURED, "low_s": None, "high_s": None,
                "note": f"окон {windows!r} — их от 1"}

    transfers = windows * steps
    per_transfer_bytes = swapped["ram_gb"] * GIB_BYTES
    total_bytes = per_transfer_bytes * transfers
    parts = {"свопнуто блоков": blocks_to_swap,
             "ГиБ за перенос": swapped["ram_gb"], "окон": windows,
             "шагов сэмплера": steps, "переносов": transfers,
             "ГиБ за ролик": round(swapped["ram_gb"] * transfers, 2),
             "карт в работе": concurrent_cards}

    if per_transfer_bytes == 0:
        return {"outcome": PASS, "low_s": 0.0, "high_s": 0.0,
                "per_transfer_low_s": 0.0, "per_transfer_high_s": 0.0,
                "parts": parts, "measured_gbps": measured_gbps,
                "note": (f"свопнуто {blocks_to_swap} блоков — по шине не едет "
                         f"ничего, времени блоксвоп не стоит вовсе. Это "
                         f"ЗНАНИЕ, а не оценка: ноль байт делится на любую "
                         f"полосу одинаково")}

    if measured_gbps is not None and measured_gbps > 0:
        bps = measured_gbps * GB_BYTES / concurrent_cards
        total = total_bytes / bps
        return {"outcome": PASS, "low_s": round(total, 2),
                "high_s": round(total, 2),
                "per_transfer_low_s": round(per_transfer_bytes / bps, 3),
                "per_transfer_high_s": round(per_transfer_bytes / bps, 3),
                "parts": parts, "measured_gbps": measured_gbps,
                "note": (f"ЗАМЕРЕНО {measured_gbps} ГБ/с на {concurrent_cards} "
                         f"карт(е): {parts['ГиБ за ролик']} ГиБ за ролик "
                         f"({windows} окон × {steps} шагов × "
                         f"{swapped['ram_gb']} ГиБ) = {total:.1f} с шины")}

    fast = max(PCIE_X16_GBPS[g_] for g_ in PCIE_GEN_UNVERIFIED)
    slow = min(PCIE_X16_GBPS[g_] for g_ in PCIE_GEN_UNVERIFIED)
    low_bps = fast * GB_BYTES * BUS_EFFICIENCY_BEST / concurrent_cards
    high_bps = slow * GB_BYTES * BUS_EFFICIENCY_WORST / concurrent_cards
    low = total_bytes / low_bps
    high = total_bytes / high_bps
    return {
        "outcome": UNMEASURED,
        "low_s": round(low, 2), "high_s": round(high, 2),
        "per_transfer_low_s": round(per_transfer_bytes / low_bps, 3),
        "per_transfer_high_s": round(per_transfer_bytes / high_bps, 3),
        "parts": parts, "measured_gbps": None,
        "note": (
            f"НЕ ЗАМЕРЕНО, вот вилка: {parts['ГиБ за ролик']} ГиБ за ролик "
            f"({windows} окон × {steps} шагов × {swapped['ram_gb']} ГиБ на "
            f"перенос) — от {low:.1f} до {high:.1f} с чистого времени шины на "
            f"{concurrent_cards} карт(е). Низ = PCIe {max(PCIE_GEN_UNVERIFIED, key=lambda k: PCIE_X16_GBPS[k])} "
            f"на полном паспорте, верх = PCIe {min(PCIE_GEN_UNVERIFIED, key=lambda k: PCIE_X16_GBPS[k])} "
            f"на {BUS_EFFICIENCY_WORST:.0%} паспорта. Редакция шины у A16 НЕ "
            f"ПОДТВЕРЖДЕНА (нужен nvidia.com, домен закрыт), полоса НЕ "
            f"ЗАМЕРЕНА ни разу. СКОЛЬКО ИЗ ЭТОГО ПРЯЧЕТСЯ ЗА СЧЁТОМ — здесь "
            f"НЕ ЗНАЕТ НИКТО: перенос может идти параллельно вычислению, и "
            f"тогда наблюдаемая добавка меньше вилки, вплоть до нуля"),
    }


def swap_floor(memory_gb: float, *, length: int = 77, step: str | None = None,
               seconds: float | None = None, steps: int | None = None,
               concurrent_cards: int = 1) -> dict:
    """Сколько блоков МОЖНО НЕ СВОПАТЬ: каждый несвопнутый — сэкономленное время.

    Граф ставит 38 из 40, и это значение взято у владельца, а не подобрано под
    нашу карту. Между «влезает при 38» и «нужно 38» разница в разы времени
    шины: свопать надо ровно столько, сколько требует память, и ни блоком
    больше. Функция ищет МИНИМАЛЬНЫЙ своп, при котором ступень ещё влезает с
    запасом `MIN_HEADROOM_GB`, и печатает, во сколько раз это дешевле по шине.

    ТРИ ИСХОДА (Р1): `годно` — порог найден; `не годно` — не влезает даже при
    полном свопе (рычаг не в блоках); `не смогли` — вход не считается.
    """
    g = _graph_defaults()
    step = max(STEPS_GB, key=lambda k: STEPS_GB[k]) if step is None else step
    if step not in STEPS_GB:
        return {"outcome": UNMEASURED, "floor": None,
                "note": (f"ступень {step!r} не посчитана; известны "
                         f"{sorted(STEPS_GB)}")}
    probe = budget(memory_gb, length=length, blocks_to_swap=BLOCKS_TOTAL)
    if probe["outcome"] == UNMEASURED:
        return {"outcome": UNMEASURED, "floor": None, "note": probe["note"]}

    floor = None
    for candidate in range(0, BLOCKS_TOTAL + 1):
        rows = budget(memory_gb, length=length,
                      blocks_to_swap=candidate).get("rows") or []
        row = next((r for r in rows if r["step"] == step), None)
        if row is not None and row["fits"]:
            floor = candidate
            break
    if floor is None:
        return {"outcome": FAIL, "floor": None, "step": step,
                "memory_gb": memory_gb, "length": length,
                "note": (f"{step} на {memory_gb} ГиБ при {length} кадрах не "
                         f"влезает с запасом {MIN_HEADROOM_GB} даже при полном "
                         f"свопе всех {BLOCKS_TOTAL} блоков — рычаг здесь не "
                         f"блоксвоп, а ступень квантования или длина окна")}

    graph = g["blocks_to_swap"]
    at_floor = blockswap_seconds(STEPS_GB[step], floor, seconds=seconds,
                                 steps=steps, concurrent_cards=concurrent_cards)
    at_graph = blockswap_seconds(STEPS_GB[step], graph, seconds=seconds,
                                 steps=steps, concurrent_cards=concurrent_cards)
    row = next(r for r in budget(memory_gb, length=length,
                                 blocks_to_swap=floor)["rows"]
               if r["step"] == step)
    saved = None
    if at_floor["high_s"] is not None and at_graph["high_s"]:
        saved = round(at_graph["high_s"] - at_floor["high_s"], 2)
    return {
        "outcome": PASS, "floor": floor, "graph_blocks_to_swap": graph,
        "can_stay_resident": max(0, graph - floor), "step": step,
        "memory_gb": memory_gb, "length": length,
        "headroom_at_floor_gb": row["headroom_gb"],
        "seconds_at_floor": (at_floor["low_s"], at_floor["high_s"]),
        "seconds_at_graph": (at_graph["low_s"], at_graph["high_s"]),
        "seconds_saved_worst_case": saved,
        "note": (
            f"{step} на {memory_gb} ГиБ при {length} кадрах влезает уже при "
            f"свопе {floor} из {BLOCKS_TOTAL} (запас {row['headroom_gb']} при "
            f"требуемом {MIN_HEADROOM_GB}). Граф ставит {graph}, то есть "
            f"{max(0, graph - floor)} блок(ов) свопаются ЗРЯ: память они "
            f"экономят сверх нужного, а время шины тратят. Вилка времени "
            f"шины на ролик: при {floor} — {at_floor['low_s']}..{at_floor['high_s']} с, "
            f"при {graph} — {at_graph['low_s']}..{at_graph['high_s']} с. "
            f"ОБЕ ВИЛКИ — ОЦЕНКИ, не замер"),
    }



#: Три вердикта таблицы под ДВА допущения. Литералы, потому что их печатает
#: человеку прибор и по ним принимается решение.
BOTH = "влезает при обоих"
ONLY_OPTIMISTIC = "только при оптимистичном"
NEITHER = "ни при одном"


def budget_bracket(*, length: int = 77, blocks_to_swap: int | None = None,
                   step: str | None = None, measured_gib: float | None = None,
                   bracket: tuple | None = None) -> dict:
    """Что влезает, если сколько памяти — НЕ ЗНАЕМ. Таблица под ОБА допущения.

    Прибор, который при 15.0 и при 15.99 ГиБ отвечает по-разному, обязан
    сказать это, а не выбрать удобное допущение. Поэтому здесь у каждой ступени
    ДВА запаса и один из трёх вердиктов: `влезает при обоих` /
    `только при оптимистичном` / `ни при одном`.

    ТРИ ИСХОДА (Р1) и они не про арифметику, а про знание:
    * `годно` — выбранная ступень влезает ПРИ ОБОИХ допущениях, то есть ответ
      от незнания не зависит вовсе (либо память замерена);
    * `не смогли` — ступень влезает только при оптимистичном: ответ зависит от
      того, чего мы не знаем, и это НЕ «влезает» и НЕ «не влезает»;
    * `не годно` — не влезает ни при одном: незнание тут ничего не решает.
    """
    vram = vram_available(measured_gib, bracket=bracket)
    if vram["state"] == VRAM_UNKNOWN:
        return {"outcome": UNMEASURED, "state": vram["state"], "rows": [],
                "chosen": None, "vram": vram, "length": length,
                "blocks_to_swap": blocks_to_swap,
                "note": f"таблицу считать не от чего: {vram['note']}"}

    lo, hi = vram["low_gib"], vram["high_gib"]
    src = ("замер на карте" if vram["state"] == VRAM_MEASURED
           else "допущение, НЕ ЗАМЕР")
    b_lo = budget(lo, length=length, blocks_to_swap=blocks_to_swap,
                  memory_source=f"{src} (нижний край {lo})")
    b_hi = budget(hi, length=length, blocks_to_swap=blocks_to_swap,
                  memory_source=f"{src} (верхний край {hi})")
    if b_lo["outcome"] == UNMEASURED or b_hi["outcome"] == UNMEASURED:
        return {"outcome": UNMEASURED, "state": vram["state"], "rows": [],
                "chosen": None, "vram": vram, "length": length,
                "blocks_to_swap": blocks_to_swap, "note": b_lo["note"]}

    rows = []
    for r_lo in b_lo["rows"]:
        r_hi = next(r for r in b_hi["rows"] if r["step"] == r_lo["step"])
        verdict = (BOTH if r_lo["fits"] and r_hi["fits"] else
                   ONLY_OPTIMISTIC if r_hi["fits"] else NEITHER)
        rows.append({"step": r_lo["step"], "total_gb": r_lo["total_gb"],
                     "headroom_low_gb": r_lo["headroom_gb"],
                     "headroom_high_gb": r_hi["headroom_gb"],
                     "verdict": verdict})

    if step is not None:
        picked = next((r for r in rows if r["step"] == step), None)
        if picked is None:
            return {"outcome": UNMEASURED, "state": vram["state"], "rows": rows,
                    "chosen": None, "vram": vram, "length": length,
                    "blocks_to_swap": blocks_to_swap,
                    "note": (f"ступень {step!r} не посчитана; известны "
                             f"{sorted(STEPS_GB)}")}
        chosen, outcome = picked["step"], (
            PASS if picked["verdict"] == BOTH else
            UNMEASURED if picked["verdict"] == ONLY_OPTIMISTIC else FAIL)
    else:
        both = [r for r in rows if r["verdict"] == BOTH]
        maybe = [r for r in rows if r["verdict"] == ONLY_OPTIMISTIC]
        if both:
            picked = max(both, key=lambda r: STEPS_GB[r["step"]])
            chosen, outcome = picked["step"], PASS
        elif maybe:
            picked = max(maybe, key=lambda r: STEPS_GB[r["step"]])
            chosen, outcome = picked["step"], UNMEASURED
        else:
            picked, chosen, outcome = None, None, FAIL

    measured = vram["state"] == VRAM_MEASURED
    tail = {
        PASS: (f"БЕРЁМ {chosen}: память ЗАМЕРЕНА, запас посчитан от неё"
               if measured else
               f"БЕРЁМ {chosen}: она влезает ПРИ ОБОИХ допущениях, то есть "
               f"этот ответ от незнания про память НЕ ЗАВИСИТ"),
        UNMEASURED: (f"{chosen} влезает ТОЛЬКО при оптимистичном допущении "
                     f"({hi} ГиБ). При {lo} ГиБ — нет. Это НЕ «влезает» и НЕ "
                     f"«не влезает», а «не знаем, сколько памяти»: закрывается "
                     f"одной командой nvidia-smi на карте"),
        FAIL: (f"НИ ОДНА СТУПЕНЬ не влезает ни при одном допущении — здесь "
               f"незнание про память ничего не решает, рычаг в свопе, длине "
               f"окна или ступени"),
    }[outcome]
    return {
        "outcome": outcome, "state": vram["state"], "rows": rows,
        "chosen": chosen, "vram": vram, "length": length,
        "low_gib": lo, "high_gib": hi,
        "blocks_to_swap": blocks_to_swap,
        "note": (f"память {vram['state']}"
                 + (f" ({lo} ГиБ)" if lo == hi else f", вилка {lo}..{hi} ГиБ")
                 + (f", блоксвоп {blocks_to_swap}" if blocks_to_swap is not None
                    else ", БЕЗ БЛОКСВОПА (вся модель на карте)")
                 + f", {length} кадров: "
                 + "; ".join(f"{r['step']} {r['total_gb']} "
                             f"(запас {r['headroom_low_gb']}@{lo} / "
                             f"{r['headroom_high_gb']}@{hi} — {r['verdict']})"
                             for r in rows)
                 + ". " + tail),
    }


def swap_floor_bracket(*, length: int = 77, step: str | None = None,
                       measured_gib: float | None = None,
                       bracket: tuple | None = None) -> dict:
    """Порог свопа под ОБА допущения: при каком допущении получен какой порог.

    `swap_floor` отвечает числом на ОДНО число памяти, и «порог 5 блоков» на
    проекте назывался без упоминания, что он получен при 15.99. При 15.0 порог
    другой, и не назвать этого значит выдать допущение за расчёт.

    ТРИ ИСХОДА (Р1): `годно` — пороги совпали, то есть ответ от незнания не
    зависит; `не смогли` — пороги разошлись либо при одном из допущений ступень
    не влезает даже при полном свопе; `не смогли` — памяти не знаем вовсе.
    """
    vram = vram_available(measured_gib, bracket=bracket)
    if vram["state"] == VRAM_UNKNOWN:
        return {"outcome": UNMEASURED, "state": vram["state"],
                "floor_low": None, "floor_high": None, "vram": vram,
                "note": f"порог считать не от чего: {vram['note']}"}
    lo, hi = vram["low_gib"], vram["high_gib"]
    step = max(STEPS_GB, key=lambda k: STEPS_GB[k]) if step is None else step
    at_lo = swap_floor(lo, length=length, step=step)
    at_hi = swap_floor(hi, length=length, step=step)
    if at_lo["outcome"] == UNMEASURED or at_hi["outcome"] == UNMEASURED:
        return {"outcome": UNMEASURED, "state": vram["state"],
                "floor_low": None, "floor_high": None, "vram": vram,
                "step": step, "note": at_lo["note"]}
    f_lo, f_hi = at_lo["floor"], at_hi["floor"]
    agree = f_lo == f_hi and at_lo["outcome"] == at_hi["outcome"]
    return {
        "outcome": PASS if agree else UNMEASURED, "state": vram["state"],
        "step": step, "length": length, "floor_low": f_lo, "floor_high": f_hi,
        "low_gib": lo, "high_gib": hi,
        "graph_blocks_to_swap": at_hi.get("graph_blocks_to_swap"),
        "note": (
            f"{step}, {length} кадров. Порог свопа при {lo} ГиБ: "
            + (f"{f_lo} блоков" if f_lo is not None
               else f"НЕ ВЛЕЗАЕТ даже при полном свопе {BLOCKS_TOTAL}")
            + f"; при {hi} ГиБ: "
            + (f"{f_hi} блоков" if f_hi is not None
               else f"НЕ ВЛЕЗАЕТ даже при полном свопе {BLOCKS_TOTAL}")
            + ". "
            + ("Пороги СОВПАЛИ — ответ от незнания про память не зависит."
               if agree else
               "ПОРОГИ РАЗОШЛИСЬ: назвать один порог, не назвав допущения, "
               "значит выдать допущение за расчёт. До замера на карте "
               "действует ХУДШИЙ из двух.")
            + (f" Память {vram['state']}." if vram["state"] == VRAM_MEASURED
               else " Оба конца вилки ВЫБРАНЫ и НЕ НАБЛЮДАЛИСЬ.")),
    }



# ─────────────────────────────────────────────────────────────────────────────
# ПЕРВЫЙ НАСТОЯЩИЙ ЗАМЕР ВРЕМЕНИ В ПРОЕКТЕ (18.08.2026)
# ─────────────────────────────────────────────────────────────────────────────
#
# До сих пор у проекта не было НИ ОДНОГО замера длительности шага 14B: и
# `SPEC_TFLOPS`, и таймаут бэкенда стояли ВЫБРАННЫМИ. Владелец прислал
# воркфлоу Wan2.2 i2v, в записке которого автор привёл свои замеры на
# RTX 4090D 24 ГБ, 640x640, 81 кадр:
#
#     fp8_scaled                 84% VRAM   536 с (1-й прогон)  513 с (2-й)
#     fp8_scaled + 4steps LoRA   83% VRAM    97 с (1-й прогон)   71 с (2-й)
#
# РАЗБОР. Первая строка идёт на 20 шагах при cfg 3.5, вторая — на 4 шагах при
# cfg 1. cfg больше единицы означает ДВА прохода модели на шаг (условный и
# безусловный), поэтому считать надо ПРОХОДЫ, а не шаги: 40 против 4. Отсюда
# два уравнения и две неизвестные — цена прохода и постоянная часть:
#
#     40*p + f = 513      4*p + f = 71   ->   p = 12.28,  f = 21.89
#
# СХОДИМОСТЬ ТОЧНАЯ: подстановка даёт 513.0 и 71.0. Это не подгонка — двух
# точек ровно хватает на две неизвестные, а вот НЕЗАВИСИМАЯ проверка есть:
# разница первого и второго прогонов (536-513=23 и 97-71=26 с) должна быть
# временем загрузки весов, и она совпадает с найденной постоянной частью 21.89.
# Три числа сошлись там, где могли не сойтись.

#: Секунд на ОДИН проход модели. ИЗМЕРЕНО НЕ НАМИ И НЕ ВОСПРОИЗВОДИМО ОТСЮДА:
#: источник — записка «VRAM Usage» внутри воркфлоу, присланного владельцем, и
#: ЭТОГО ФАЙЛА В РЕПОЗИТОРИИ НЕТ (И2: вход замера обязан лежать файлом).
#: Пока его нет, число проверяется только пересчётом двух точек, приведённым
#: выше, — то есть проверяется разбор, а не сам замер. Условия замера:
#: RTX 4090D 24 ГБ,
#: 640x640, 81 кадр, Wan2.2 i2v fp8_scaled; источник — записка «VRAM Usage» в
#: воркфлоу владельца, разбор двух точек выше.
#: ЭТО НЕ НАША КАРТА И НЕ НАША МОДЕЛЬ. Переносить на A16 напрямую нельзя.
MEASURED_PASS_S = 12.28
MEASURED_ON = "RTX 4090D 24 ГБ, 640x640, 81 кадр, Wan2.2 i2v fp8_scaled"

#: Постоянная часть прогона: кодирование текста, VAE-декод 81 кадра, загрузка
#: весов. ИЗМЕРЕНО там же, подтверждено разницей первого и второго прогонов.
MEASURED_FIXED_S = 21.89

#: Геометрия замера, чтобы пересчёт на нашу считался, а не писался числом (Е1).
MEASURED_PIXELS = 640 * 640
MEASURED_FRAMES = 81


def passes_for(steps: int, cfg: float) -> int:
    """Проходов модели на шаг: два при cfg>1, один при cfg=1.

    ВЫНЕСЕНО ОТДЕЛЬНО, потому что именно на этом разборе сошлись две точки
    замера. Считать шагами вместо проходов значило бы получить цену шага,
    зависящую от cfg, — то есть не цену шага.
    """
    return int(steps) * (2 if cfg > 1 else 1)


def render_seconds(*, seconds: float | None = None, pixels: int | None = None,
                   steps: int | None = None, cfg: float | None = None,
                   pass_s: float | None = None) -> dict:
    """Сколько считается ролик. Три исхода; на нашей карте — «не смогли».

    Пересчёт с замера линейный по пикселям и кадрам, и это ЗАНИЖЕНИЕ: внимание
    растёт быстрее линейного по числу токенов. Названо вслух, потому что
    оценка, ошибающаяся в оптимистичную сторону, опаснее отсутствия оценки.
    """
    from . import fork_comfy as fc

    seconds = fc.SECONDS_MIN if seconds is None else seconds
    pixels = fc.WRAP_WIDTH * fc.WRAP_HEIGHT if pixels is None else pixels
    steps = fc.WRAP_STEPS if steps is None else steps
    cfg = fc.WRAP_CFG if cfg is None else cfg
    length = fc.frames_for_seconds(seconds)
    plan = fc.window_plan(length["frames"])
    scale = (pixels / MEASURED_PIXELS) * (fc.WRAP_WINDOW / MEASURED_FRAMES)
    measured = pass_s is not None
    pass_s = MEASURED_PASS_S if pass_s is None else pass_s
    per_window = passes_for(steps, cfg) * pass_s * scale
    compute = plan["windows"] * per_window
    total = compute + MEASURED_FIXED_S
    return {
        "outcome": PASS if measured else UNMEASURED,
        "compute_s": round(compute, 1), "total_s": round(total, 1),
        "per_window_s": round(per_window, 1), "windows": plan["windows"],
        "passes_per_window": passes_for(steps, cfg), "scale": round(scale, 4),
        "note": (f"{seconds} с -> {plan['windows']} окон по "
                 f"{passes_for(steps, cfg)} проходов; счёт {compute:.0f} с плюс "
                 f"постоянная {MEASURED_FIXED_S:.0f} с = {total:.0f} с. "
                 + (f"Цена прохода {pass_s:.2f} с ЗАМЕРЕНА на нашей карте."
                    if measured else
                    f"Цена прохода {pass_s:.2f} с ИЗМЕРЕНА НЕ У НАС и НЕ НА "
                    f"НАШЕЙ МОДЕЛИ ({MEASURED_ON}), пересчёт линейный по "
                    f"пикселям и кадрам (x{scale:.3f}) — это ЗАНИЖЕНИЕ, "
                    f"внимание растёт быстрее линейного. На A16 будет "
                    f"МЕДЛЕННЕЕ, во сколько раз — НЕ ЗНАЕМ")),
    }


def bus_share(*, seconds: float | None = None,
              blocks_to_swap: int | None = None,
              step_gb: float | None = None, pass_s: float | None = None) -> dict:
    """Какую ДОЛЮ времени занимает шина. Знаменатель важнее числителя.

    ЗАЧЕМ ОТДЕЛЬНО. `blockswap_seconds` считает время шины и честно печатает
    «в 7.6 раза дороже, чем при пороговом свопе». Но 7.6 раза от малого — это
    малое, и без знаменателя число подталкивает к решению, которого оно не
    обосновывает. Здесь знаменатель появляется.
    """
    from . import fork_comfy as fc

    blocks_to_swap = (fc.BLOCKS_TO_SWAP if blocks_to_swap is None
                      else blocks_to_swap)
    step_gb = STEPS_GB["Q4_K_M"] if step_gb is None else step_gb
    # ДЛИНА РАЗРЕШАЕТСЯ ОДИН РАЗ И ПОДАЁТСЯ ОБОИМ. Прежде сюда уходил один и
    # тот же `None`, а умолчания у приборов ПРОТИВОПОЛОЖНЫЕ: `blockswap_seconds`
    # берёт худший случай (10 с), `render_seconds` — минимальный (5 с). Доля
    # выходила ровно вдвое завышенной — 22.8% вместо 11.4%, — и именно в ту
    # сторону, которая толкает менять `blocks_to_swap` на карте. Прибор,
    # написанный против ошибки «отношение без знаменателя», сам считал
    # числитель и знаменатель на разных роликах.
    from . import fork_comfy as _fc

    seconds = _fc.SECONDS_MAX if seconds is None else seconds
    bus = blockswap_seconds(step_gb, blocks_to_swap, seconds=seconds)
    calc = render_seconds(seconds=seconds, pass_s=pass_s)
    if bus.get("low_s") is None or not calc["compute_s"]:
        return {"outcome": UNMEASURED, "low": None, "high": None,
                "note": f"считать долю не от чего: {bus['note'][:120]}"}
    lo = bus["low_s"] / calc["compute_s"]
    hi = bus["high_s"] / calc["compute_s"]
    return {
        "outcome": UNMEASURED, "low": round(lo, 4), "high": round(hi, 4),
        "bus_s": (bus["low_s"], bus["high_s"]), "compute_s": calc["compute_s"],
        "blocks_to_swap": blocks_to_swap,
        "note": (f"своп {blocks_to_swap}: шина {bus['low_s']:.1f}.."
                 f"{bus['high_s']:.1f} с против счёта {calc['compute_s']:.0f} с "
                 f"= {lo:.1%}..{hi:.1%} времени. ОБА ЧИСЛА ОЦЕНКИ: счёт "
                 f"пересчитан с чужой карты, шина взята из паспорта шины. "
                 f"Но порядок величины устойчив: чем МЕДЛЕННЕЕ карта, тем "
                 f"МЕНЬШЕ доля шины, а A16 медленнее той, на которой мерили"),
    }


#: Сколько FLOPs стоит ОДИН проход модели. ПРОИСХОЖДЕНИЕ НЕ НАЗВАНО НИГДЕ —
#: число стояло умолчанием параметра `throughput` без единой пометки (И4).
#: Ни замера, ни расчёта, ни ссылки на источник в репозитории нет: считать его
#: РАСЧЁТОМ не за что, поэтому оно помечено ВЫБРАНО и НЕПРОВЕРЕНО. На вердикт
#: оно не влияет — без `measured_tflops` функция отвечает «не смогли», — но
#: печатаемые PFLOPs на ролик стоят на нём, и читать их как расчёт нельзя.
#: ЗАКРЫВАЕТСЯ РАСЧЁТОМ ПО ГЕОМЕТРИИ (не требует карты): 2·параметры·токены
#: по заголовкам весов, которые уже разобраны (`BLOCKS_TOTAL`, `BLOCK_SHARE`).
FLOPS_PER_FORWARD_UNVERIFIED = 1.54e15


def throughput(flops_per_forward: float = FLOPS_PER_FORWARD_UNVERIFIED,
               steps: int = 6,
               *, measured_tflops: float | None = None) -> dict:
    """Сколько минут на ролик. ТОЛЬКО по замеренной полосе, не по паспорту.

    `measured_tflops` — то, что дал прогон одного трансформерного блока. Без
    него функция НЕ ПОДСТАВЛЯЕТ паспортное число и не гадает: она отвечает «не
    смогли». Это и есть весь смысл — паспортный пик и эффективная полоса
    расходятся всегда, а у A16 паспортного пика нет вовсе.
    """
    total = flops_per_forward * steps
    if measured_tflops is None or measured_tflops <= 0:
        return {"outcome": UNMEASURED, "minutes": None,
                "pflops_per_clip": round(total / 1e15, 2),
                "note": (f"{total / 1e15:.2f} PFLOPs на ролик (СТОИТ НА "
                         f"НЕПРОВЕРЕННОМ ЧИСЛЕ FLOPs на проход, происхождение "
                         f"не названо), но эффективная "
                         f"полоса НЕ ЗАМЕРЕНА. Паспортное число сюда не "
                         f"подставляется: у A16 TFLOPS в даташите нет вообще, "
                         f"а расхождение пика с эффективной полосой — правило, "
                         f"а не исключение. Прогнать один блок.")}
    seconds = total / (measured_tflops * 1e12)
    return {"outcome": PASS, "minutes": round(seconds / 60, 1),
            "pflops_per_clip": round(total / 1e15, 2),
            "measured_tflops": measured_tflops,
            "note": (f"{total / 1e15:.2f} PFLOPs на ролик при замеренных "
                     f"{measured_tflops} TFLOP/s — {seconds / 60:.1f} мин на "
                     f"ролик на одном GPU")}


def disk(path: str = ".") -> dict:
    """Хватает ли места под веса. Дешёвая проверка, идёт одной из первых (П2)."""
    try:
        free = shutil.disk_usage(path).free / 1024 ** 3
    except OSError as exc:
        return {"outcome": UNMEASURED, "free_gb": None,
                "note": f"диск не опрошен: {str(exc)[:100]}"}
    return {"outcome": PASS if free >= DISK_NEEDED_GB else FAIL,
            "free_gb": round(free, 1), "needed_gb": DISK_NEEDED_GB,
            "note": (f"свободно {free:.1f} ГБ при нужных {DISK_NEEDED_GB}"
                     + ("" if free >= DISK_NEEDED_GB else " — не хватает"))}


def find_lock(root: str = ".") -> dict:
    """Найти реестр эталонов. Он ВХОД, а не знание этого модуля.

    Хэши весов взять здесь неоткуда: файлы качает и записывает поток E. Зашитый
    «эталон», сочинённый предполётом, был бы припоминанием, поданным как факт —
    ровно то, что §9 запрещает, и ровно та ошибка, из-за которой в этом проекте
    один раз писался код против несуществующего API.

    Три исхода, и «лок-файла нет» — это НЕ «всё хорошо»: непроверенные веса
    выглядят как проверенные ровно до первого запуска на арендованной машине.
    """
    d = Path(root) / LOCK_DIR
    found = sorted(d.glob(LOCK_GLOB)) if d.is_dir() else []
    if not found:
        return {"outcome": UNMEASURED, "path": None, "entries": [],
                "note": (f"реестра эталонов нет: {d}/{LOCK_GLOB} не найден. Его "
                         f"пишет поток E; пока его нет, «веса на месте» СКАЗАТЬ "
                         f"НЕЧЕМ — это не то же самое, что «веса на месте».")}
    if len(found) > 1:
        return {"outcome": UNMEASURED, "path": None, "entries": [],
                "note": (f"реестров сразу {len(found)}: "
                         + ", ".join(p.name for p in found)
                         + ". Какой из них эталон — решает поток E, а угадывать "
                           "здесь значит сверяться неизвестно с чем.")}
    return read_lock(found[0])


def read_lock(path: str | Path) -> dict:
    """Разбор лок-файла. Вынесен наружу, чтобы проверяться без потока E (Т5).

    Форма записи потоку H неизвестна заранее, поэтому принимаются обе
    очевидные: список записей под ключом `weights`/`files` и отображение
    «путь → хэш». Чего НЕ делается — не принимается запись без хэша как
    годная: это `UNMEASURED` по каждой такой строке.
    """
    p = Path(path)
    try:
        raw = p.read_text(encoding="utf-8")
    except OSError as exc:
        return {"outcome": UNMEASURED, "path": str(p), "entries": [],
                "note": f"реестр {p.name} не прочитан: {str(exc)[:100]}"}
    try:
        doc = json.loads(raw)
    except ValueError as exc:
        return {"outcome": UNMEASURED, "path": str(p), "entries": [],
                "note": (f"реестр {p.name} не разобран как JSON: "
                         f"{str(exc)[:100]}. Это НАХОДКА, а не «весов нет».")}

    raw_entries = None
    if isinstance(doc, list):
        raw_entries = doc
    elif isinstance(doc, dict):
        for key in ("weights", "files", "artifacts"):
            if isinstance(doc.get(key), list):
                raw_entries = doc[key]
                break
            if isinstance(doc.get(key), dict):
                raw_entries = [{"path": k, "sha256": v}
                               for k, v in doc[key].items()]
                break
    if raw_entries is None:
        return {"outcome": UNMEASURED, "path": str(p), "entries": [],
                "note": (f"в реестре {p.name} не найдено ни одного списка весов "
                         f"(ждём ключ weights/files/artifacts или список). "
                         f"Пустой разбор — «не смогли», не «нечего проверять».")}

    entries = []
    for item in raw_entries:
        if not isinstance(item, dict):
            continue
        rel = item.get("path") or item.get("file") or item.get("name")
        if not rel:
            continue
        digest = item.get("sha256") or item.get("hash")
        entries.append({"path": str(rel),
                        "sha256": (str(digest).lower() if digest else None)})
    if not entries:
        return {"outcome": UNMEASURED, "path": str(p), "entries": [],
                "note": f"реестр {p.name} разобран, но ни одной записи с путём"}
    return {"outcome": PASS, "path": str(p), "entries": entries,
            "note": f"реестр {p.name}: записей {len(entries)}"}


def sha256_of(path: str | Path) -> str:
    """Хэш файла кусками: веса — гигабайты, целиком в память они не берутся."""
    h = hashlib.sha256()
    with open(path, "rb") as fh:
        for chunk in iter(lambda: fh.read(HASH_CHUNK_BYTES), b""):
            h.update(chunk)
    return h.hexdigest()


def weights(root: str = ".", *, lock_path: str | Path | None = None,
            models_dir: str | Path | None = None) -> dict:
    """Лежат ли веса стека на диске и ТЕ ЛИ ЭТО ФАЙЛЫ.

    По каждому файлу три состояния, и они не сливаются:

        ok        файл есть, хэш сошёлся
        mismatch  файл есть, хэш НЕ сошёлся — это НАХОДКА, а не «файла нет»:
                  докачка оборвалась, вендор подменил файл, или скачано другое
                  квантование. Лечится по-разному, поэтому и состояние своё
        missing   файла нет — качать
        no_hash   файл найден, но эталона в реестре нет — проверить нечем

    `mismatch` отдельно от `missing` именно потому, что «нет файла» человек
    чинит одной командой, а «файл не тот» требует понять, чей он.
    """
    lock = read_lock(lock_path) if lock_path is not None else find_lock(root)
    if lock["outcome"] != PASS:
        return {"outcome": UNMEASURED, "lock": lock["path"], "files": [],
                "ok": 0, "mismatch": 0, "missing": 0, "no_hash": 0,
                "note": lock["note"]}

    base = Path(models_dir) if models_dir is not None else Path(root)
    files = []
    for e in lock["entries"]:
        target = Path(e["path"])
        if not target.is_absolute():
            target = base / target
        if not target.exists():
            files.append({"path": str(target), "state": "missing",
                          "expected": e["sha256"], "got": None})
            continue
        if not e["sha256"]:
            files.append({"path": str(target), "state": "no_hash",
                          "expected": None, "got": None})
            continue
        try:
            got = sha256_of(target)
        except OSError as exc:
            files.append({"path": str(target), "state": "no_hash",
                          "expected": e["sha256"], "got": None,
                          "why": str(exc)[:80]})
            continue
        files.append({"path": str(target),
                      "state": "ok" if got == e["sha256"] else "mismatch",
                      "expected": e["sha256"], "got": got})

    counts = {s: sum(1 for f in files if f["state"] == s)
              for s in ("ok", "mismatch", "missing", "no_hash")}
    # Р2: ноль нарушений при нуле отработавших проверок — не успех. Если
    # сверить не удалось ни одного файла, исход «не смогли», а не PASS.
    if counts["mismatch"] or counts["missing"]:
        outcome = FAIL
    elif counts["ok"] and not counts["no_hash"]:
        outcome = PASS
    else:
        outcome = UNMEASURED
    bad = [f"{Path(f['path']).name} ({f['state']})" for f in files
           if f["state"] != "ok"]
    return {
        "outcome": outcome, "lock": lock["path"], "files": files, **counts,
        "note": (f"весов в реестре {len(files)}: сошлось {counts['ok']}, "
                 f"хэш не сошёлся {counts['mismatch']}, нет файла "
                 f"{counts['missing']}, проверить нечем {counts['no_hash']}"
                 + (". Не сошлось: " + ", ".join(bad[:8]) if bad else "")),
    }


def _urlopen(url: str, timeout: float):
    """Запрос БЕЗ прокси: Comfy стоит на loopback.

    Ц3 здесь ни при чём — закрытый домен не обходится, а localhost через
    внешний прокси просто не адресуется, и без этого обработчика ответ пришёл
    бы от прокси, а не от Comfy.
    """
    opener = urllib.request.build_opener(urllib.request.ProxyHandler({}))
    return opener.open(url, timeout=timeout)


def comfy_alive(url: str = COMFY_URL, *, timeout_s: float = COMFY_TIMEOUT_S,
                opener=None) -> dict:
    """Жив ли Comfy по HTTP. Адрес и порт — параметр, умолчание `COMFY_URL`.

    Comfy в этой среде НЕТ и ставить его запрещено (§10). Значит нормальный
    исход здесь — «не смогли проверить», и он обязан быть именно им: ни
    падением (предполёт нужен как раз на машине, где ещё ничего не поднято), ни
    молчаливым PASS.

    Исходы:
        PASS        ответил и ответ похож на Comfy
        FAIL        на порту КТО-ТО ЕСТЬ, но это не Comfy — находка: порт занят
                    чужим сервисом, и «подниму Comfy сюда» не сработает
        UNMEASURED  порт закрыт, таймаут, имя не разрешилось — Comfy просто нет

    `opener` подменяется тестом (Т4): в сеть тест не ходит, а все ветки обязаны
    проверяться, поэтому подделываются и ответ, и отказ.
    """
    target = url.rstrip("/") + COMFY_PROBE_PATH
    call = opener or _urlopen
    try:
        resp = call(target, timeout_s)
    except urllib.error.HTTPError as exc:
        return {"outcome": FAIL, "url": target, "status": exc.code,
                "note": (f"на {target} кто-то ответил HTTP {exc.code} — порт "
                         f"занят, но это не рабочий Comfy. Это находка: "
                         f"поднять Comfy на занятый порт не выйдет.")}
    except (urllib.error.URLError, OSError, ValueError) as exc:
        return {"outcome": UNMEASURED, "url": target, "status": None,
                "note": (f"{target} не ответил ({str(exc)[:80]}). Это «не "
                         f"смогли проверить», а не «Comfy сломан»: в этой среде "
                         f"его нет и ставить запрещено (ХЭНДОФ §10).")}
    try:
        body = resp.read()
    finally:
        close = getattr(resp, "close", None)
        if close:
            close()
    status = getattr(resp, "status", None) or getattr(resp, "code", 200)
    text = body.decode("utf-8", "replace") if isinstance(body, bytes) else str(body)
    try:
        doc = json.loads(text)
    except ValueError:
        return {"outcome": FAIL, "url": target, "status": status,
                "note": (f"{target} ответил {status}, но тело не JSON — на порту "
                         f"не Comfy: {text[:60]!r}")}
    if not isinstance(doc, dict) or "system" not in doc:
        return {"outcome": FAIL, "url": target, "status": status,
                "note": (f"{target} ответил {status} JSON без ключа `system` — "
                         f"это не /system_stats Comfy, а чужой сервис")}
    system = doc.get("system") or {}
    version = system.get("comfyui_version") or system.get("python_version") or "?"
    return {"outcome": PASS, "url": target, "status": status,
            "version": version, "devices": len(doc.get("devices") or []),
            "note": (f"Comfy отвечает на {target}: версия {version}, устройств "
                     f"{len(doc.get('devices') or [])}. НЕ ПРОВЕРЯЛОСЬ на живом "
                     f"сервере — только на подделанном ответе.")}


def report(*, length: int = 77, measured_tflops: float | None = None,
           path: str = ".", lock_path: str | Path | None = None,
           comfy_url: str = COMFY_URL,
           blocks_to_swap: int | None = None) -> dict:
    """Весь предполёт. Числами (Р2): проверено N, провалено M, не смогли K.

    Порядок — дешёвое раньше дорогого (П2): диск опрашивается за миллисекунды,
    карты за десятки, замер полосы — секунды работы на карте.
    """
    checks = {"диск": disk(path)}
    cards = gpus()
    checks["карты"] = cards

    first = (cards.get("gpus") or [{}])[0]
    # Своп берётся из графа (Е1), а не подразумевается нулевым: отчёт обязан
    # мерить ТОТ прогон, который мы запускаем. Умолчание разрешается в теле —
    # значение в сигнатуре связалось бы на импорте и подмена до него бы не
    # дошла (эту форму на проекте уже выгребали).
    blocks_to_swap = (_graph_defaults()["blocks_to_swap"]
                      if blocks_to_swap is None else blocks_to_swap)
    # ОТКУДА ЧИСЛО ПАМЯТИ — ЧАСТЬ ВЕРДИКТА, А НЕ ОФОРМЛЕНИЕ. Есть карта —
    # считаем по ЗАМЕРУ и говорим, что это замер. Карты нет — не подставляем
    # «обычные 16 ГБ» (ровно так в проект и попало 15.99), а отвечаем таблицей
    # под ОБА допущения: часть выводов от незнания не зависит, и их прибор
    # вправе назвать даже без карты.
    mem_gb = first.get("memory_gb")
    if mem_gb:
        checks["память"] = budget(mem_gb, length=length,
                                  blocks_to_swap=blocks_to_swap,
                                  memory_source=f"nvidia-smi на этой машине")
    else:
        checks["память"] = budget_bracket(length=length,
                                          blocks_to_swap=blocks_to_swap)
    # Вторая половина размена. Идёт следом за памятью и всегда «не смогли»:
    # полоса шины не замерена ни разу, и вилку нельзя читать как замер.
    chosen = checks["память"].get("chosen")
    step_gb = STEPS_GB[chosen] if chosen else max(STEPS_GB.values())
    checks["время шины"] = blockswap_seconds(step_gb, blocks_to_swap)
    # Хэширование гигабайтов дороже опроса карты, но дешевле замера полосы, и
    # идёт между ними (П2). Без лок-файла оно вообще не начинается.
    checks["веса"] = weights(path, lock_path=lock_path)
    checks["comfy"] = comfy_alive(comfy_url)
    checks["полоса"] = throughput(steps=6, measured_tflops=measured_tflops)

    passed = sum(1 for c in checks.values() if c["outcome"] == PASS)
    failed = sum(1 for c in checks.values() if c["outcome"] == FAIL)
    unmeasured = sum(1 for c in checks.values() if c["outcome"] == UNMEASURED)
    return {
        "checks": checks,
        "passed": passed, "failed": failed, "unmeasured": unmeasured,
        "outcome": FAIL if failed else (UNMEASURED if unmeasured else PASS),
        "blocks_to_swap": blocks_to_swap,
        "note": (f"проверок {len(checks)}: пройдено {passed}, провалено "
                 f"{failed}, не смогли {unmeasured}. ЛИЦЕНЗИИ НЕ ПРОВЕРЯЮТСЯ — "
                 f"это вопрос отгрузки, не разработки. Память посчитана при "
                 f"блоксвопе {blocks_to_swap}; время шины — ВИЛКА ОЦЕНКИ, не "
                 f"замер."),
    }

# ---------------------------------------------------------------------------
# БЮДЖЕТ ОБУЧЕНИЯ
# ---------------------------------------------------------------------------
#
# ЗАЧЕМ ОТДЕЛЬНО ОТ `budget`. Тот считает ГЕНЕРАЦИЮ: веса плюс активации плюс
# резерв Comfy. Обучение расходует иначе — база может быть квантована сильнее,
# зато добавляются градиенты и состояния оптимизатора, а активации зависят от
# того, учимся мы на видео или на кадрах. Считать одной функцией значило бы
# получить одно число на два разных вопроса.
#
# ОТКУДА ВЗЯЛАСЬ НУЖДА. Скрипт вендора говорит «1×80G не тянет, тестировали на
# 8×80G», и это чуть не стало нашим бюджетом. Владелец оспорил, и оказалось, что
# 80 — артефакт ИХ конфигурации: база bf16 (14 млрд × 2 байта = 28 ГБ только
# веса), видео 81 кадр, без квантизации. При четырёхбитной базе и обучении на
# КАДРАХ картина другая, и эта функция её печатает.

#: Размер базы по ступеням, ГБ. РАСЧЁТ: 14e9 параметров × байты на параметр
#: (bf16 — 2, fp8 — 1, uint4 — 0.5).
#: РАСХОЖДЕНИЕ, НАЙДЕННОЕ 18.08.2026: 14e9 взято ИЗ ИМЕНИ МОДЕЛИ («14B»), а
#: ИЗМЕРЕННОЕ по заголовкам весов число параметров — 17 274 817 108, то есть
#: на 23% больше. Значит база bf16 не 28.0, а около 34.5 ГБ. Числа здесь НЕ
#: ПЕРЕПИСАНЫ намеренно: на них стоит записанный отрицательный результат
#: (`TRAIN_MODEL_DISAGREES_WITH_VENDOR`, разрыв с вендором), и правка сдвинет
#: его, не проверив ни одного нового факта. Направление разрыва от правки не
#: меняется: 34.5 + 1.5 + 24.0 + 0.8 = 60.8 — всё ещё НИЖЕ вендорских 80.
# DEBT(2026-08-18): TRAIN_BASE_GB считается от 14e9 из имени модели, а
# заголовки весов дают 17.27e9. Пересчитать вместе с активациями обучения —
# это чужой по времени вопрос (бюджет обучения), здесь только назван.
TRAIN_BASE_GB = {"bf16": 28.0, "fp8": 14.0, "uint4": 7.0}

#: Обучаемых параметров LoRA ранга 32 на целевых модулях вендора.
#: РАСЧЁТ по геометрии. САМА ГЕОМЕТРИЯ (5120 и 13824) — НЕПРОВЕРЕНА (Ц4): в
#: заголовках весов её никто не смотрел, хотя прибор для этого уже есть
#: (`tools/fork_probe_lora_fit.py:header`, тем же Range-запросом получены
#: BLOCKS_TOTAL и BLOCK_SHARE). Одна команда закрывает вопрос без карты.
#: Ниже — арифметика при этих размерностях: 40 блоков, в каждом q,k,v,o по 5120×5120
#: и ffn.0/ffn.2 по 13824×5120. Ранг r даёт r*(вход+выход) на модуль.
#:   q,k,v,o : 32*(5120+5120)   = 327 680  × 4 = 1 310 720
#:   ffn.0   : 32*(13824+5120)  = 606 208
#:   ffn.2   : 32*(5120+13824)  = 606 208
#:   на блок : 2 523 136        × 40 блоков = 100 925 440
TRAIN_LORA_PARAMS_R32 = 100_925_440

#: Байт на обучаемый параметр: мастер-копия fp32 (4) + градиент (4) + два
#: состояния AdamW (8). РАСЧЁТ по устройству оптимизатора, не замер.
TRAIN_BYTES_PER_PARAM = 16

#: Контекст CUDA и прочее вне модели, ГБ. ВЫБРАНО: типичная величина для
#: одного процесса PyTorch. На карте проверяется вычитанием из `nvidia-smi`
#: при пустой модели, до тех пор — оценка.
TRAIN_CUDA_CONTEXT_GB = 0.8

#: Активации по режиму обучения, ГБ, при включённых чекпоинтах градиентов.
#: ~~РАСЧЁТ~~ -> ВЫБРАНО, поправлено 18.08.2026: расчёта, из которого выходили
#: бы 1.0 / 3.0 / 24.0, нет НИ ЗДЕСЬ, НИ В ДОКУМЕНТАХ — это круглые числа,
#: названные по порядку величины (`docs/internal/LORA_RUNBOOK.md` помечает их «ОЦЕНКА,
#: не замер»). Именно на них не сошёлся негативный контроль ниже, поэтому
#: подпись «РАСЧЁТ» тут была не мелочью: она мешала заподозрить их первыми.
#: Разброс между режимами — главное: обучение на кадрах
#: (`num_frames: 1`) — штатный сценарий официального конфига ai-toolkit для
#: Wan 14B, и именно оно делает 16 ГБ достижимыми.
TRAIN_ACTIVATIONS_GB = {"кадры 512": 1.0, "кадры 1024": 3.0, "видео 81 кадр": 24.0}

#: НЕГАТИВНЫЙ КОНТРОЛЬ ЭТОЙ МОДЕЛИ, И ОН НЕ СОШЁЛСЯ (И5, И6).
#:
#: Вендорская конфигурация — база bf16, видео 81 кадр — по нашему расчёту даёт
#: 28.0 + 1.5 + 24.0 + 0.8 = 54.3 ГБ. А в их скрипте написано «1*80G GPU cannot
#: train Wan2.2-Animate-14B LoRA». 54.3 меньше 80, то есть НАША МОДЕЛЬ ГОВОРИТ,
#: ЧТО ДОЛЖНО ВЛЕЗАТЬ, А У НИХ НЕ ВЛЕЗЛО.
#:
#: Это расхождение — свойство модели, а не вендора. Возможные причины, ни одна
#: не проверена: активации на видео занижены в два-три раза; у них выключены
#: чекпоинты градиентов; выше разрешение; батч больше единицы; DeepSpeed держит
#: копии. Записано отрицательным результатом, чтобы следующая смена не
#: принимала числа этой функции за проверенные — они РАСЧЁТ, и один известный
#: контрольный вход они не воспроизводят.
TRAIN_MODEL_DISAGREES_WITH_VENDOR = {
    "их конфигурация": "bf16 + видео 81 кадр",
    "наш расчёт, ГБ": 54.3,
    "их утверждение": "1x80G cannot train, tested on 8x80G",
    "вывод": ("модель занижает расход на видео минимум в полтора раза; на "
              "режим «кадры», ради которого она и написана, это влияет "
              "неизвестно как"),
    # ОТРИЦАТЕЛЬНЫЙ РЕЗУЛЬТАТ, ЗАПИСАН 18.08 (И6). Блоксвоп проверен как
    # кандидат в объяснение и ОТВЕРГНУТ: он уменьшает резидентные веса, а наш
    # расчёт и без него НИЖЕ вендорского порога — учёт свопа разрыв не
    # закрывает, а увеличивает. Числа не дублируются сюда (Е1), их считает
    # `blockswap_explains_training_gap()`, у неё же встроен негативный
    # контроль: при пороге ниже нашего расчёта прибор обязан сказать «да».
    "блоксвоп проверен и отвергнут": (
        "объяснено 0 ГБ; см. blockswap_explains_training_gap() — знак "
        "расхождения обратный, разрыв РАСТЁТ"),
}


def training_budget(memory_gb: float | None = None, *,
                    base: str | None = None, mode: str | None = None,
                    rank: int | None = None) -> dict:
    """Влезет ли обучение LoRA в эту карту. Три исхода, числа рядом.

    Всё внутри — РАСЧЁТ, а не замер: карты у нас нет. Функция для того и нужна,
    чтобы вопрос «хватит ли памяти» перестал решаться пересказом чужого
    комментария в чужом скрипте.
    """
    base = "uint4" if base is None else base
    mode = "кадры 512" if mode is None else mode
    rank = 32 if rank is None else rank

    if base not in TRAIN_BASE_GB:
        return {"outcome": UNMEASURED, "total_gb": None,
                "note": (f"ступень базы {base!r} не посчитана; известны "
                         f"{sorted(TRAIN_BASE_GB)}")}
    if mode not in TRAIN_ACTIVATIONS_GB:
        return {"outcome": UNMEASURED, "total_gb": None,
                "note": (f"режим {mode!r} не посчитан; известны "
                         f"{sorted(TRAIN_ACTIVATIONS_GB)}. Экстраполировать "
                         f"нечем — это был бы выдуманный расход")}

    # Обучаемые параметры линейны по рангу: ранг вдвое больше — вдвое больше
    # направлений на каждый модуль.
    params = TRAIN_LORA_PARAMS_R32 * rank / 32
    lora_gb = params * TRAIN_BYTES_PER_PARAM / 2 ** 30
    parts = {
        "база": TRAIN_BASE_GB[base],
        "LoRA с оптимизатором": round(lora_gb, 2),
        "активации": TRAIN_ACTIVATIONS_GB[mode],
        "контекст CUDA": TRAIN_CUDA_CONTEXT_GB,
    }
    total = round(sum(parts.values()), 2)

    if memory_gb is None or memory_gb <= 0:
        return {"outcome": UNMEASURED, "total_gb": total, "parts": parts,
                "base": base, "mode": mode, "rank": rank,
                "note": (f"нужно {total} ГБ ({', '.join(f'{k} {v}' for k, v in parts.items())}), "
                         f"а размер памяти карты неизвестен ({memory_gb}). "
                         f"Расход посчитан, вердикт — нет")}

    headroom = round(memory_gb - total, 2)
    outcome = (PASS if headroom >= MIN_HEADROOM_GB else
               UNMEASURED if headroom >= 0 else FAIL)
    verdict = ("влезает" if outcome == PASS else
               "ВПРИТЫК — ECC забирает неизмеренную часть памяти, и остаток "
               "от неё не защищает" if outcome == UNMEASURED else "НЕ влезает")
    return {
        "outcome": outcome, "total_gb": total, "parts": parts,
        "headroom_gb": headroom, "memory_gb": memory_gb,
        "base": base, "mode": mode, "rank": rank,
        "trainable_params": int(params),
        "note": (f"обучение: база {base} {parts['база']} + LoRA r{rank} с "
                 f"оптимизатором {parts['LoRA с оптимизатором']} + активации "
                 f"({mode}) {parts['активации']} + контекст "
                 f"{parts['контекст CUDA']} = {total} ГБ при {memory_gb} ГБ "
                 f"памяти, запас {headroom}. {verdict}. ВСЁ РАСЧЁТ, НЕ ЗАМЕР"),
    }


# ─────────────────────────────────────────────────────────────────────────────
# ЗАДАЧА, КОТОРУЮ НЕЛЬЗЯ ЗАКРЫТЬ РАССУЖДЕНИЕМ: МЕНЯЕТ ЛИ СВОП АКТИВАЦИИ
# ─────────────────────────────────────────────────────────────────────────────
#
# `ACTIVATIONS_GB` ПОСЧИТАНЫ по формуле для конфигурации БЕЗ блоксвопа
# (77 кадров -> 2.03 ГиБ, 49 -> 1.32). ~~«снималось»~~ — так было написано
# здесь до 18.08.2026, и это подавало расчёт замером: пик памяти на карте не
# мерил никто.
# Владелец полагает, что своп их не меняет, и рассуждение за это есть: своп
# двигает ВЕСА, а активации — промежуточные тензоры счёта, их геометрия от
# места хранения весов не зависит.
#
# РАССУЖДЕНИЕ — НЕ ЗАМЕР, И ЗДЕСЬ ОНО НЕ ЗАСЧИТЫВАЕТСЯ. Известны как минимум
# три механизма, по которым оно может не сработать, и ни один не проверен:
#   1. закреплённые (pinned) буферы на стороне хоста и очередь копирования
#      живут в том же аллокаторе, и `max_memory_allocated` их видит;
#   2. предзагрузка следующего блока делает пик «активации + два блока»
#      одновременным, а не последовательным — у нас это учтено отдельно
#      (`IN_FLIGHT_BLOCKS`), но ТОЛЬКО как веса;
#   3. обёртка при свопе может менять путь внимания (offload вынуждает
#      материализовать то, что иначе жило бы в регистрах).
#
# Поэтому здесь ПРИБОР, а не вывод: без замера он отвечает «не смогли» и
# печатает, ЧТО и КАК мерить; с замером — сходится или нет, числом.

#: Насколько замер при свопе может разойтись с замером без свопа, чтобы это
#: ещё считалось «своп активации не меняет», ГиБ.
#: ВЫБРАНО 0.15 (кем: смена 18.08; из чего: 0.15 ГиБ — заметно меньше одного
#: блока Q4_K_M (0.250 ГиБ), то есть порог различает «шум замера» и «на карте
#: лежит лишний блок». Не измерено: разброса повторных замеров у нас нет).
#: СТОП-УСЛОВИЕ (Ц9): первый прогон на карте печатает оба пика; если разброс
#: двух прогонов ОДНОЙ конфигурации окажется больше 0.15 ГиБ, порог негоден и
#: переписывается по наблюдённому разбросу, а не подгоняется под ответ.
ACTIVATIONS_SWAP_TOLERANCE_GB = 0.15

#: ЧЕМ ЭТО МЕРЯЕТСЯ. Текст здесь затем, чтобы следующая смена не изобретала
#: процедуру заново и не мерила не то. Ключевое — вычесть веса: `nvidia-smi`
#: показывает ВСЁ занятое, а веса при свопе и без свопа разные по построению,
#: и без вычитания разница весов была бы прочитана как разница активаций.
ACTIVATIONS_SWAP_MEASUREMENT = (
    "два прогона ОДНОГО ролика (77 кадров, Q4_K_M, одно зерно), меняется "
    "ровно один вход графа — `blocks_to_swap`: 0 и 38.\n"
    "  1) фон:  nvidia-smi --query-gpu=memory.used --format=csv,noheader,"
    "nounits -lms 200 > peak_swap0.csv   (и peak_swap38.csv на втором прогоне)\n"
    "  2) пик:  python3 -c \"import sys;print(max(int(l) for l in "
    "open(sys.argv[1]) if l.strip()))\" peak_swap0.csv\n"
    "  3) вычесть веса: fork_preflight.blockswap_weights(10.71, 0)['vram_gb'] "
    "и то же для 38 — остаток и есть активации плюс резерв Comfy;\n"
    "  4) подать оба остатка сюда: activations_under_swap(остаток_при_свопе, "
    "baseline_gb=остаток_без_свопа).\n"
    "ПРИ ЧЁМ ЗДЕСЬ БЛОКСВОП=0: это негативный контроль самого замера — если "
    "два прогона при 0 разойдутся больше порога, мерить нечем, и никакая "
    "разница при 38 ничего не докажет."
)


def activations_under_swap(measured_gb: float | None = None, *,
                           length: int = 77,
                           baseline_gb: float | None = None,
                           tolerance_gb: float | None = None) -> dict:
    """Меняет ли блоксвоп расход на активации. ТРИ ИСХОДА, и по умолчанию третий.

    * `не смогли` — замера нет (обычный случай: карты нет). Печатает, что и
      как мерить, а НЕ догадку владельца, поданную как ответ;
    * `годно` — замер при свопе сошёлся с замером без свопа: гипотеза
      «своп активации не трогает» ЭТИМ ЗАМЕРОМ подтверждена;
    * `не годно` — разошёлся: `ACTIVATIONS_GB` мерились не той конфигурации, и
      бюджет памяти врёт на разницу.
    """
    tolerance_gb = (ACTIVATIONS_SWAP_TOLERANCE_GB if tolerance_gb is None
                    else tolerance_gb)
    base = ACTIVATIONS_GB.get(length) if baseline_gb is None else baseline_gb
    if base is None:
        return {"outcome": UNMEASURED, "delta_gb": None, "baseline_gb": None,
                "note": (f"активации для длины {length} не сняты и без свопа; "
                         f"известны {sorted(ACTIVATIONS_GB)} — сравнивать не с "
                         f"чем")}
    if measured_gb is None:
        return {
            "outcome": UNMEASURED, "delta_gb": None, "baseline_gb": base,
            "measured_gb": None, "tolerance_gb": tolerance_gb,
            "how": ACTIVATIONS_SWAP_MEASUREMENT,
            "note": (
                f"НЕ ПРОВЕРЕНО и проверить нечем: карты нет. Известно ровно "
                f"одно — {base} ГиБ на {length} кадрах ПОСЧИТАНЫ по формуле "
                f"для конфигурации БЕЗ свопа (не замер). "
                f"Рассуждение «своп двигает веса, а не активации» правдоподобно "
                f"и в вердикт НЕ ЗАСЧИТЫВАЕТСЯ: три механизма, по которым оно "
                f"может не сработать, перечислены выше и ни один не закрыт. "
                f"Что мерить — в поле `how`, порог схождения "
                f"{tolerance_gb} ГиБ"),
        }
    if not isinstance(measured_gb, (int, float)) or isinstance(measured_gb, bool) \
            or measured_gb < 0:
        return {"outcome": UNMEASURED, "delta_gb": None, "baseline_gb": base,
                "note": f"замер {measured_gb!r} — не похоже на гигабайты"}
    delta = round(measured_gb - base, 3)
    agrees = abs(delta) <= tolerance_gb
    return {
        "outcome": PASS if agrees else FAIL,
        "delta_gb": delta, "baseline_gb": base, "measured_gb": measured_gb,
        "tolerance_gb": tolerance_gb, "length": length,
        "note": (
            f"замер при свопе {measured_gb} против {base} без свопа: разница "
            f"{delta:+} ГиБ при пороге {tolerance_gb}. "
            + ("СОШЛОСЬ — на этом замере блоксвоп активации не меняет, "
               "и догадка владельца подтверждена ЗАМЕРОМ, а не рассуждением."
               if agrees else
               f"РАЗОШЛОСЬ — `ACTIVATIONS_GB` посчитаны не для той конфигурации, "
               f"которую мы запускаем, и бюджет памяти врёт на {delta:+} ГиБ. "
               f"Чинить надо ACTIVATIONS_GB, а не порог.")),
    }


# ─────────────────────────────────────────────────────────────────────────────
# ПРОВАЛИВШИЙСЯ НЕГАТИВНЫЙ КОНТРОЛЬ ОБУЧЕНИЯ: ОБЪЯСНЯЕТ ЛИ ЕГО БЛОКСВОП
# ─────────────────────────────────────────────────────────────────────────────
#
# `TRAIN_MODEL_DISAGREES_WITH_VENDOR` записал: наш расчёт даёт 54.3 ГБ там, где
# вендор пишет «1×80G не тянет». Появившийся блоксвоп — первый кандидат в
# объяснение, и его надо было проверить, а не отложить.
#
# ОТВЕТ: НЕ ОБЪЯСНЯЕТ, И ЗНАК РАСХОЖДЕНИЯ ЭТО ЗАКРЫВАЕТ НАСМЕРТЬ. Блоксвоп
# только УМЕНЬШАЕТ резидентные веса, а наше число и без него МЕНЬШЕ вендорского
# порога. Учесть своп — значит уехать от вендора ещё дальше, а не приблизиться.
# Записано отрицательным результатом с числами (И6), а не мнением: функция
# ниже считает обе стороны и печатает, на сколько разрыв РАСТЁТ.


def blockswap_explains_training_gap(*, vendor_gb: float = 80.0,
                                    base: str = "bf16",
                                    mode: str = "видео 81 кадр",
                                    rank: int = 32,
                                    blocks_to_swap: int | None = None) -> dict:
    """Объясняет ли блоксвоп разрыв нашей модели обучения с вендором.

    ТРИ ИСХОДА (Р1): `годно` — своп сближает наш расчёт с вендорским
    утверждением (разрыв по модулю уменьшается); `не годно` — не сближает или
    уводит дальше; `не смогли` — вход не считается.

    НЕГАТИВНЫЙ КОНТРОЛЬ ВСТРОЕН В СИГНАТУРУ (И5): `vendor_gb` — параметр. При
    вендорском 80 прибор обязан сказать «нет», при пороге НИЖЕ нашего расчёта
    (например 30) — «да», потому что там своп разрыв действительно закрывает.
    Прибор, умеющий только «нет», не отличается от заглушки.
    """
    ours = training_budget(base=base, mode=mode, rank=rank)
    if ours["outcome"] == UNMEASURED and ours["total_gb"] is None:
        return {"outcome": UNMEASURED, "explains": None, "note": ours["note"]}
    if not isinstance(vendor_gb, (int, float)) or isinstance(vendor_gb, bool) \
            or vendor_gb <= 0:
        return {"outcome": UNMEASURED, "explains": None,
                "note": f"порог вендора {vendor_gb!r} — не гигабайты"}
    b = (_graph_defaults()["blocks_to_swap"] if blocks_to_swap is None
         else blocks_to_swap)
    swapped = blockswap_weights(TRAIN_BASE_GB[base], b)
    if swapped["outcome"] != PASS:
        return {"outcome": UNMEASURED, "explains": None,
                "note": swapped["note"]}

    total_resident = ours["total_gb"]
    total_swapped = round(total_resident - TRAIN_BASE_GB[base]
                          + swapped["vram_gb"], 2)
    gap_before = round(vendor_gb - total_resident, 2)
    gap_after = round(vendor_gb - total_swapped, 2)
    explains = abs(gap_after) < abs(gap_before)
    return {
        "outcome": PASS if explains else FAIL, "explains": explains,
        "vendor_gb": vendor_gb, "blocks_to_swap": b,
        "ours_resident_gb": total_resident, "ours_swapped_gb": total_swapped,
        "gap_before_gb": gap_before, "gap_after_gb": gap_after,
        "gap_change_gb": round(abs(gap_after) - abs(gap_before), 2),
        "note": (
            f"их конфигурация {base} + {mode}, порог вендора {vendor_gb} ГБ. "
            f"Наш расчёт без свопа {total_resident}, со свопом {b} блоков "
            f"{total_swapped} (база {TRAIN_BASE_GB[base]} -> "
            f"{swapped['vram_gb']} на карте, {swapped['ram_gb']} уезжает в "
            f"оперативную). Разрыв с вендором был {gap_before}, стал "
            f"{gap_after}. "
            + (f"БЛОКСВОП РАЗРЫВ СБЛИЖАЕТ на "
               f"{round(abs(gap_before) - abs(gap_after), 2)} ГБ" if explains else
               f"БЛОКСВОП РАЗРЫВ НЕ ОБЪЯСНЯЕТ: он уменьшает расход, а наш "
               f"расчёт и так НИЖЕ вендорского порога — учёт свопа уводит "
               f"дальше ещё на {round(abs(gap_after) - abs(gap_before), 2)} ГБ. "
               f"Отрицательный результат (И6): объяснено 0 ГБ из "
               f"{abs(gap_before)}. Искать надо в активациях на видео, в "
               f"чекпоинтах градиентов, в батче и в копиях DeepSpeed — "
               f"то есть там, где расход РАСТЁТ, а не падает")),
    }

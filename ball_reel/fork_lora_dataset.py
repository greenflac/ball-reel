"""LoRA темплейта: набор под ОДНУ фигуру, из которого убраны лица — и это измерено.

ЧТО ЭТА LoRA ДЕЛАЕТ И ЧЕГО НЕ ДЕЛАЕТ. Она привязана к темплейту и отвечает за
телосложение и домен отрисовки. Учится один раз при сборке темплейта; обучения
на клиента нет. Личность приходит НЕ отсюда: под неё у модели выделен
`face_adapter` на 1.5628 ГБ и вход `reference_image`.

---

LoRA ОДНА, А НЕ ДВЕ, И ЭТО НЕ ЭКОНОМИЯ.

MVP-темплейт объявлен «две фигуры»: как в драйвинге и полная женщина 40+.
Второе плечо набора НЕ ТРЕБУЕТ (ХЭНДОФ §2a): телосложение драйвинга приходит
само — скелет задаёт пропорции, маска задаёт силуэт, модель рисует человека,
который в них укладывается. «Как в драйвинге» есть ОТСУТСТВИЕ LoRA, а не вторая
LoRA. Значит обучается ровно одна — `THE_ONLY_FIGURE`, — и попытка собрать
набор под второе плечо здесь отвергается кодом (`refuse_baseline_build`), а не
предупреждением в документе: набор под «как в драйвинге» состоял бы из кадров
актёра драйвинга и был бы LoRA личности на не того человека.

---

НАБОР МОЖНО И ЛУЧШЕ ПОРОЖДАТЬ ИЗ КАДРОВ ДРАЙВИНГА — ЦЕНОЙ ОБЯЗАТЕЛЬНОГО ПРИБОРА.

ХЭНДОФ §2a: у кадров драйвинга совпадают поза, кадрировка, свет и манера
отрисовки, то есть посторонние объяснения снимаются сами. Прежняя формулировка
§6 G «набор НЕ из драйвинга» снята решением владельца как более грубая, чем
нужно. Но на каждом таком кадре ЛИЦО АКТЁРА, поэтому вырезание лиц перестаёт
быть предосторожностью и становится единственным, что отделяет такой набор от
LoRA личности на не того человека.

Отсюда `from_driving=True` в `build` не пометка, а РЕЖИМ С ДОПОЛНИТЕЛЬНЫМ
ПРИБОРОМ: перед сборкой детектор запускается на ИСХОДНЫХ, НЕОБРЕЗАННЫХ кадрах и
обязан там лицо НАЙТИ (`driving_face_control`). Причина арифметическая: на
кадрах драйвинга лицо есть по построению, поэтому детектор, не нашедший его ни
на одном исходнике, молчит — и его «лиц нет» на кропах не значит ничего. Без
этого контроля набор из драйвинга уходит в исход «не смогли проверить», а не в
«годен».

---

«МНОГО РАЗНЫХ ЛЮДЕЙ, А НЕ ОДИН» — ТОЖЕ УЧЁТ, А НЕ ПОЖЕЛАНИЕ.

Категория усредняет любого отдельного человека только если людей в наборе
много. Набор из одного человека — это LoRA личности, как её ни назови, и кропы
без лиц от этого не спасают: телосложение конкретного человека тоже опознаваемо.
Поэтому `build` принимает `sources` — по метке ЧЕЛОВЕКА на каждый кадр — и
считает `source_mix`. Меток не подали — исход «не смогли проверить», не «годен».

Здесь же цена режима из драйвинга, названная прямо: один драйвинг-ролик — это
ОДИН человек, поэтому набор, собранный из одного ролика, отвергается по
`source_mix`, даже если все лица вырезаны безупречно. Из драйвинга собирать
можно — из одного драйвинга нельзя.

---

ГЛАВНАЯ ОПАСНОСТЬ, РАДИ КОТОРОЙ НАПИСАН МОДУЛЬ, И ПОЧЕМУ ОНА НЕ ЛЕЧИТСЯ
ОБЕЩАНИЕМ.

LoRA, обученная на изображениях «женщин за 40», выучит и их ЛИЦА — и потянет
лицо клиента к среднему по категории. Это тот же риск утечки, что убил LoRA
личности, только источник другой: не актёр драйвинга, а сама категория.

Лечение направлением известно — «учить на кропах тела с исключёнными лицами».
Но НАПРАВЛЕНИЕ НЕ ЕСТЬ ГАРАНТИЯ. Кроп «ниже шеи» промахивается ровно там, где
это дороже всего: наклон, поворот, лежащая поза, второй человек в кадре, отражение.
Набор, про который СКАЗАНО «лиц нет», и набор, в котором лиц нет, — разные вещи,
и отличить их можно только прибором.

ПОЭТОМУ ЗДЕСЬ ЛИЦА НЕ «ИСКЛЮЧАЮТСЯ», А ИСКЛЮЧАЮТСЯ И ПРОВЕРЯЮТСЯ. Каждый
готовый кроп прогоняется ТЕМ ЖЕ детектором лиц, которым судится личность
(`identity_arcface`), и кроп, где детектор нашёл лицо, В НАБОР НЕ ИДЁТ. Не
«помечается», не «понижается в весе» — отбрасывается.

Негативный контроль обязателен и стоит рядом (И5): на кропе, СОДЕРЖАЩЕМ лицо,
проверка обязана сработать. Проверка, которая всегда говорит «лиц нет», говорит
это и про набор, полный лиц.

---

ПОЧЕМУ НИЗКИЙ РАНГ — ОБОСНОВАНИЕ, А НЕ ОБЪЯВЛЕНИЕ.

Ранг LoRA ограничивает размерность добавки. Телосложение и домен отрисовки —
низкочастотные, крупномасштабные свойства: пропорции силуэта, распределение
объёма, характер штриха. Личность — высокочастотное и мелкомасштабное: взаимное
положение черт в области в несколько сотен пикселей.

Низкий ранг поэтому работает не как «поменьше на всякий случай», а как
СТРУКТУРНОЕ ограничение ёмкости: адаптеру, которому дали мало направлений,
дешевле выучить общую форму, чем набор конкретных лиц. Это АРГУМЕНТ, а не
замер — на нашем материале он не проверялся, и проверяется он ровно одним
вопросом приёмки: портится ли `d_raw` при включённой LoRA.

ЧИСЛО. `DEFAULT_RANK = 16`, и оно ВЫБРАНО, а не измерено. Опорная точка,
прочитанная из файла тренера, а не по памяти: у `DiffSynth-Studio`
`examples/wanvideo/model_training/train.py:15` умолчание `lora_rank=32`
(команда — в `docs/FORK_LORA.md`). Берём вдвое ниже умолчания, потому что
умолчание рассчитано на задачу «выучить облик», а наша — «выучить сложение»,
и ёмкость здесь ограничивают нарочно. Двигать это число можно только замером
`d_raw` с LoRA и без.

---

ПРИЁМКА ДО ВСЯКОГО РЕНДЕРА — `actor_memorisation_probe`.

ХЭНДОФ §2a и §6 G: сгенерировать несколько картинок ОДНОЙ LoRA БЕЗ РЕФЕРЕНСА и
замерить расстояние до лица актёра драйвинга. Проступило лицо актёра — набор
негоден, и это видно за минуты вместо полного прогона пайплайна. Без референса
принципиально: с ним `face_adapter` перекрыл бы вклад LoRA и опыт измерял бы не
LoRA.

Порождение картинок требует карты и здесь НЕ ДЕЛАЕТСЯ — функция судит уже
порождённые. Судейская логика при этом проверяема без всякой карты и проверена:
подан кадр с лицом актёра — обязана сработать; подано чужое лицо — обязана
молчать. Оба случая в тестах, живьём на пикселях.

Тренер LoRA выбран не здесь: `docs/FORK_LORA.md` §1 (DiffSynth-Studio —
единственный из четырёх, кто вообще называет Animate: 36 упоминаний против 0).

---

НЕПРОВЕРЕНО (Ц4), наверх:

* **опыт «LoRA поверх Wan-Animate против референса без LoRA» не проводился.**
  Всё, что здесь сказано про то, кто победит в споре личности и телосложения, —
  аргументы, не замеры;
* **обучение не запускалось.** Модуль собирает НАБОР и проверяет его. Что на
  таком наборе LoRA выучит телосложение, здесь не показано;
* **`d_raw` с включённой LoRA не измерен** — измерить его без карты нельзя;
* **картинки для `actor_memorisation_probe` ни разу не порождались** — нужна
  карта. Проверена СУДЕЙСКАЯ ЧАСТЬ, а не результат приёмки.
"""

from __future__ import annotations

import json
from collections import Counter
from pathlib import Path

from . import fork_channels
from .fork_identity import (DEFAULT_INSTRUMENT, FAIL, MIN_COVERAGE, PASS,
                            UNMEASURED)

#: Кадрировки. НЕ СВОЙ список: берётся у `skeleton`, потому что это один и тот
#: же словарь понятий, и разъехавшись, он дал бы набор под кадрировку, которой
#: пайплайн не умеет строить (Е1).
from .skeleton import FRAMINGS  # noqa: E402

#: Под какую кадрировку собирается набор. **Во весь рост — решение владельца**
#: (ХЭНДОФ §2), и набор обязан идти за ним: если темплейт отдаёт полный рост, а
#: набор собран по пояс, LoRA учится на одном масштабе тела и применяется на
#: другом.
#:
#: ЦЕНА ЭТОГО РЕШЕНИЯ ИЗМЕРЕНА И ЗАКРЫВАЕТСЯ НЕ ЗДЕСЬ. Полный рост при высоте
#: 848 даёт лицо 63–80 px против бара ArcFace в 100 px для видео — то есть
#: покадровая ось личности будет чаще всего возвращать «СУДИТЬ НЕЧЕМ», и это
#: отсутствие измерения, а не провал. Лечится не кадрировкой набора, а
#: доводкой лица на выходе, которая по ХЭНДОФ §2 объявлена несущим шагом
#: стека; личность меряется ПОСЛЕ неё.
#:
#: Числа не наши и здесь не пересчитываются: `skeleton.face_share()` и
#: `identity_arcface`, сведены в ХЭНДОФ §4.
#:
#: DEBT(2026-08-17): здесь стояло `waist_up` — я поставил его по предыдущей
#: редакции брифинга, где кадрировка ещё не была решена владельцем. Финальный
#: хэндоф решает иначе; перечёркнуто, а не стёрто, потому что тесты на
#: `waist_up` остаются годными и нужны, если решение когда-нибудь вернётся.
DEFAULT_FRAMING = "full_body"

#: Ранг адаптера. ВЫБРАНО — обоснование в докстринге модуля. Опорная точка:
#: умолчание тренера 32 (прочитано из train.py:15), берём вдвое ниже.
DEFAULT_RANK = 16

#: Умолчание тренера, ИЗМЕРЕНО чтением файла. Держится рядом с нашим числом,
#: чтобы «вдвое ниже умолчания» можно было проверить, а не принять на слово.
TRAINER_DEFAULT_RANK = 32

#: Сколько отступить ВНИЗ от самой нижней точки головы, в долях высоты кропа
#: тела. Не ноль: шея и подбородок дают детектору достаточно, чтобы поймать
#: лицо, а нам они не нужны — телосложение начинается с плеч.
#: ВЫБРАНО 0.04; проверяется не этим числом, а прибором ниже.
CHIN_MARGIN = 0.04

#: Минимальная сторона кропа, px. Кроп мельче не несёт телосложения — он несёт
#: шум масштабирования. ВЫБРАНО.
MIN_CROP_PX = 256

#: Доля кадров, которую допустимо потерять на отбраковке, прежде чем набор
#: считается негодным. ВЫБРАНО: если больше половины кропов не прошли проверку
#: на лица, значит кадрирование промахивается систематически, и чинить надо
#: его, а не добирать примеры.
MAX_REJECT_SHARE = 0.5

#: Минимальный размер набора. ВЫБРАНО: ниже этого разговор о телосложении
#: ведётся по единичным примерам.
MIN_SAMPLES = 12

#: Единственная фигура, под которую этот модуль собирает набор. Вторая фигура
#: MVP («как в драйвинге») — ОТСУТСТВИЕ LoRA, см. докстринг модуля и ХЭНДОФ §2a.
THE_ONLY_FIGURE = "полная женщина 40+"

#: Как второе плечо называют в разговоре и в конфигах. Список нужен потому, что
#: `build_type` — свободный текст: без него набор под базовую линию собрался бы
#: молча, из кадров актёра драйвинга, то есть стал бы LoRA личности.
#: Сверка идёт по нормализованной подстроке, а не по точному равенству.
BASELINE_NAMES = ("как в драйвинге", "как в драйвинг", "as driving",
                  "baseline", "базовая линия", "без lora", "no lora",
                  "нулевое плечо", "первое плечо")

#: Сколько РАЗНЫХ людей обязано быть в наборе. ВЫБРАНО 5, не измерено, и вот из
#: чего выбрано: при ровном делении пять человек означают, что ни один не даёт
#: больше 20% примеров, то есть любая индивидуальная черта требует пяти
#: независимых подтверждений, чтобы выглядеть типичной для категории. Числу
#: нужен замер, и замер у него ровно один — `d_drv` на картинках, порождённых
#: обученной LoRA (`actor_memorisation_probe`). До него двигать нечем.
MIN_DISTINCT_PEOPLE = 5

#: Доля набора, которую допустимо отдать ОДНОМУ человеку. ВЫБРАНО 0.4: одного
#: счётчика людей мало — пять человек, где один даёт 90% кадров, это набор из
#: одного человека с четырьмя свидетелями. 0.4 — чуть меньше половины: ни один
#: человек не должен быть большинством набора.
MAX_ONE_PERSON_SHARE = 0.4

#: Сколько ИСХОДНЫХ кадров драйвинга проверяется позитивным контролем (И5).
#: ВЫБРАНО 3: контроль отвечает на вопрос «прибор вообще шевелится», а не
#: «сколько лиц в ролике», и каждый кадр стоит одного прогона детектора. Один
#: кадр слишком легко оказывается тем, где актёр отвернулся.
DRIVING_CONTROL_SAMPLE = 3

#: Сколько картинок нужно порождать для приёмки до рендера. ВЫБРАНО 4: одна
#: картинка отличает удачу от свойства LoRA ничем, а порождение идёт на карте и
#: стоит времени. Число — низ, а не рекомендация.
MIN_PROBE_IMAGES = 4


class FaceLeak(ValueError):
    """В готовом кропе найдено лицо. Набор с таким кропом не собирается."""


class BaselineHasNoDataset(ValueError):
    """Просят набор под второе плечо. У второго плеча набора нет — оно без LoRA."""


def _instrument():
    from . import identity_arcface

    return identity_arcface


def body_box(points, width: int, height: int,
             framing: str = DEFAULT_FRAMING) -> tuple | None:
    """Прямоугольник тела БЕЗ головы, в пикселях, или None.

    Верхняя граница — ниже самой нижней точки головы, а не «на уровне плеч»:
    при наклоне плечо оказывается ВЫШЕ подбородка, и рез по плечам оставил бы
    в кадре половину лица. Голова берётся целиком — 68 лицевых точек плюс
    нос, глаза и уши COCO, — потому что край челюсти в лицевые точки входит,
    а ухо нет.

    НИЖНЯЯ граница зависит от КАДРИРОВКИ, и это не украшение. Набор собирается
    под ту кадрировку, которую темплейт реально даёт: если темплейт по пояс, а
    набор в рост, LoRA учится на одном масштабе тела, а применяется на другом.
    `waist_up` режет по тазу — ровно там же, где `skeleton._window`, и по той
    же причине, что записана там: опущенные руки иначе вернут кадр к полному
    росту и вся затея развалится.
    """
    # Негодный аргумент отвергается ДО любой работы (П2): проверка стоит
    # микросекунды, а стояла она ниже разбора точек.
    if framing not in FRAMINGS:
        raise ValueError(
            f"неизвестная кадрировка {framing!r}; бывают только {FRAMINGS}")
    if not points:
        return None
    n = len(points)

    def seen(indices):
        return [points[i] for i in indices
                if i < n and points[i][2] >= fork_channels.MIN_SCORE]

    head = seen(list(fork_channels.group_indices("face")) + list(range(0, 5)))
    body = seen([i for i in fork_channels.channel_indices("body")
                 if i >= 5])
    if not body:
        return None

    xs = [p[0] for p in body]
    ys = [p[1] for p in body]
    x0, x1 = max(0.0, min(xs)), min(float(width), max(xs))
    if framing == "waist_up":
        hips = [points[i][1] for i in (11, 12)
                if i < n and points[i][2] >= fork_channels.MIN_SCORE]
        if not hips:
            return None
        y1 = min(float(height), max(hips))
    else:
        y1 = min(float(height), max(ys))

    if head:
        chin = max(p[1] for p in head)
        y0 = chin + CHIN_MARGIN * max(1.0, y1 - chin)
    else:
        # Головы не видно — резать не от чего. Это НЕ «лица нет»: голова могла
        # быть просто не распознана. Верх берётся по телу, а решает прибор.
        y0 = max(0.0, min(ys))

    y0 = max(0.0, min(y0, float(height)))
    if x1 - x0 < 1 or y1 - y0 < 1:
        return None
    return (x0, y0, x1, y1)


def face_free(path: str | Path) -> dict:
    """Есть ли на изображении лицо, по мнению ТОГО ЖЕ прибора, что судит личность.

    Три исхода, и третий не декоративный: прибор, который не смог отработать,
    не должен читаться как «лиц нет». Именно так набор с лицами и прошёл бы.
    """
    try:
        detail = _instrument().face_detail(path)
    except Exception as exc:  # noqa: BLE001
        return {"outcome": UNMEASURED, "face_px": None,
                "note": f"прибор не отработал: {str(exc)[:120]}"}
    if detail is None:
        return {"outcome": PASS, "face_px": None, "note": "лица не найдено"}
    return {"outcome": FAIL, "face_px": detail["face_px"],
            "note": (f"найдено лицо {detail['face_px']}px — кроп в набор не "
                     f"идёт")}


def crop_sample(frame_path: str | Path, out_path: str | Path,
                *, framing: str = DEFAULT_FRAMING) -> dict:
    """Один кроп тела без головы, ПРОВЕРЕННЫЙ прибором. Три исхода.

    Проверка идёт ПОСЛЕ записи кропа и по самому файлу, а не по намерению:
    геометрия могла промахнуться, изображение могло быть перевёрнуто, поза
    могла быть лежачей. Судить надо то, что поедет в обучение.
    """
    from PIL import Image

    src = Path(frame_path)
    points = fork_channels.wholebody_points(src)
    if points is None:
        return {"outcome": UNMEASURED, "path": None,
                "note": f"{src.name}: человека не нашли, резать нечего"}

    with Image.open(src) as im:
        rgb = im.convert("RGB")
        box = body_box(points, *rgb.size, framing=framing)
        if box is None:
            return {"outcome": UNMEASURED, "path": None,
                    "note": f"{src.name}: тела в кадре нет"}
        x0, y0, x1, y1 = (int(v) for v in box)
        if min(x1 - x0, y1 - y0) < MIN_CROP_PX:
            return {"outcome": FAIL, "path": None, "reason": "too_small",
                    "note": (f"{src.name}: кроп {x1 - x0}x{y1 - y0} мельче "
                             f"{MIN_CROP_PX}px — это шум масштабирования, а не "
                             f"телосложение")}
        out = Path(out_path)
        out.parent.mkdir(parents=True, exist_ok=True)
        rgb.crop((x0, y0, x1, y1)).save(out)

    checked = face_free(out)
    if checked["outcome"] != PASS:
        out.unlink(missing_ok=True)
        return {"outcome": checked["outcome"], "path": None,
                "reason": "face" if checked["outcome"] == FAIL else "unmeasured",
                "note": f"{src.name}: {checked['note']}"}
    return {"outcome": PASS, "path": str(out), "box": (x0, y0, x1, y1),
            "reason": "kept",
            "note": f"{src.name}: кроп {x1 - x0}x{y1 - y0}, лица не найдено"}


def _normalise(text: str) -> str:
    """Свободный текст к виду, по которому сверяют имена плеча."""
    lowered = str(text).lower().replace("-", " ").replace("_", " ")
    return " ".join(lowered.split())


def refuse_baseline_build(build_type: str) -> None:
    """Уронить сборку набора под ВТОРОЕ плечо. У него набора нет (ХЭНДОФ §2a).

    Почему это код, а не абзац. `build_type` — свободный текст, и заказ «собери
    набор как в драйвинге» выглядит совершенно обычно. Выполненный, он даёт
    набор из кадров одного актёра драйвинга — то есть LoRA личности на не того
    человека, ровно то, что §2a запрещает. Отказ стоит микросекунды и стоит
    ДО любой работы (П2).
    """
    text = _normalise(build_type)
    for name in BASELINE_NAMES:
        if _normalise(name) in text:
            raise BaselineHasNoDataset(
                f"набор под {build_type!r} не собирается: второе плечо MVP — "
                f"ОТСУТСТВИЕ LoRA, а не вторая LoRA (ХЭНДОФ §2a). "
                f"Телосложение драйвинга приходит скелетом и маской само. "
                f"Обучается одна LoRA: {THE_ONLY_FIGURE!r}.")


def source_mix(sources) -> dict:
    """Сколько РАЗНЫХ людей в наборе. Три исхода, и отсутствие учёта — третий.

    Кропы без лиц не делают набор категориальным. Телосложение конкретного
    человека опознаваемо и без лица, поэтому набор, сведённый к одному
    человеку, — это LoRA личности, как её ни назови. Отсюда две проверки, а не
    одна: сколько людей всего (`MIN_DISTINCT_PEOPLE`) и не забрал ли один
    человек набор себе (`MAX_ONE_PERSON_SHARE`). Пять человек, где один даёт
    девять кадров из десяти, — набор из одного человека.

    Меток не подали — исход «не смогли проверить». Это не придирка: «источники
    неизвестны» и «источники разные» неразличимы для того, кто читает вердикт,
    и слив их в PASS означал бы, что учёт можно не вести.
    """
    if sources is None:
        return {"outcome": UNMEASURED, "people": None, "frames": 0,
                "top_share": None, "problems": [],
                "note": ("учёт источников не подан: сколько разных людей в "
                         "наборе — неизвестно. Это не «много» и не «один»")}
    items = [s for s in sources if s is not None]
    if not items:
        return {"outcome": UNMEASURED, "people": 0, "frames": 0,
                "top_share": None, "problems": [],
                "note": "в наборе нет ни одного кадра с меткой источника"}

    counts = Counter(items)
    top_label, top_n = counts.most_common(1)[0]
    top_share = round(top_n / len(items), 3)
    problems = []
    if len(counts) < MIN_DISTINCT_PEOPLE:
        problems.append(
            f"разных людей {len(counts)}, нужно хотя бы {MIN_DISTINCT_PEOPLE}: "
            f"категория усредняет любого отдельного человека только если людей "
            f"много")
    if top_share > MAX_ONE_PERSON_SHARE:
        problems.append(
            f"источник {top_label!r} даёт {top_share:.0%} набора при пределе "
            f"{MAX_ONE_PERSON_SHARE:.0%} — набор сводится к одному человеку")
    return {"outcome": FAIL if problems else PASS,
            "people": len(counts), "frames": len(items),
            "top_source": top_label, "top_share": top_share,
            "per_source": dict(counts), "problems": problems,
            "note": (f"разных людей {len(counts)} на {len(items)} кропов, "
                     f"самый частый {top_label!r} — {top_share:.0%}. "
                     + ("; ".join(problems) if problems else "смесь годна"))}


def driving_face_control(frame_paths, *,
                         sample: int = DRIVING_CONTROL_SAMPLE) -> dict:
    """Позитивный контроль прибора на ИСХОДНЫХ кадрах драйвинга (И5).

    Арифметика простая и вся суть в ней: на кадрах драйвинга лицо актёра есть
    ПО ПОСТРОЕНИЮ. Значит детектор, не нашедший лица ни на одном необрезанном
    исходнике, не «подтвердил чистоту» — он молчит, и его «лиц нет» на кропах
    не значит ровно ничего. Без этого контроля набор из драйвинга годен ровно
    настолько, насколько годен молчащий прибор.

    Возвращает PASS, только если лицо НАЙДЕНО хотя бы на одном исходном кадре.
    """
    frames = list(frame_paths)[:sample]
    found, blind, unmeasured = [], [], []
    for p in frames:
        got = face_free(p)
        if got["outcome"] == FAIL:
            found.append(f"{Path(p).name}: лицо {got['face_px']}px")
        elif got["outcome"] == UNMEASURED:
            unmeasured.append(f"{Path(p).name}: {got['note']}")
        else:
            blind.append(Path(p).name)

    if not frames:
        return {"outcome": UNMEASURED, "checked": 0, "found": 0,
                "note": "контроль не на чем ставить: кадров не подано"}
    if found:
        return {"outcome": PASS, "checked": len(frames), "found": len(found),
                "note": (f"прибор шевелится: на исходных кадрах драйвинга "
                         f"найдено {len(found)} лицо(а) из {len(frames)} "
                         f"проверенных — {'; '.join(found)}")}
    return {"outcome": UNMEASURED, "checked": len(frames), "found": 0,
            "unmeasured": unmeasured,
            "note": (f"на {len(frames)} исходных кадрах драйвинга прибор не "
                     f"нашёл ни одного лица, хотя актёр там есть по "
                     f"построению: прибор молчит, и «лиц нет» на кропах "
                     f"ничего не значит")}


def build(frame_paths, out_dir: str | Path, *, build_type: str,
          domain: str, sources=None, from_driving: bool = False,
          rank: int = DEFAULT_RANK,
          framing: str = DEFAULT_FRAMING) -> dict:
    """Собрать набор ОДНОЙ LoRA и написать его паспорт. Отчёт числами (Р2).

    `build_type` и `domain` — ВХОД, а не вывод. Классификатор телосложения
    здесь не строится: для MVP тип фигуры выбирает человек, а неверный выбор
    виден глазом. Второе плечо MVP отвергается сразу — у него набора нет.

    `sources` — метка ЧЕЛОВЕКА на каждый входной кадр, той же длины, что
    `frame_paths`. Не имя файла и не номер кадра: сто кадров одного человека —
    это один человек. Метки не подали — набор уходит в «не смогли проверить».

    `from_driving=True` — штатный путь (§2a), а не исключение: у кадров
    драйвинга совпадают поза, свет и манера. Включает позитивный контроль
    детектора на необрезанных исходниках, потому что там лицо есть заведомо.

    Три исхода не сворачиваются: измеренное нарушение даёт FAIL, непроверенное
    даёт UNMEASURED, и в `note` печатаются оба счётчика рядом (Р2).
    """
    refuse_baseline_build(build_type)
    frames = [Path(p) for p in frame_paths]
    labels = None if sources is None else list(sources)
    if labels is not None and len(labels) != len(frames):
        raise ValueError(
            f"меток источников {len(labels)}, а кадров {len(frames)}: учёт "
            f"людей идёт покадрово, иначе он считает не то")

    out = Path(out_dir)
    (out / "img").mkdir(parents=True, exist_ok=True)

    control = (driving_face_control(frames) if from_driving else
               {"outcome": PASS, "checked": 0, "found": 0,
                "note": "набор не из драйвинга — позитивный контроль не нужен"})

    kept, kept_sources = [], []
    face_rejected, unmeasured, too_small = [], [], []
    for i, p in enumerate(frames):
        got = crop_sample(p, out / "img" / f"body_{i:04d}.png",
                          framing=framing)
        if got["outcome"] == PASS:
            kept.append(got["path"])
            if labels is not None:
                kept_sources.append(labels[i])
        elif got["outcome"] == UNMEASURED:
            unmeasured.append(got["note"])
        elif got.get("reason") == "too_small" or "мельче" in got["note"]:
            too_small.append(got["note"])
        else:
            face_rejected.append(got["note"])

    total = len(frames)
    rejected = len(face_rejected)
    reject_share = round(rejected / total, 3) if total else 0.0
    mix = source_mix(None if labels is None else kept_sources)

    problems, blind = [], []
    # Пустой прогон — НЕ маленький набор: не подали ни одного кадра, значит
    # ничего и не мерили. Сворачивать это в FAIL по MIN_SAMPLES означало бы
    # выдавать «набор плохой» там, где верно «набора нет» (Р1). Поймано тестом
    # test_an_empty_run_is_unmeasured_not_a_good_set.
    if total and len(kept) < MIN_SAMPLES:
        problems.append(f"кропов {len(kept)}, нужно хотя бы {MIN_SAMPLES}")
    if reject_share > MAX_REJECT_SHARE:
        problems.append(
            f"на лицах отбраковано {reject_share:.0%} — кадрирование "
            f"промахивается систематически, чинить его, а не добирать примеры")
    if mix["outcome"] == FAIL:
        problems.extend(mix["problems"])
    elif mix["outcome"] == UNMEASURED:
        blind.append(mix["note"])
    if control["outcome"] != PASS:
        blind.append(control["note"])
    if not total:
        blind.append("кадров не подано: сборка не запускалась")

    passport = {
        "build_type": build_type, "domain": domain, "rank": rank,
        "framing": framing, "from_driving": from_driving,
        "trainer_default_rank": TRAINER_DEFAULT_RANK,
        "trainer": ("DiffSynth-Studio — docs/FORK_LORA.md §1: единственный из "
                    "четырёх, кто называет Animate"),
        "samples": len(kept), "from_frames": total,
        "face_rejected": rejected, "too_small": len(too_small),
        "unmeasured": len(unmeasured),
        "people": mix["people"], "top_source_share": mix["top_share"],
        "sources": mix["note"],
        "driving_control": control["note"],
        "acceptance": acceptance_for(domain),
        "one_lora": (f"обучается ОДНА LoRA: {THE_ONLY_FIGURE}. Второе плечо "
                     f"MVP — отсутствие LoRA, набора не требует (ХЭНДОФ §2a)"),
        "faces": ("проверено прибором identity_arcface на КАЖДОМ кропе; "
                  "кроп с найденным лицом в набор не попал"),
        "before_render": ("до передачи в рендер обязателен "
                          "actor_memorisation_probe: картинки одной LoRA без "
                          "референса против лица актёра драйвинга"),
        "unrun": ("НЕПРОВЕРЕНО: обучение не запускалось, d_raw с включённой "
                  "LoRA не измерен, картинки приёмки не порождались"),
    }
    (out / "passport.json").write_text(
        json.dumps(passport, ensure_ascii=False, indent=2), encoding="utf-8")

    return {
        "outcome": (FAIL if problems else UNMEASURED if blind else PASS),
        "kept": kept, "face_rejected": face_rejected,
        "too_small": too_small, "unmeasured": unmeasured,
        "reject_share": reject_share, "passport": passport,
        "sources": mix, "driving_control": control,
        "dir": str(out), "problems": problems, "blind": blind,
        "note": (f"из {total} кадров в набор пошло {len(kept)}; "
                 f"на лицах отбраковано {rejected} ({reject_share:.0%}), "
                 f"мелких {len(too_small)}, не смогли проверить "
                 f"{len(unmeasured)}. "
                 f"Проверок провалено {len(problems)}, "
                 f"не смогли проверить {len(blind)}. "
                 + ("; ".join(problems + blind) if problems or blind
                    else "набор годен")),
    }


def actor_memorisation_probe(probe_images, *, driving_actor: str | Path,
                             control_frame: str | Path | None = None,
                             instrument: str = DEFAULT_INSTRUMENT) -> dict:
    """ПРИЁМКА ДО РЕНДЕРА: не проступает ли лицо актёра драйвинга (ХЭНДОФ §2a).

    ЧТО ПОДАЁТСЯ. `probe_images` — картинки, порождённые ОДНОЙ обученной LoRA
    БЕЗ РЕФЕРЕНСА. Без референса принципиально: с ним `face_adapter` на 1.5628
    ГБ перекрыл бы вклад LoRA, и опыт мерил бы не LoRA. Порождение требует
    карты и здесь не делается — функция судит уже порождённые пиксели, и это
    ровно то разделение, из-за которого судейскую часть можно проверить сегодня
    (см. НЕПРОВЕРЕНО в шапке модуля).

    ЧТО ЗНАЧИТ ВЕРДИКТ. Найденное расстояние до лица актёра внутри бара
    «тот же человек» означает ЗАПОМИНАНИЕ: при малом наборе и высоком ранге
    LoRA не смещает облик, а вставляет актёра (§2a, четвёртый отказ). Такой
    набор негоден, и узнаётся это за минуты вместо полного прогона пайплайна.

    БАР НЕ СВОЙ. Он приходит из `fork_identity.distances`, то есть из
    `identity_arcface.SAME_PERSON_MAX` — тот же, которым судится личность
    везде. Свой бар здесь означал бы, что «тот же человек» на разных осях
    значит разное (Е1).

    ПОЗИТИВНЫЙ КОНТРОЛЬ (`control_frame`) — кадр, где лицо актёра ТОЧНО есть.
    Если прибор не узнаёт актёра там, где актёр заведомо есть, молчание на
    пробах не значит ничего, и весь прогон уходит в «не смогли проверить», а не
    в «чисто». Контроль не подан — это отдельное состояние отчёта, не успех.
    """
    probes = list(probe_images)
    from . import fork_identity

    out = {"outcome": UNMEASURED, "bar": None, "probes": len(probes),
           "control": "НЕ СТАВИЛСЯ", "d_drv": None,
           "unrun": ("НЕПРОВЕРЕНО: картинки этой функцией не порождаются — "
                     "нужна карта; судится уже порождённое")}
    if len(probes) < MIN_PROBE_IMAGES:
        out["note"] = (f"картинок {len(probes)}, нужно хотя бы "
                       f"{MIN_PROBE_IMAGES}: на меньшем числе удача "
                       f"неотличима от свойства LoRA")
        return out

    if control_frame is not None:
        c = fork_identity.distances([control_frame], driving_actor,
                                    instrument=instrument)
        out["bar"] = fork_identity.SAME_PERSON_MAX
        if c.get("inside", 0) < 1:
            out["control"] = (
                f"{UNMEASURED}: на контрольном кадре {Path(control_frame).name} "
                f"прибор не признал актёра (медиана {c.get('median')}) — "
                f"молчание на пробах ничего не значит")
            out["note"] = out["control"]
            return out
        out["control"] = (f"{PASS}: актёр на контрольном кадре узнан, "
                          f"расстояние {c['median']}")

    d = fork_identity.distances(probes, driving_actor, instrument=instrument)
    out["d_drv"] = d
    out["bar"] = fork_identity.SAME_PERSON_MAX
    inside, judged = d.get("inside", 0), d.get("judged", 0)
    tail = (f" Контроль: {out['control']}."
            if control_frame is not None else
            " Позитивный контроль НЕ СТАВИЛСЯ — прибор на этом прогоне не"
            " показал, что вообще умеет узнавать актёра.")

    if inside:
        out["outcome"] = FAIL
        out["note"] = (f"лицо актёра драйвинга ПРОСТУПИЛО: {inside} из "
                       f"{judged} судимых картинок в баре "
                       f"{fork_identity.SAME_PERSON_MAX} (медиана "
                       f"{d['median']}). Набор негоден — это запоминание, а не "
                       f"телосложение." + tail)
        return out
    if judged == 0:
        out["note"] = (f"судить нечем: из {len(probes)} картинок лица нашлись "
                       f"на {judged}. Либо LoRA лиц не рисует, либо прибор их "
                       f"не видит — различить нечем." + tail)
        return out
    if d.get("coverage", 0.0) < MIN_COVERAGE:
        out["note"] = (f"лица нашлись лишь на {judged} из {len(probes)} "
                       f"картинок (покрытие {d['coverage']} ниже "
                       f"{MIN_COVERAGE}): вердикт по такому меньшинству — "
                       f"не вердикт." + tail)
        return out

    out["outcome"] = PASS
    out["note"] = (f"лицо актёра не проступило: ближайшая картинка на "
                   f"{d['min']}, медиана {d['median']} при баре "
                   f"{fork_identity.SAME_PERSON_MAX}; судимо {judged} из "
                   f"{len(probes)}." + tail)
    return out


#: Критерий приёмки ОБЪЯВЛЯЕТСЯ У КАЖДОГО ТЕМПЛЕЙТА, а не берётся один на всех.
#: Причина не организационная: для стилизованного домена ArcFace перестаёт быть
#: верным судьёй — фотографического сходства там не ожидается, и НИЗКОЕ
#: расстояние до сырого фото означало бы, что стилизация НЕ СРАБОТАЛА. Один
#: критерий на оба домена молча поменял бы знак у половины каталога.
ACCEPTANCE = {
    "photoreal": ("ArcFace против сырой фотографии, планка 0.35; "
                  "d_raw не должен ухудшиться при включённой LoRA"),
    "stylised": ("ArcFace НЕ судья: низкое расстояние здесь означало бы, что "
                 "стилизация не сработала. Приёмка человеком, критерий "
                 "объявляется в паспорте темплейта до сборки"),
}


def acceptance_for(domain: str) -> str:
    """Критерий приёмки домена. Неизвестный домен — отказ, а не умолчание.

    Умолчание здесь означало бы «судим аниме ArcFace'ом», то есть ровно ту
    ошибку, ради предотвращения которой таблица и заведена.
    """
    if domain not in ACCEPTANCE:
        raise ValueError(
            f"неизвестный домен отрисовки: {domain!r}. Известны: "
            f"{', '.join(ACCEPTANCE)}. Критерий приёмки объявляется до сборки "
            f"темплейта, а не выясняется на демо.")
    return ACCEPTANCE[domain]

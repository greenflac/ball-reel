# Хэндоф: ветка `fork/template-mvp`. Спринт 1 плюс LoRA темплейта

Append-only (Ц6). Задания — `docs/FORK_SPRINT1.md` и `docs/FORK_LORA_BRIEF.md`.
Результаты и команды — `docs/FORK_LORA.md`, оно первично; здесь только то, чего
там нет: состояние ветки, долги и найденные дефекты процесса.

---

## НЕПРОВЕРЕНО — наверх (Ц4)

Полный список — в `docs/FORK_LORA.md` §0. Коротко: **обучение не запускалось,
генерации не было, карты нет.** Всё, что написано про поведение LoRA, — это
приборы для её проверки, а не её проверка.

---

## ~~ДОЛГ, КОТОРЫЙ НЕ ЗАКРЫТ~~ — ЗАКРЫТ РАЗРЕШЕНИЕМ ВЛАДЕЛЬЦА

```
# ~~DEBT(2026-08-17): test_reachable.py краснеет на fork_run.~~
# ЗАКРЫТО: владелец разрешил одну строку, fork_run внесён в STANDALONE.
# Гейт после правки: PASS тесты, PASS покрытие ядра.
```

Разбор ниже оставлен намеренно — он объясняет, почему одна строка в чужом
файле оказалась единственным честным выходом, и почему циклический импорт
ради зелени им не был.

**Суть.** `test_reachable` требует, чтобы у каждого модуля в `ball_reel/*.py`
был импортирующий его модуль, либо строка в списке `STANDALONE` с объяснением,
почему модуль работает сам. Список закрытый — и это правильно, добавление туда
должно стоить осознанной строки.

**Почему это неустранимо изнутри форка.** У любого дерева импортов есть корень.
Сейчас корень — `fork_run`, он зовёт остальные семь модулей форка. Какой модуль
ни поставь корнем, у него по определению не будет импортирующего. Значит корень
обязан быть в `STANDALONE` — то есть в **существующем файле**, а брифинг прямо
запрещает править существующие файлы.

Обойти это можно было бы циклическим импортом (`fork_seam` тянет `fork_run`
внутри функции) — тогда корня формально не станет. **Это не сделано
намеренно:** докстринг самого сторожа говорит, что список не должен быть
«способом заглушить тест», а циклический импорт ради зелени — тот же способ,
только грязнее.

**Цена сейчас.** `python3 -m ball_reel.codeaudit --quick` останавливается на
красных тестах, то есть гейт не проходит целиком. Все остальные тесты зелёные,
включая все восемь модулей форка.

**Как закрывается — одна строка, решение владельца:**

```python
# ball_reel/tests/test_reachable.py, в STANDALONE
"fork_run": "точка входа форка, запускается человеком",
```

Ровно та же формулировка, что уже стоит там у `run_local`, `produce`, `gen`,
`preflight_gpu` — семи точек входа существующего пайплайна. То есть это не
исключение из правила, а применение правила к восьмой точке входа.

---

## Дефекты, найденные и закрытые в этой смене

**1. `git add -A` подмёл чужой временный файл.** `test_codeaudit` создаёт
`codeaudit_clone_probe.tmp` **неотслеживаемым** и проверяет, что клон из индекса
его не подхватывает. Закоммитив его, я сделал файл отслеживаемым и уронил ровно
этот сторож. Закрыто `git rm --cached`; дальше в этой ветке только явные пути.

**2. Произведённый граф Comfy не был подключён.** Первая редакция `fork_comfy`
вырезала кастомные ноды и клала три загрузчика РЯДОМ, не соединяя их. Аудит
говорил «ЧИСТО» — кастомных нод нет, оборванных связей нет, — а четыре входа
`WanAnimateToVideo` остались без питания: вырезание сняло связи 704, 705, 706,
707. Закрыто подключением и отдельной проверкой `unfed_inputs`, которая умеет
краснеть.

**3. Локальный канал оси протечки судился без порога.** Замер планки показал,
что локальный шум кодека в шестнадцать раз выше среднего (0.071895 против
0.004513 при crf 18). Судить максимум средней планкой значило бы объявлять
протечкой каждый блок сжатия; не судить — терять канал, ради которого он
заведён. Закрыто второй планкой.

**4. Мутационный тест краснел не по делу.** Проверка ширины кольца шовной оси
стояла на фикстуре с идеально гладкой серединой, где отношение равно нулю при
любой ширине. Подмена константы ничего не показывала. Закрыто фикстурой, где
фактура меняется с глубиной.

**5. Первая редакция `axis` считала дорогое до дешёвого.** Негодный референс
отвергался уже после полного прохода по набору. Закрыто проверкой обоих якорей
до первого обращения к прибору (П2).

---

## Расхождение с `docs/NUMBERS.md`: было, названо, закрыто

Первый прогон дал красное и меньший размер набора. Причина оказалась не в коде:
контейнер не нёс даже `numpy` и `pillow`, и недостающие импорты уводили с
дискаверинга целые файлы. После восстановления окружения размер совпал с
`docs/NUMBERS.md` в точности. Подробности и таблица установленного — в
`HANDOFF_claude-ball-reel-fork-sprint-1-57nhtg.md`.

`download-r2.pytorch.org` закрыт политикой прокси (403) — назван, не обойдён,
torch взят с PyPI. `api.github.com/repos` и `github.com/<repo>` тоже 403 —
названы, читалось с открытого `raw.githubusercontent.com`.

---

## Второй долг: `docs/NUMBERS.md` разошёлся с веткой

```
# DEBT(2026-08-17): docs/NUMBERS.md называет размер набора до форка.
```

Ветка добавила 236 тестов (восемь модулей форка со своими наборами), и число в
`docs/NUMBERS.md` теперь описывает состояние ДО спринта. По правилу проекта
документ, разошедшийся с кодом, — дефект, а не косметика.

**Почему не исправлено здесь:** `docs/NUMBERS.md` — существующий файл, а
брифинг запрещает править существующие. Ни один тест на этом не краснеет:
`test_docs_numbers` запрещает называть размер набора ВНЕ этого файла и сверяет
число мутаций, но актуальность самого числа проверить статически нельзя — его
можно только прогнать. Поэтому расхождение тихое, и записано оно здесь.

Свежее число — прогоном, той же командой, что стоит в самом `NUMBERS.md`:

```
python3 -m unittest discover -s ball_reel/tests -t . -p 'test_*.py'
```

Вывод на 2026-08-17 в этой ветке: **1 отказ** (`test_reachable`, долг выше),
13 пропусков. Число тестов намеренно не повторяю здесь — его место в
`docs/NUMBERS.md`, и вписать его туда должен тот, кому можно править файл.

---

## Что лежит в ветке

| модуль | что делает |
|---|---|
| `fork_identity` | `d_raw`, `d_ref`, `d_neg`, `d_drv`, регрессия по LoRA; медоид запрещён кодом |
| `fork_leak` | протечка вне маски; ~~планки из шума кодека~~ → **VAE-круг 0.007308 и полный круг**, кодековая планка снята как ЗАНИЖЕННАЯ |
| `fork_mask` | последовательность масок; предел ~~«32 у модели»~~ → 16 у модели, 32 наш; три плеча |
| `fork_channels` | два канала условий; 68 точек лица найдены в выходе модели |
| `fork_comfy` | граф произведён из официального, 8 кастомных нод вырезаны |
| `fork_lora_dataset` | набор под ОДНУ фигуру; лица исключены и проверены прибором; сборка из драйвинга разрешена с контролем |
| `fork_seam` | шовная ось с нуля, порог только внутрь измеренного разрыва |
| `fork_build_route` | ~~корзины~~ → «LoRA нужна / не нужна / не уверен», полоса несимметрична 10:1 |
| `fork_run` | сквозной путь на моке, точка входа |

---

## Чем это отменяется

Тем же, чем и вся гипотеза: **референсный шаг пробил 0.4185 → LoRA не нужна.**
Не пробил → LoRA его не спасает. Ни один прибор этой ветки не отвечает на тот
вопрос: на него отвечает карта.

---

## Дописано в конце смены: приведение к `docs/HANDOFF.md`

Финальный хэндоф пришёл после того, как восемь модулей уже стояли, и **отменил
одно моё решение и подтвердил остальные**. Что изменилось:

| было | стало | почему |
|---|---|---|
| набор по пояс (`waist_up`) | **во весь рост** | §2, решение владельца. Я поставил `waist_up` по промежуточной редакции брифинга |
| лицензии как блокеры релиза | **строка в учёт к отгрузке** | §10: на разработке не блокирует |
| корзины «A» и «B» | «как в драйвинге» / «полная женщина 40+» | §2, §6 F |
| — | `before_after_restore`: личность до и после доводки, одним баром | §6 A, новое требование |
| — | `check_geometry`: кратность 16, шаг длины 4 от 1 | §3.2 |
| — | `fork_preflight` (поток H) | §6 H |

**Найденное расхождение ВНУТРИ хэндофа.** §3.2 говорит «длина кратна 4», §3 и
§3.1 задают 77 кадров, 77 на 4 не делится. Первоисточник
(`WanAnimateToVideo.doc.md:20`) разрешает спор: «default: 77, **step: 4**» при
минимуме 1 — шаг, а не кратность. Моя первая проверка читала прозу буквально и
**забраковала бы штатную геометрию стека**; поймано тем, что число из §3 не
прошло проверку, написанную по §3.2. Верх за файлом, тест на 76 сторожит.

**Дефект, пойманный собственным тестом предполёта.** Первая редакция бюджета
брала `Q4_K_M` — 15.97 из 16.0 арифметически влезает. §3.1 называет это
«впритык» и берёт `Q3_K_M`, и второе основание непреодолимо: ECC включён по
умолчанию и забирает неизмеренную часть 16 ГБ, так что остаток 0.03 — совпадение,
а не запас. Заведено третье состояние ступени и порог запаса.

### Состояние на конец смены

```
python3 -m unittest discover -s ball_reel/tests -t . -p 'test_*.py'
```
1 отказ (`test_reachable`, долг выше), 13 пропусков. Все девять модулей форка и
их наборы зелёные:
```
python3 -m unittest discover -s ball_reel/tests -t . -p 'test_fork_*.py'   # OK
```

### Что осталось невыполненным из §6, и это надо знать следующей смене

* **`fork_comfy`: адаптер `render(workflow, inputs) → frames` на моке и
  лок-файл** (§6 E) — не написаны. Граф производится, вырезается и проверяется
  структурно; исполнять его нечем.
* **`fork_mask`: три плеча как готовые конфигурации** (§6 C) — есть параметр
  `grow_px` и проверка предела 32 px, но именованных «узкая/широкая/насколько
  широкая» конфигураций нет.
* **`fork_preflight`: живость Comfy по HTTP и наличие файлов по хэшу** (§6 H) —
  не написаны: проверять нечего, Comfy не установлен и весов нет.
* **Замер одного трансформерного блока** — требует карты.

---

# Спринт девяти потоков. Что нашли, и почему это дороже того, что построили

Дописано ведущим. Девять потоков писали параллельно, каждому было запрещено
открывать чужой файл — поэтому свои находки они сдали в отчётах с пометкой
«за ведущим», и здесь они собраны. Порядок: сначала опровергнутое, потом
построенное.

## ШЕСТЬ УТВЕРЖДЕНИЙ, КОТОРЫЕ БЫЛИ ФАКТОМ И ПЕРЕСТАЛИ ИМ БЫТЬ

Все шесть найдены **до аренды карты**. Ради этого спринт и затевался.

### 1. «Вне маски пиксели копируются» — опровергнуто чтением, подтверждено замером

Снято в §1a хэндофа по находке разработки. У ноды `WanAnimateToVideo` нет
`noise_mask` и нет композитинга: драйвинг пишется в серый холст, холст идёт в
`vae.encode`, возвращается `torch.zeros`. **Весь кадр сэмплируется и
декодируется.**

Дальше поток B сделал то, чего не делал никто: **поднял VAE без Comfy**
(`diffusers` + `AutoencoderKLWan`) и замерил неустранимый пол представления.

| величина | значение |
|---|---|
| VAE-круг, среднее | **0.007308** |
| снятая кодековая планка | 0.004513 |
| локальный пол | 0.338698 — в **46 раз** выше среднего |

**Кодековая планка лежала НИЖЕ пола представления** — то есть была меньше того,
что тракт тратит до единого шага сэмплера. Довод §1a перестал быть рассуждением.

Отрицательный результат (И6): на целевой 480×848 прогон убит по памяти
(5.6 ГБ RSS), замер снят на половинной геометрии и **занижен**. Пересчёт на карте.

### 2. «32 px — предел модели» — нет, 16; 32 это наш собственный блокификатор

Поток C проверил по исходнику: маска ужимается до латента (**/8**), патч
`[1,2,2]` даёт токен **16 px**. Число 32 — виджет вендорского `BlockifyMask`,
который мы вырезали и пишем сами. То есть **инструмент управления силуэтом
вдвое грубее, чем позволяет модель**, ровно там, где решается гипотеза
телосложения.

Там же уточнена временная решётка: четыре кадра **не усредняются**, они едут
четырьмя каналами (`in_dim 36`). Теряется не значение маски, а раздельность
латента — честная формулировка «покадрового управления нет, шаг 4 кадра».

### 3. Граф грузит НЕ ТО, что записано в стеке

Поток E прогоном: произведённый граф грузит
`Wan2_2-Animate-14B_fp8_e4m3fn_scaled_KJ` (`UNETLoader`) и
`umt5_xxl_fp8_e4m3fn_scaled` (`CLIPLoader`) — **не Q3_K_M и не GGUF-энкодер**
из §3. Оба узла из `comfy-core`, под вырезание не попадают.

**Бюджет VRAM §3.1 (13.30 из 16) к нынешнему графу не относится.** На карте это
всплыло бы как OOM и было бы объяснено чем угодно — ECC, активациями, длиной.
Не чинится переименованием файла: `UnetLoaderGGUF` — **четвёртый** кастомный
набор со своей лицензионной цепочкой. Решение владельца, записано в лок-файл,
тест сторожит расхождение записи с кодом.

### 4. `FACE_SIDE = 512` была объявлена и нигде не использовалась

Поток D. И это не мелочь: `face_video` приводится к 512 **самой нодой**, и
приводится `crop="center"`. Кадр 480×848 обрезается до центральных **480×480**,
а при кадрировке во весь рост голова лежит **выше** этой полосы. Лицевое
условие уехало бы из картинки **целиком и молча**, мимики бы не было, и
виноватым назначили бы что угодно другое.

### 5. Приёмка личности воспроизводится на одну строку из трёх

Поток A, машиночитаемо в `ACCEPTANCE_ROWS`:

| строка §6 A | требуется | получено |
|---|---|---|
| против медоида | 0.2579, 19/21 | **0.2579, 19/21** — точно |
| негативный контроль | 0.96–1.05 | **0.6809** — направление верное, величина не та |
| против сырой фотографии | 0.5067, 0/21 | **НЕ СМОГЛИ** — фотографии нет в дереве |

И разведено то, что легко сложить в «почти готово»: воспроизводимая строка —
**медоидная**, то есть сравнение порождённого с порождённым. Она доказывает, что
прибор тот же, и не говорит о продукте ничего.

### 6. Тринадцать сторожей зеленели ровно на провале

Поток A: `assertIn("годно", text)` — **«годно» есть подстрока «не годно»**.
Проверка на успех проходила и тогда, когда вердикт был отрицательный.
Подтверждено мутацией: подмена `PASS` на `FAIL` в `control_verdict` теперь
роняет тест, а раньше не роняла.

---

## ПОБОЧНЫЕ НАХОДКИ, КОТОРЫЕ СТОИТ ЗНАТЬ

* **Ложь в самом первоисточнике вендора.** `WanAnimateToVideo.doc.md:27`
  обещает «composite with generated content», которого в ноде нет. Файл только
  на чтение — опровержение живёт константой и **едет внутрь произведённого
  графа**, потому что граф уедет на машину без этого репозитория.
* **Лестница блокификации соизмерима с самим плечом.** Поток C, живьём:
  блокификация даёт +40 блоков даром, плечо `wide` (32 px) — +50. Значит плечо
  `narrow` это не «силуэт как есть», и разрыв между плечами меньше, чем
  читается по `grow_px`.
* **Шум шовной оси зависит от положения маски вшестеро** (0.0723 против
  0.4590): кольцо, легшее на человека внутри и на гладкую стену снаружи, честно
  меряет разницу сюжетов, а не шва.
* **Ось выдала 27.18 на кадре без шва** — внешнее кольцо на выбитой в белое
  шторе, `d_out = 0.000022`. Найдено глазами (П3). Заведён пол знаменателя,
  максимум по облаку упал с 26.18 до 0.9827.
* **`fork_run` звал удалённую функцию, и сьют молчал.** Все три теста падали на
  отсутствующем фото и до той строки не доходили. Заведена проверка, разбирающая
  дерево сводящего прохода: каждое вызванное имя обязано существовать.
* **Два потока разошлись именем лок-файла.** Хэндоф обещал `fork_*.lock`, поток E
  положил `fork_stack.lock.json`. Зашитое имя дало бы «реестра нет» вместо «хэш
  не сошёлся» — тихий ответ там, где проверить было чем.
* **Веса замерены, а не приняты:** шесть файлов, размеры и sha256 двумя
  независимыми запросами HF. Все числа §3 совпали — **но совпадение установлено
  замером**. Сумма 15.34 GiB, «40 ГБ достаточно» перестало быть оценкой.

---

## ЧТО ПОСТРОЕНО

Девять модулей, у каждого свой красный тест и мутации констант в обе стороны.

| модуль | чем отличается от «просто написан» |
|---|---|
| `fork_identity` | медоид запрещён кодом тремя проверками; один бар на «до/после» невозможно задать разным тремя способами; негативный контроль для **доводки лица** — пробел §7, которого не заметил никто |
| `fork_leak` | планка несёт **клеймо происхождения**, и судить вправе только полный круг при нулевой маске; голый словарь с числом вердикта не выносит |
| `fork_mask` | предел 16/32 разведён; три именованных плеча; временная решётка описана честно |
| `fork_channels` | лицевой канал реально 512; квантование маски проверено чтением, а не предположено |
| `fork_comfy` | **мок физически не может вернуть PASS**, кадры помечены в пикселях, и обратная проверка ловит бэкенд, вернувший помеченные кадры |
| `fork_build_route` | полоса **несимметрична** 10:1 — числовая запись того, что зря включить LoRA дороже, чем зря не включить |
| `fork_lora_dataset` | лица не «исключены», а исключены **и проверены прибором**; сборка из драйвинга требует контроля, что детектор вообще шевелится |
| `fork_seam` | порог мимо калибровки **физически невозможен** — планка это класс с приватным ключом |
| `fork_preflight` | паспортные TFLOPS в код не попали; `SPEC_TFLOPS` намеренно `None`, и тест подставляет туда 18.0, требуя, чтобы ответ не изменился |

---

## ЧТО ЖДЁТ КАРТЫ, ПО ПОРЯДКУ ПЕРВОГО ЧАСА

1. **Референсный шаг против планки 0.35** — главный незакрытый вопрос §4. Не
   пробил — продуктовая логика пересматривается целиком.
2. **Пол (б):** полный круг при `character_mask = 0`, ≥5 кадров, целевые
   480×848×77. Без него ось протечки не судит, а отвечает «не смогли».
3. **Пересчёт VAE-круга на целевой геометрии** — нынешние 0.007308 занижены.
4. **`sampler_contribution`**: если полный пол вдвое выше VAE-пола, реконструкция
   вне маски нестабильна сама по себе, и `LEAK_OVER_FLOOR = 2.0` надо
   калибровать разбросом пола, а не оставлять ВЫБРАННЫМ.
5. **Облако «шовных»** — вторая половина калибровки шовной оси.
6. **Приёмка LoRA до рендера:** картинки одной LoRA без референса, расстояние до
   лица актёра.
7. **Замер одного трансформерного блока** — настоящие TFLOP/s вместо паспорта.
8. **Опыт трёх плеч** — последним, он дороже всего и читается только после 1–7.

## Решения владельца, принятые по ходу спринта

* **LoRA нужна** — снято с обсуждения, первое плечо опыта перестало быть
  кандидатом в победители и стало базовой линией.
* **LoRA не взаимодействует с фоторефересом, только с драйвингом.** Записано как
  проектное намерение; механизмом это не гарантировано (LoRA правит веса), и
  проверяют его ровно три уже построенных прибора: `d_drv`, `lora_regression`,
  `actor_memorisation_probe`.

## Закрытие спринта: полный аудит

Приведено то, что напечатала команда, а не пересказ.

```
$ python3 -m ball_reel.codeaudit
PASS  тесты             Ran 1906 tests in 139.395s
PASS  покрытие ядра     порог 50%
PASS  копия для мутаций в копии пропущено 13, в дереве 13 — не слабее
PASS  клон из индекса    в клоне пропущено 13, в дереве 13 — не слабее
мутационное покрытие: 100% (119/119)

АУДИТ ПРОЙДЕН
```

```
$ python3 -m unittest discover -s ball_reel/tests -t . -p 'test_fork_*.py'
Ran 531 tests in 175.796s
OK
```

**Паритет копии и клона был красным** в середине спринта: `в копии пропущено 15
против 13` — два сторожа молчали во время КАЖДОЙ мутации, потому что фикстура
негативного контроля лежала в `veoprobe/` и не попадала ни в копию, ни в клон.
Фикстура переехала в `ball_reel/fixtures/foreign_face.png`, числа пересняты
(0.6809). Теперь 13/13 в обеих проверках — то есть мутации гоняются на наборе,
не слабее дерева, и вердикт «убита» больше не может быть получен молчанием.

**Чего эти 1906 тестов НЕ проверяют** — и это главное, что нужно унести из
закрытия спринта: генерации не было. Ни один кадр не произведён Wan-Animate,
все восемь осей приёмки на моке отвечают «не смогли проверить» там, где нужен
живой выход. `АУДИТ ПРОЙДЕН` означает «приборы и сторожа исправны», а не
«продукт работает». Порядок первого часа на карте — в разделе выше.

Два решения владельца всё ещё ждут ответа и блокируют аренду:

1. **fp8 против Q3_K_M GGUF** в произведённом графе. Граф грузит fp8, а бюджет
   VRAM 13.30/16 считался по Q3_K_M. Переход на GGUF означает `UnetLoaderGGUF`
   и ЧЕТВЁРТЫЙ сторонний пак нод.
2. **Диск.** Предполёт отвечает «не годно»: 19.2 ГБ свободно против 40 ГБ,
   которые нужны под лок-файл (15.338 ГиБ весов) плюс выходы.

## Развилка §1: fp8 против GGUF — замеренные числа

Размеры сняты тем же способом, что и весь лок-файл (`paths-info/main`,
`lfs.size`), дата 2026-08-17.

| ступень | файл | ГиБ | загрузчик |
|---|---|---|---|
| bf16 | `Comfy-Org/…/wan2.2_animate_14B_bf16.safetensors` | 32.177 | штатный |
| fp8 (в шаблоне) | `Kijai/WanVideo_comfy_fp8_scaled/…_fp8_e4m3fn_scaled_KJ.safetensors` | 17.138 | штатный |
| Q4_K_M | `QuantStack/Wan2.2-Animate-14B-GGUF` | 10.707 | `UnetLoaderGGUF` |
| Q3_K_M (в локе) | `QuantStack/Wan2.2-Animate-14B-GGUF` | 8.038 | `UnetLoaderGGUF` |

Текстовый энкодер: fp8-путь берёт `umt5_xxl_fp8_e4m3fn_scaled.safetensors`
**6.273 ГиБ**, GGUF-путь — `umt5-xxl-encoder-Q5_K_M.gguf` **3.861 ГиБ**.

Суммарная закачка: **fp8-путь 26.849 ГиБ**, **GGUF-путь 15.338 ГиБ**.

Пак `city96/ComfyUI-GGUF` существует и проверен командой (Ц10):
`curl https://raw.githubusercontent.com/city96/ComfyUI-GGUF/main/pyproject.toml`
→ `name = "comfyui-gguf"`, `version = "2.0.0"`, зависимости `gguf>=0.13.0`,
`sentencepiece`, `protobuf`. Лицензия файлом `LICENSE` — НЕПРОВЕРЕНО, текст не
читан.

**Чего эти числа НЕ говорят.** Размер файла — не потребление VRAM. ComfyUI
умеет держать часть блоков в системной памяти, поэтому 17.138 ГиБ на карте в
16 ГБ означает не «не запустится», а «пойдёт с переносом блоков», и цена этого
во времени НЕ ЗАМЕРЕНА — замерить её можно только на карте
(`fork_preflight.throughput`).

## Решения владельца, продолжение

* **Диск: будет 50 ГБ.** Предполёт требует 40 и на этой машине отвечает
  «не годно» (19.2 свободно) — это про машину разработки, не про арендуемую.
  50 покрывает оба пути; GGUF оставляет 34.7 ГБ под выходы, fp8 — 23.2 ГБ.
* **AnimateDiff из стека снят.** В `fork_*` его и не было: `guoyww/animatediff-
  motion-adapter-v1-5-3` живёт только в `ball_reel/animate.py`, то есть в
  старом конвейере, и остаётся там как запасное плечо, а не как часть форка.

## Развилка LoRA: замер вместо спора

Вопрос стоял так: тренеры Wan-Animate не поддерживают, а вендор пишет
«If you're using Wan-Animate, we do not recommend using LoRA models trained on
`Wan2.2`, since weight changes during training may lead to unexpected
behavior». Отсюда опасение, что LoRA к Animate вообще не прицепится.

**Опасение снято замером.** Прибор — `tools/fork_probe_lora_fit.py`, читает
заголовки safetensors Range-запросами, весов не качает (сотни килобайт против
34 ГБ).

```
$ python3 tools/fork_probe_lora_fit.py
донор: Wan2.2-I2V-A14B (high_noise)  цель: Wan2.2-Animate-14B
двумерных весов у донора 406: сядет 406, форма не сойдётся у 0,
имени нет в цели у 0. У цели останутся нетронутыми 113 весов,
которых у донора нет вовсе
```

Полное сравнение множеств:

```
Animate тензоров          1441
I2V-A14B (high) тензоров  1095
общих имён                1095  = ВСЕ имена I2V есть в Animate
расхождений по форме      0
расхождений по типу       1095  (BF16 против F32 — на прицепление не влияет)
имена low_noise == high_noise: True, и тоже все есть в Animate
```

**Animate — надмножество I2V-A14B**, а не другая модель. Сверх него он несёт 346
тензоров: `cross_attn.k_img/v_img/norm_k_img` (200 на 40 блоков), `face_adapter`,
`face_encoder`, `motion_encoder`, `img_emb`, `pose_patch_embedding` — то есть
ровно тот аппарат, которым он и анимирует персонажа.

**Что это значит и чего НЕ значит.** Модуль адаптации ключей писать не нужно:
переименовывать нечего, LoRA садится по именам как есть. Но совпадение имён —
это «прицепится», а не «сработает». Веса за одинаковыми именами разные, и
предостережение вендора именно про это. Одно из двух плеч оно теперь НЕ
объясняет: если LoRA даст ноль эффекта, дело не в том, что она не прицепилась.

Косвенное подтверждение лежит в самом стеке: `lightx2v_I2V_14B_480p_cfg_step_distill`
обучена под I2V-14B и стоит в ОФИЦИАЛЬНОМ шаблоне ComfyUI для Animate.

**НЕПРОВЕРЕНО:** из какого эксперта (high или low) выведены веса Animate. Имена и
формы у обоих одинаковы, поэтому прибор их не различает, а содержимое требует
скачивания 34 ГБ. На выбор донора для обучения это влияет, и решается это
замером на карте, а не чтением.

**Отдельная дыра, найденная попутно.** ComfyUI при несовпадении ключей НЕ падает:
`comfy/sd.py` печатает `logging.warning("NOT LOADED {}".format(x))` и продолжает.
Ноль совпавших ключей = чистый прогон без ошибки и без эффекта. Между «загрузили
файл» и «оценили эффект» (`lora.accept`) нет шага «проверили, что приложилось»;
в старом конвейере такой шаг есть — `animate.active_loras`, — а на пути форка
через ComfyUI его нет.

## Положение о DoRA

Принято по итогам замеров этой сессии. Отменяется только новым замером.

**1. Умолчание — LoRA, не DoRA.** Ранг 16–32, как записано в `lora.MAX_RANK`.
DoRA — план Б, а не улучшение по умолчанию.

**2. Основание — цена, а не сомнение в методе.** Замерено на заглушке из
четырёх проекций 5120x5120 (форма блока 14B), `peft 0.20.0`, CPU, fp32:

```
$ python3 dora_probe.py
метод       ранг    обучаемых  доля базы    с/шаг
LoRA          16      655,360     0.625%    0.073
DoRA          16      675,840     0.645%    1.046
LoRA          64    2,621,440     2.500%    0.079
DoRA          64    2,641,920     2.520%    1.029
```

По памяти DoRA почти бесплатна: прибавка ровно 20 480 параметров НА ЛЮБОМ
ранге — 4 модуля по 5120, одно число на выходной канал. По времени дорого.

**3. Цена растёт с шириной базы, не с рангом** — негативный контроль:

```
база 640 -> 2.8x,  1280 -> 10.1x,  2560 -> 15.1x,  5120 -> 20.1x
при базе 5120: ранг 8 -> 20.7x, ранг 64 -> 13.8x
```

Механизм виден в исходнике (`peft/tuners/lora/dora.py`, тело sha256
`2ff9d222eeada2ec…`): норма считается по ВСЕЙ базовой матрице каждый шаг.
Значит выбором ранга эту цену не снять. **НЕПРОВЕРЕНО: всё это CPU/fp32, на
GPU соотношение будет другим и не замерено.**

**4. Условие включения — наблюдаемое, а не «попробуем».** DoRA берётся тогда и
только тогда, когда LoRA на ранге 16–32 не пробила `lora.MIN_EFFECT` ЛИБО дала
разброс шире `lora.RESOLUTION_SIGMAS`. Судья тот же — `lora.accept`, ему
безразлично, что внутри адаптера. Заявленная сильная сторона DoRA — низкие
ранги; наш случай (телосложение плюс домен) на низком ранге не сидит, поэтому
на умолчании ожидаемого выигрыша нет.

**5. Формат файла проверяется ДО обучения, а не после.** PEFT сохраняет
магнитуду как `lora_magnitude_vector`, ComfyUI ищет `dora_scale`
(`comfy/lora.py`). Расхождение молчаливое: адаптер загрузится без ошибки, без
магнитуды, и DoRA станет обычной LoRA — то есть тем, ради отказа от чего её и
брали. Ловится `fork_lora_attach.dora_readiness`, вердикт «не годно».

**6. Что DoRA НЕ решает.** Ни один из трёх тренеров не поддерживает
Wan-Animate, и переход на DoRA этого не меняет. Развилка «против чего обучать»
стоит выше по течению и решается раньше.

## Исследование LoRA через продуктовую логику

**Продуктовое обещание не изменилось.** Фото клиента плюс готовый драйвинг даёт
то же видео с клиентом. LoRA привязана к ТЕМПЛЕЙТУ и отвечает за телосложение и
домен отрисовки. Всё вне маски наследуется из драйвинга.

**Что подтвердилось замером.** Animate — надмножество I2V-A14B: все 1095 имён
совпадают, расхождений по форме 0, и настоящий вендорский адаптер садится
731 модулем из 731. Значит плечо с LoRA осуществимо механически. Риск «обучать
не на чем» снят.

**Что осталось риском.** Тренера под Animate нет ни у одного из трёх, и вендор
предостерегает против LoRA, обученных на Wan2.2. То есть LoRA — ставка на
КАЧЕСТВО, а не на выполнимость, и проигрыш этой ставки деградирует одну ось
(телосложение), а не отменяет продукт: базовая линия без адаптера остаётся
рабочим состоянием тракта. Решение владельца «LoRA нужна» этим не пересмотрено.

**Находка, усиливающая продуктовую логику.** 113 двумерных весов Animate донор
не содержит вовсе: `face_adapter`, `face_encoder`, `motion_encoder`,
`cross_attn.k_img/v_img`, `pose_patch_embedding` — то есть аппарат переноса лица
и движения. Адаптер, обученный на I2V-A14B, ИХ ВЕСА НЕ ПРАВИТ НИКОГДА. Это
частичное механическое подтверждение решения владельца «LoRA не взаимодействует
с фотореференсом, только с драйвингом»: раньше это было проектным намерением,
теперь у него есть измеренная опора.

Опора именно ЧАСТИЧНАЯ, и путать нельзя: общие блоки LoRA правит, а их выход
дальше течёт в тот самый аппарат лица. «Не правит веса» не равно «не влияет».
Проверяют это по-прежнему три прибора: `d_drv`, `lora_regression`,
`actor_memorisation_probe`.

**Практическое следствие для первого часа на карте.** Порядок опасности обратный
интуитивному: падение при загрузке видно сразу и стоит минуту, а тихий ноль
стоит часов ложной диагностики. Поэтому шаг «адаптер» стоит ПЕРЕД дорогими
осями, и вопрос «приложилось ли» закрывается до вопроса «стало ли лучше».

## Разведка по чужим реализациям: три находки, меняющие план

Все три перепроверены мной по первоисточникам, а не приняты от агентов на слово.

### 1. ОТМЕНЯЕТСЯ прежнее «тренера под Animate нет»

Утверждение было верно для трёх проверенных тренеров и НЕВЕРНО вообще.
`modelscope/DiffSynth-Studio` — официальный тренер, знающий именно Animate:

```
$ curl .../DiffSynth-Studio/main/examples/wanvideo/model_training/lora/Wan2.2-Animate-14B.sh
--model_id_with_origin_paths "Wan-AI/Wan2.2-Animate-14B:diffusion_pytorch_model*.safetensors,…"
--data_file_keys "video,animate_pose_video,animate_face_video"
--extra_inputs "input_image,animate_pose_video,animate_face_video"
--lora_base_model "dit" --lora_target_modules "q,k,v,o,ffn.0,ffn.2" --lora_rank 32
--learning_rate 1e-4 --num_epochs 5 --height 480 --width 832 --num_frames 81
# 1*80G GPU cannot train Wan2.2-Animate-14B LoRA
# We tested on 8*80G GPUs
```

Следствия: развилка «против какой базы обучать» СНЯТА — учим против самой Animate;
вопрос про двух экспертов снят вместе с ней (boundary не задаём, эксперт один);
`tools/fork_probe_lora_fit.py` остаётся полезен, но уже не как основание выбора
донора, а как сторож совместимости.

Вендорский эталонный рецепт, проверен в исходнике
(`wan/modules/animate/animate_utils.py`):

```python
def get_loraconfig(transformer, rank=128, alpha=128, init_lora_weights="gaussian"):
    if "blocks" in name and "face" not in name and "modulation" not in name
```

r=128 против демонстрационных 32 у DiffSynth, и **face-ветка исключена явно**.
Последнее — независимое подтверждение решения владельца: вендор своей LoRA
аппарат лица не трогает.

### 2. КАНАЛ ЛИЦА: возможно, мы кормим модель не тем

`wan/modules/animate/preprocess/process_pipepline.py:72-80`:

```python
face_bbox_for_image = get_face_bboxes(meta['keypoints_face'][:, :2], scale=1.3, …)
x1, x2, y1, y2 = face_bbox_for_image
face_image = frames[idx][y1:y2, x1:x2]      # пиксели ИСХОДНОГО кадра
face_image = cv2.resize(face_image, (512, 512))
```

Вендор подаёт в `face_video` НАСТОЯЩИЙ RGB-кроп лица; точки нужны только для
рамки. Потребитель — свёрточный энкодер движения, портированный из LIA.

Мы подаём нарисованные точки DWPose (§ поправка 4 хэндофа). Так сделано в
официальном шаблоне ComfyUI, из которого мы производили граф, — то есть в
экосистеме ДВА несовместимых канала лица, и с обучением совпадает не тот,
что в шаблоне.

**НЕПРОВЕРЕНО и это главное:** что нарисованные точки дают мёртвый сигнал, я не
измерял. Проверяется первым же прогоном на карте (И2): прогнать энкодер
движения на точках и на настоящем кропе, сравнить разброс выходного кода. Почти
константа на точках = число вместо гипотезы. До замера канал не менять.

### 3. МАСКА: вендорское умолчание грубее нашего, а режим замены свёрнут

`preprocess_data.py`, argparse:

```
--iterations default=3   --k default=7
--w_len default=1  "A value of 1 means no subdivision is performed"
--h_len default=1
```

`get_aug_mask` при w_len=h_len=1 заливает весь bbox целиком. То есть умолчание
вендора в режиме замены — дилатация ~21 px ПЛЮС схлопывание в ПРЯМОУГОЛЬНИК.
Не силуэт. Реквизит рядом с телом попадает внутрь и перерисовывается всегда.

И у новой ноды `WanAnimate2ToVideo` входов `background_video` и `character_mask`
НЕТ ВОВСЕ (проверено по списку входов в `comfy_extras/nodes_wan.py`):

```
WanAnimateToVideo  : … reference_image, face_video, pose_video,
                     background_video, character_mask, continue_motion, …
WanAnimate2ToVideo : … reference_image, pose_video, positive_pose,
                     pose_strength, pose_start_percent …   ← маски и фона нет
```

Вывод для продукта: сохранность реквизита — НЕ решённая где-то задача, которую
мы зря изобретаем. Её не решил никто: вендор ушёл в прямоугольник, upstream
свернул режим замены, единственный найденный проект, который пробовал плотную
маску, вынес обе опции в интерфейс и не выбрал. Наш путь — плотная маска с
классами и вычитанием предметов — единственный известный, который пытается.
Это довод НЕ отказываться от `fork_props`, но и трезвость: аналогов нет,
сравнивать не с чем, планки придётся ставить своим замером.

## Находка, задевающая основание форка: вендор ушёл с нашего шаблона

Проверено мной, не принято от агента на слово.

Мы производим граф из `video_wan2_2_14B_animate.json` и вырезали из него три
пака — прежде всего из-за `PointsEditor`, который «ждёт человека с мышью».
В том же репозитории `Comfy-Org/workflow_templates` лежит БОЛЕЕ НОВЫЙ шаблон
`template_purz_wan22_animate_auto_character_replace.json`. Разбор обоих:

```
наш исходный (40 нод):
  PointsEditor, DWPreprocessor, Sam2Segmentation, BlockifyMask, GrowMask, DrawMaskOnImage

auto character replace (сабграф «Wan 2.2 Animate - Character Replace», 32 ноды):
  PointsEditor      — 0 вхождений
  DWPreprocessor    — 0 вхождений
  PoseAndFaceDetection, Sam2Segmentation, DrawViTPose,
  BlockifyMask, GrowMaskWithBlur, DrawMaskOnImage
```

Слово «auto» в имени файла — ровно про снятый ручной шаг. Промпт для SAM2 даёт
не мышь, а детектор: `PoseAndFaceDetection.bboxes → Sam2Segmentation`.

**Что это значит для двух наших решений.**

1. **Вырезание паков.** Ручным был ОДИН ВИДЖЕТ, а не пак. Вырезав паки целиком,
   мы вместе с ним выбросили ViTPose-препроцессор — единственный, кто даёт
   face-кроп 512 «как учили», — и SAM2-propagate, дающий временну́ю
   когерентность маски. Решение не было ошибочным на момент принятия: в
   захэшированном нами шаблоне `PointsEditor` действительно есть. Но апстрим
   ушёл вперёд, и наш граф произведён из устаревшего源.
2. **Канал лица.** `DWPreprocessor`, из-за которого у нас нарисованные точки,
   в новом шаблоне отсутствует. То есть расхождение с обучением (см. раздел
   выше) исправлено вендором самостоятельно — тем же способом, что и в его
   питоновском препроцессинге.

**Цена перехода — лицензии, проверено (Ц5):**

```
kijai/ComfyUI-WanAnimatePreprocess   Apache-2.0     ← нужен, чистый
kijai/ComfyUI-segment-anything-2     Apache-2.0     ← нужен, чистый
facebookresearch/sam2 (сама модель)  Apache-2.0     ← чистый
kijai/ComfyUI-KJNodes                GPL-3.0        ← ПРОБЛЕМНЫЙ
Fannovel16/comfyui_controlnet_aux    LICENSE 404    ← не объявлена, и больше НЕ НУЖЕН
```

Из KJNodes новому шаблону нужны ровно три ноды: `BlockifyMask`,
`GrowMaskWithBlur`, `DrawMaskOnImage`. Все три у нас уже есть своими руками —
`fork_mask.blockify`, `fork_mask.grow_selective`, зачернение фона тривиально.
Значит путь усвоения: взять два Apache-пака, GPL-пак НЕ брать, три ноды
подставить своими. `comfyui_controlnet_aux` выбывает совсем — он и без лицензии,
и не нужен, раз позу с лицом даёт `PoseAndFaceDetection`.

**Что от нашей работы остаётся при переходе.** Всё, что про измерение и про
предметы: `fork_props` (у SAM2 один объект, классов нет — вычитать предмет из
маски он не умеет), обе оси приёмки, `fork_lora_attach`, `audit_weights`,
проверка питания входов. Заменяется только добыча условий и маски —
`fork_channels` и часть `fork_mask`.

**НЕ МЕНЯТЬ ДО ЗАМЕРА.** Переход трогает несущие каналы, и делать его по чтению
чужого JSON нельзя. Порядок: сперва замер мёртвости нынешнего канала лица
(раздел выше), затем сравнение двух графов на одном входе, и только потом
решение владельца. Обе версии шаблона лежат рядом и хэшируются одинаково.

## Решения владельца от 2026-08-18

1. **Переходим на новый шаблон** `template_purz_wan22_animate_auto_character_replace`.
   Берём Apache-паки `ComfyUI-WanAnimatePreprocess` и `ComfyUI-segment-anything-2`;
   GPL-пак `KJNodes` НЕ берём — три нужные ноды подставляем своими
   (`fork_mask.blockify`, `fork_mask.grow_selective`, зачернение фона).
   `comfyui_controlnet_aux` выбывает совсем.
2. **Канал лица переводим на настоящий RGB-кроп 512** — как в питоновском
   препроцессинге вендора и в новом шаблоне. Нарисованные точки DWPose снимаются.
   Замер мёртвости старого канала остаётся в плане первого часа — он нужен не
   чтобы решить (решено), а чтобы знать цену прежней ошибки.
3. **Сохранность реквизита пробуем.** `fork_props` остаётся, планки ставим своим
   замером: аналогов в открытом поле нет.
4. Обучение — обсуждается отдельно, см. раздел ниже.

## ТРИ РЫЧАГА ТЕЛОСЛОЖЕНИЯ, а не один

Разбор к вопросу 4. Телосложение на выходе определяется не одним механизмом, и
наш план обучения бил только по третьему.

**Рычаг 1 — МАСКА.** Она снимается с драйвинга, то есть очерчивает силуэт
АКТЁРА. Клиент физически не может выйти за неё. Наши плечи `ARMS`
(0/32/64 px) — это ровно ручка этого рычага, и замер потока C уже есть:
силуэт как есть 65.6% кадра, +32 px → 78.6%, +64 px → 89.6%.

**Рычаг 2 — СКЕЛЕТ ПОЗЫ.** У вендора есть штатный ретаргетинг, пересчитывающий
скелет драйвинга под пропорции референса, — то есть буквально «взять
телосложение с фотографии». В нашем режиме он ВЫКЛЮЧЕН, и вендор объясняет
почему (`UserGuider.md`, 1.2):

> Pose retargeting is DISABLED by default in this mode. This is a deliberate
> choice to account for potential spatial interactions between the character
> and the environment.
> **WARNING**: If there is a significant mismatch in body proportions between
> the reference and driving characters, artifacts or deformations may appear
> in the final output.

Это центральное противоречие нашего продукта, сформулированное вендором в двух
фразах: включишь ретаргетинг — телосложение поедет за клиентом, но рука уедет с
мяча; выключишь — контакт цел, но при сильном расхождении пропорций артефакты.

**Рычаг 3 — ОБУСЛОВЛИВАНИЕ ВНЕШНОСТИ:** референсное фото через CLIP и латент
плюс веса, которые правит LoRA. Только сюда бил наш план обучения.

**Следствие, меняющее порядок работ.** Если связывающим окажется рычаг 1,
LoRA не поможет: человек не станет шире маски, сколько его ни обучай. Проверить
это можно БЕЗ обучения и БЕЗ восьми карт — тремя плечами `ARMS` на одном
клиенте без всякого адаптера. Это уже построено. Замер обязан идти ПЕРЕД
решением о бюджете обучения (П2: дешёвое раньше дорогого).

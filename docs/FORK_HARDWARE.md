# Железо под форк: что замерено, что выведено, что неизвестно

> **ИСТОРИЯ РАЗБОРА, НЕ ЗАДАНИЕ.** Задание — `docs/HANDOFF.md`, он самодостаточен
> и при расхождении главнее. Этот файл оставлен, чтобы можно было узнать, *почему*
> решение такое: числа, замеры и снятые утверждения. По нему не работают.

Этот файл появился как исправление. В плане спринта стояло «рекомендация —
48 ГБ VRAM», и это была не рекомендация, а домысел, выданный за замер: я взял
размер файла весов (17.14 ГБ) и молча предположил, что он целиком лежит в
памяти. Владелец возразил, что у него сервер на 16 ГБ и что требования
завышены. **Возражение верное.** Ниже — то, что можно проверить командой.

**Всё, что ниже, пересчитывается одной командой** — она и есть ответ на
«чем проверить»:

```
python3 docs/measure_hardware.py            # заголовок весов, бюджет, лицензии
python3 docs/measure_hardware.py --offline   # только арифметика, без сети
```

Три состояния помечены прямо: **ЗАМЕРЕНО** — снято командой, команда рядом;
**ВЫВЕДЕНО** — арифметика из замеренных величин, с явными допущениями;
**НЕИЗВЕСТНО** — проверяется только запуском.

---

## 1. Официального минимума VRAM не существует

**ЗАМЕРЕНО.** Туториал вендора (`workflows/upstream/`, скачан 17 августа) не
называет ни одного числа. Единственное упоминание памяти во всём документе:

> «If you are running this workflow for the first time, please use a small size
> for video generation, in case you don't have enough VRAM»

Ни минимума, ни рекомендации, ни таблицы. Всякое число «нужно N ГБ», названное
без прогона, — чужой домысел или мой. Это стоит помнить, читая форумы.

```
grep -inE 'vram|memory|gb' workflows/upstream/*.md
```

---

## 2. Устройство модели. Почему размер файла ≠ требуемая память

**ЗАМЕРЕНО** чтением заголовка safetensors range-запросом — без скачивания
17 ГБ. Заголовок весит 195 800 байт:

```
python3 - <<'EOF'
import json, struct, subprocess
U="https://huggingface.co/Kijai/WanVideo_comfy_fp8_scaled/resolve/main/Wan22Animate/Wan2_2-Animate-14B_fp8_e4m3fn_scaled_KJ.safetensors"
r=lambda a,b: subprocess.run(["curl","-sL","-H",f"Range: bytes={a}-{b}",U],capture_output=True).stdout
n=struct.unpack("<Q", r(0,7)[:8])[0]
h=json.loads(r(8,8+n-1)[:n]); h.pop("__metadata__",None)
print(len(h),"тензоров")
EOF
```

Результат:

| величина | значение |
|---|---|
| тензоров | 1922 |
| трансформерных блоков | **40** |
| каждый блок | **0.3762 ГБ**, все сорок ровно одинаковы |
| все блоки вместе | 15.049 ГБ |
| неблочная часть | **2.0885 ГБ**, из них `face_adapter` 1.5628 |
| типы | 15.039 ГБ `F8_E4M3` + 2.099 ГБ `BF16` |
| размерности | `dim` 5120, `ffn` 13824, патч `(1,2,2)` |

Два наблюдения, которые важнее суммы.

**Первое: блоки равны и их сорок.** Это то, что делает поблочную выгрузку
осмысленной: резидентной обязана быть неблочная часть плюс столько блоков,
сколько влезло, а не все сразу.

**Второе:** `face_adapter` на **1.5628 ГБ** — отдельная подсистема под канал
лица, с собственными `linear1_kv [10240,5120]`, `linear1_q`, `linear2` в каждом
fuser-блоке. Плюс `face_encoder`, `motion_encoder` и отдельное
`pose_patch_embedding [5120,16,1,2,2]` рядом с основным
`patch_embedding [5120,36,1,2,2]`. Мимика из драйвинга — не обещание в
маркетинге, а полтора гигабайта параметров и отдельный вход в архитектуре.

---

## 3. Comfy рассчитан на модели, которые не влезают

**ЗАМЕРЕНО** чтением `comfy/model_management.py` и `comfy/cli_args.py`:

| факт | где |
|---|---|
| `partially_load(device, extra_memory, …)` — частичная загрузка весов штатный путь, а не аварийный | `model_management.py:818` |
| `lowvram_model_memory` считается от **свободной** памяти, а не от размера модели | `:995` |
| `MIN_WEIGHT_MEMORY_RATIO = 0.4` | `:454` |
| `minimum_inference_memory() = 0.8 ГБ + extra_reserved_memory()` | `:860` |
| `extra_reserved_memory()` = **0.40 ГБ** на Linux, 0.60 на Windows | `:847–851` |
| динамический VRAM включён **по умолчанию** — выключается флагом `--disable-dynamic-vram` | `cli_args.py:315` |
| **«By default models will be unloaded to CPU memory after being used»** — это описание флага `--highvram`, то есть выгрузка и есть поведение по умолчанию | `cli_args.py:169` |
| `--lowvram` — «makes the text encoders run on the CPU» | `cli_args.py:170` |
| `--novram` — «when lowvram isn't enough» | `cli_args.py:171` |

Итого на Linux Comfy держит свободными около **1.20 ГБ** и грузит веса ровно
настолько, насколько хватает места. Утверждение «нужно 48 ГБ, потому что файл
17 ГБ» противоречит устройству загрузчика.

---

## 4. Кванты: измеренная дорога под 16 ГБ

**ЗАМЕРЕНО** через HF API. Репозиторий `QuantStack/Wan2.2-Animate-14B-GGUF`,
129 732 скачивания, лицензия **apache-2.0**, база `Wan-AI/Wan2.2-Animate-14B`:

```
curl -s "https://huggingface.co/api/models/QuantStack/Wan2.2-Animate-14B-GGUF/tree/main?recursive=true"
```

Столбец «итог» — веса плюс активации при 480×848×77 плюс резерв Comfy, то есть
то, что реально надо от карты. Считается `measure_hardware.py`.

| квант | размер | итог на 16 ГБ | вердикт |
|---|---|---|---|
| Q2_K | 6.01 ГБ | 9.24 | сильная потеря качества |
| Q3_K_M | 8.04 ГБ | 11.27 | ступень вниз при OOM |
| **Q4_K_M** | **10.71 ГБ** | **13.94** | кандидат на стартовую ступень |
| Q5_K_M | 12.11 ГБ | 15.34 | тесно; ступень вверх после замера пика VAE |
| Q6_K | 13.60 ГБ | 16.83 | **не влезает** при 77 кадрах |
| Q8_0 | 17.43 ГБ | 20.66 | не влезает |

Работает через кастомную ноду `UnetLoaderGGUF` из `city96/ComfyUI-GGUF` —
**Apache-2.0, проверено по файлу `LICENSE`**. Это единственная кастомная нода,
которую приходится вернуть в граф, и её лицензия чистая.

В том же репозитории лежит рабочий пример: `example_workflow/wan animate
native.json`, 62 ноды, `UnetLoaderGGUF` с `Q8_0` — то есть путь не
теоретический.

---

## 5. Активации. ВЫВЕДЕНО, с явными допущениями

Токенов на прогон считается из **замеренных** размерностей. Допущения, которые
не из заголовка и потому названы: VAE сжимает 8× по пространству и 4× по
времени; внимание эффективное (SDPA/Sage), то есть O(N) по памяти, а не O(N²).

| размер | кадров | латент | токенов | активации на блок |
|---|---|---|---|---|
| 640×640 | 77 | 20×40×40 | 32 000 | 2.04 ГБ |
| 832×480 | 77 | 20×30×52 | 31 200 | 1.99 ГБ |
| 640×640 | 49 | 13×40×40 | 20 800 | 1.33 ГБ |
| 480×480 | 77 | 20×30×30 | 18 000 | 1.15 ГБ |
| 512×512 | 49 | 13×32×32 | 13 312 | 0.85 ГБ |

Формула: `(4·N·dim + N·ffn) · 2 байта`, где 4 — на q, k, v и выход.

**Латенты сами по себе ничтожны:** 20×80×80×16 в fp16 — это около 4 МБ. Если бы
я посчитал это в первой редакции, вывод про 48 ГБ не появился бы.

Складывая замеренное с выведенным для карты на 16 ГБ:

```
Q5_K_M веса                     12.11 GB
активации при 640x640x77         2.04 GB
резерв Comfy на Linux            1.20 GB
                                --------
                                15.35 GB   впритык, но в габарит
Q4_K_M вместо Q5_K_M            13.95 GB   с запасом
Q5_K_M при 49 кадрах            14.64 GB   с запасом
fp8 целиком + поблочная выгрузка резидентно 2.09 + 0.38 на блок
```

**НЕИЗВЕСТНО и проверяется только запуском:** пик при декоде VAE 77 кадров
(у Comfy есть тайловый декод, но цена не замерена); фактическая фрагментация
памяти; скорость при поблочной выгрузке — она обменивает память на время, и
обмен не измерен.

**Вывод, который я готов защищать:** 16 ГБ — рабочая конфигурация, начинать
надо с Q5_K_M при 640×640 и 49–77 кадрах. Прежняя «рекомендация 48 ГБ» снята
как необоснованная. Локальная 3050 на 8 ГБ остаётся непригодной — но теперь это
следствие 6–8 ГБ квантов против 8 ГБ карты с системой, а не следствие 17 ГБ.

---

## 6. Лицензии: вопрос отгрузки, а не разработки

**Здесь была моя ошибка, и она обошлась дорого.** Я выкинул из стека оба LoRA
вендора на том основании, что у репозитория `Kijai/WanVideo_comfy` не объявлена
лицензия. Из этого вышло 20+ шагов вместо 6, четырёх-пятикратный рост стоимости
клипа, и вывод «на слабой карте это уже не стоимость, а выполнимость».

Ошибка в том, что смешаны две разные вещи:

* **отгрузка коммерческого продукта** — здесь лицензия обязательна, и правило
  «лицензии до сборки» написано ровно про это;
* **прогон эксперимента на своей машине** — это не распространение, и вопрос
  лицензии тут не наступает.

Плюс «лицензия не объявлена» есть **неопределённость, а не запрет**. Я подал
отсутствие декларации как прямой запрет, что сильнее, чем позволяют факты.

**Решение: оба LoRA возвращены в стек как в штатном темплейте вендора** —
step-distill и relight, вес 1.0 каждая. Лицензионного шлюза в предполёте нет.

Что остаётся — **одна строка учёта**, чтобы на отгрузке никто не удивился:

| что | откуда | лицензия |
|---|---|---|
| базовая модель | `Wan-AI/Wan2.2-Animate-14B` | apache-2.0 |
| fp8-репак | `Kijai/WanVideo_comfy_fp8_scaled` | apache-2.0 |
| GGUF-кванты | `QuantStack/Wan2.2-Animate-14B-GGUF` | apache-2.0 |
| нода GGUF | `city96/ComfyUI-GGUF` | Apache-2.0 |
| VAE, энкодеры | `Comfy-Org/Wan_*_Repackaged` | apache-2.0 |
| темплейт воркфлоу | `Comfy-Org/workflow_templates` | MIT |
| DWPose + YOLOX | уже в репозитории | apache-2.0 |
| step-distill и relight LoRA | `Kijai/WanVideo_comfy` | **не объявлена** — уточнить перед отгрузкой |

Метод проверки, раз уж он здесь: **модельный** эндпойнт HF
(`/api/models/<owner>/<repo>`), не поисковый. Поисковый не отдаёт `cardData` и
врёт `None` даже про официальные репозитории — на этом я сначала ошибся в
обратную сторону.

---

## 7. Диск

**ЗАМЕРЕНО** `curl -sIL` по URL из записки темплейта:

```
animate-14B fp8 (Kijai)      17.14 GB       ИЛИ  Q5_K_M   12.11 GB
umt5-xxl fp8                  6.27 GB
clip_vision_h                 1.18 GB
wan 2.1 vae                   0.24 GB
                             --------            --------
путь fp8, без LoRA           24.83 GB            19.80 GB  путь GGUF
```

Плюс окружение: torch с CUDA, ComfyUI, DWPose. **60 ГБ хватает с запасом**
для пути GGUF без LoRA. Прежнее «≥ 100 ГБ» тоже было завышено.

**Экономия, специфичная для нашего продукта:** промпт один на темплейт, значит
текстовое условие считается **один раз и кладётся в кэш**. Тогда `umt5-xxl` на
6.27 ГБ не нужен ни на диске рабочей машины, ни в памяти во время рендера —
только один раз при заготовке темплейта. Это снимает крупнейшую после самой
UNET позицию.

---

## 8. Что теперь проверяет предполёт

Не «есть ли 48 ГБ», а измеримое:

1. свободная VRAM в мегабайтах — и подбор кванта под неё по таблице §4;
2. свободный диск против суммы выбранного набора;
3. доступна ли эффективная реализация внимания — от неё зависит §5;
4. наличие каждого файла по хэшу;
5. лицензий предполёт **не проверяет** — это вопрос отгрузки, а не запуска (§6).

---

## 9. Кандидат под 16 ГБ. РЕШЕНИЕ НЕ ПРИНЯТО

> **Это входные данные для выбора, а не выбор.** Стек и карта выбираются
> отдельной сессией. Ниже — самосогласованный вариант под 16 ГБ, у которого
> каждая строка либо замерена, либо взята из исходников; он годится как точка
> отсчёта и как проверка, что задача вообще влезает в такой габарит. Ни одна
> строка не утверждена владельцем.

Геометрия — портретная, потому что **драйвинг продукта портретный**:
`demo/kit/driving` это 720×1278, отношение 0.563.

```
python3 -c "from PIL import Image; print(Image.open('demo/kit/driving/0000.jpg').size)"
```

| слой | выбор | размер | лицензия |
|---|---|---|---|
| диффузия | `Wan2.2-Animate-14B` **Q4_K_M** GGUF | 10.71 ГБ | apache-2.0 |
| загрузчик | `UnetLoaderGGUF` (`city96/ComfyUI-GGUF`) | — | Apache-2.0 |
| текстовый энкодер | `umt5-xxl-encoder` **Q5_K_M** GGUF | 3.86 ГБ | apache-2.0 |
| VAE | `wan_2.1_vae`, декод тайлами | 0.24 ГБ | apache-2.0 |
| CLIP vision | `clip_vision_h` | 1.18 ГБ | apache-2.0 |
| LoRA ×2 | `lightx2v` step-distill + `WanAnimate_relight`, вес 1.0 | 2.03 ГБ | не объявлена, §6 |
| внимание | **PyTorch SDPA, само** | — | — |
| препроцессинг | свой DWPose + сегментация | — | apache-2.0 |
| геометрия | **480×848, 77 кадров, 16 fps** → 4.8 с | — | — |
| шаги | **6 при cfg 1** — как в штатном темплейте | — | — |

**Диск:** 15.99 ГБ весов. С окружением (torch, Comfy, DWPose) — **40 ГБ
достаточно.** Прежние «≥ 100 ГБ» и «60 ГБ» оба были завышены.

**VRAM при 480×848×77:**

```
Q4_K_M веса                     10.71 GB
LoRA x2 (step-distill+relight)   2.03 GB
активации на блок                2.03 GB
резерв Comfy на Linux            1.20 GB
                                --------
                                <b>15.97 GB</b>   из 16 — впритык

то же при 49 кадрах             15.26 GB   запас 0.74
Q3_K_M вместо Q4_K_M            13.30 GB   запас 2.70
```

**НЕПРОВЕРЕНО:** держит ли Comfy патчи LoRA резидентно при GGUF-загрузчике.
Считаю по худшему — что держит. Если нет, запас больше.

Отсюда стартовая ступень честнее: **Q4_K_M при 49 кадрах** либо **Q3_K_M при
77**, и выбор между ними — вопрос замера, а не вкуса. Прежние 13.94 ГБ были
посчитаны без LoRA.

Текстовый энкодер в этот итог **не входит**, и это не оптимизм: Comfy по
умолчанию выгружает модель в память CPU после использования (§3). Энкодер
загружается, считает условие, выгружается, и только потом грузится диффузия.
Пик — максимум из 3.86 и 13.94, а не сумма.

### Почему в этом варианте Q4_K_M, а не Q5_K_M

Q5_K_M даёт 15.34 из 16 — запас 0.66 ГБ при **неизмеренном** пике декода VAE.
Ставить впритык то, чей пик неизвестен, значит узнать о нехватке на середине
прогона. Q4_K_M оставляет 2.06 ГБ.

**Лестница вверх** — после того как пик декода замерен: Q5_K_M при 49 кадрах
(14.63), затем Q5_K_M при 77 (15.34).
**Лестница вниз** при OOM: 448×800 (13.70), 416×736 (13.44), Q3_K_M (11.27).
Двигаться по одной ступени и записывать, что изменилось.

### Единственное, что зависит от модели карты

В модели **2.099 ГБ тензоров в BF16** (§2). На Ampere и новее (3090, A4000,
4060 Ti, 4080, L4) bf16 нативный. На Turing и Volta (T4, V100, 2080) bf16 нет
— torch поднимет эту часть до fp32, и она вырастет примерно на 2 ГБ, а скорость
упадёт. Тогда запас 2.06 ГБ съедается целиком, и стартовать надо сразу с
448×800 или Q3_K_M.

**НЕПРОВЕРЕНО:** какая именно карта на сервере. Это единственный вход, который
меняет стартовую ступень; всё остальное в таблице от карты не зависит. Флаги
внимания не нужны ни на одной из них: `flash_sdp` требует sm80+, но на Turing
Comfy падает на `mem_efficient_sdp`, а он тоже O(N) по памяти.

---

## 10. Если карта — A16: узкое место переезжает на вычисления

Владелец назвал A16 как имеющуюся карту. **Выбор карты при этом остаётся за
отдельной сессией**, поэтому раздел читается как разбор одного варианта: если
карта такова, то по памяти вариант §9 подтверждается без правок, а ограничением
становятся **вычисления**. Арифметика ниже полезна и для других карт — меняется
только строка пропускной способности.

### Сначала — чего я НЕ знаю про эту карту

Первая редакция этого раздела начиналась словами «что закрылось» и заявляла:
A16 — Ampere, значит bf16 нативный; A16 — четыре GPU по 16 ГБ, значит 64 ГБ не
пул. **Ни одно из двух утверждений не проверено.** Оба взяты из памяти и подан­ы
как решённые — то есть ровно та ошибка, за которую с меня уже сняли
«рекомендацию 48 ГБ»: припоминание, выданное за факт.

Здесь она опаснее, потому что на этой посылке стоит весь раздел:

| если посылка неверна | что рушится |
|---|---|
| A16 не Ampere, а Turing | bf16 нет → 2.099 ГБ BF16-тензоров поднимаются до fp32, запас 2.06 ГБ съеден, стартовая ступень уже не Q4_K_M при 480×848 |
| у A16 не 16 ГБ на GPU | вся таблица §9 считана не под ту карту |
| это не четыре GPU, а один | «три плеча гипотезы параллельно» отменяется, спринт 2 становится последовательным |

**Проверяется это не сайтом вендора, а машиной,** и одной строкой — что делает
закрытый домен неважным:

```
nvidia-smi --query-gpu=name,memory.total,compute_cap,driver_version --format=csv
```

Вывод этой команды авторитетнее любого даташита: он говорит про **эту** карту,
а не про модельный ряд. `compute_cap` 8.6 — Ampere, bf16 есть; 7.5 — Turing,
bf16 нет. Число строк в выводе — число GPU. **До этого вывода §10 читать как
условный: «если A16 такова, как я помню, то…».**

> **Про закрытый домен, для полноты.** `nvidia.com`, `docs.nvidia.com` и
> `techpowerup.com` отдают `000`; шлюз отвечает `403` на CONNECT. Замерено по
> шести хостам, включая голый `nvidia.com`, — значит дело не в поддоменах.
> Обход не искался. Домен запрошен в вайтлист и, по-видимому, применяется при
> старте контейнера. **Но держать план из-за него не нужно:** оттуда нужна была
> одна величина, пропускная способность, а её всё равно надёжнее замерить на
> карте (см. конец раздела) — паспортный пик и эффективная пропускная
> способность расходятся всегда.

### Спрос на вычисления — ВЫВЕДЕНО из замеренной архитектуры

Из заголовка safetensors: 40 блоков, `dim` 5120, `ffn` 13824. Из геометрии
драйвинга: 31 800 токенов при 480×848×77.

```
один forward всей сети            1.54 PFLOPs
   внимание O(N²)                 53.9%   <- главная статья
   FFN                            23.4%
   проекции self-attn             17.4%
   cross-attn                      5.3%
```

Внимание считается за O(N²) **по вычислениям** даже с flash-ядрами: O(N) там
про память, не про работу. Отсюда и берётся вся дальнейшая арифметика.

| эфф. TFLOP/s | 6 шагов cfg 1 | 20 шагов cfg 1 | 20 шагов cfg > 1 |
|---|---|---|---|
| 2 | 77 мин | 4.3 ч | 8.5 ч |
| 5 | 31 мин | 1.7 ч | 3.4 ч |
| 10 | 15 мин | 51 мин | 1.7 ч |
| 20 | 8 мин | 26 мин | 51 мин |

### Стоимость клипа при штатных шести шагах

С step-distill LoRA, как в темплейте вендора:

| эфф. TFLOP/s | мин на клип | клипов за 5 часов |
|---|---|---|
| 2 | 77 | 3.9 |
| 5 | 31 | 9.8 |
| 10 | 15 | 19.5 |
| 20 | 8 | 39.0 |

Этого хватает на смену с запасом: трёхплечевая проверка гипотезы — три прогона,
и даже в худшей строке они укладываются.

> Прежняя редакция считала 20+ шагов при cfg > 1, потому что я выкинул LoRA по
> лицензии, и получала 1.5–2.9 клипа за смену с выводом «уже не стоимость, а
> выполнимость». Вывод снят вместе с причиной: см. §6.

### Что делать вместо ожидания решения

**Первое: четыре GPU — это четыре параллельных прогона.** Продукт устроен как
«одна фотография → один клип», и такая задача делится по клипам идеально. Для
спринта это значит, что трёхплечевая проверка гипотезы (контроль, +15%, +30%)
идёт **одновременно на трёх GPU**: настенное время — один прогон, а не три.
Ни шардинга, ни правок модели.

**Второе: итерировать на малой геометрии, добивать на целевой.** Стоимость
падает как ~N², поэтому дешёвая петля стоит того:

| геометрия | токенов | от базы | 20 шагов cfg>1 при 10 TFLOP/s |
|---|---|---|---|
| **480×848×77** | 31 800 | 1.00× | 1.7 ч ← целевая, только для финала |
| 480×848×49 | 20 670 | 0.53× | 54 мин |
| 416×736×49 | 15 548 | 0.36× | 36 мин |
| **320×576×33** | 6 480 | **0.12×** | **12 мин** ← петля гипотезы |
| 288×512×25 | 4 032 | 0.07× | 7 мин |

**Оговорка, которая важнее таблицы.** Малая геометрия годится **не для всякого
вопроса**:

* **расширение силуэта — годится.** Эффект грубый: маска всё равно снимается до
  блоков 32 px (§ выше), и на 320×576 блок покрывает большую долю фигуры. Если
  гипотеза не работает здесь, она не заработает и на 480×848;
* **перенос личности — НЕ годится.** Судится по пикселям лица, а их на 320×576
  останется вдвое меньше. В репозитории уже есть метрика `face_px`, и у
  сданного hero-кадра она 127. Мерять личность на дешёвом прогоне значит
  получить ответ на другой вопрос;
* **ось протечки — годится с оговоркой:** вне маски сравнение с драйвингом
  корректно на любой геометрии, но шум сжатия придётся замерить отдельно для
  каждой.

**Третье, и это главное: предполёт обязан замерить карту, а не поверить
спецификации.** Величина «эффективных TFLOP/s» в таблицах выше — параметр, а не
знание. Замер стоит секунды и делается до всего остального: прогнать один
трансформерный блок на настоящем числе токенов, засечь время, домножить на 40 и
на шаги. Полученное число заменяет весь столбец догадок и решает, на какой
геометрии идёт петля. Спецификация вендора при этом не нужна вовсе — что и
снимает зависимость от закрытого домена.

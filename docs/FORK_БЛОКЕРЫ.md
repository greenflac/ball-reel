# Блокеры до прода — полный список, 22.08.2026

Порядок — по тому, что чем открывается, а не по важности на глаз. Ряд 2 не
начинается, пока не закрыт ряд 1: это не осторожность, это устройство.

## НЕПРОВЕРЕНО — наверх (Ц4)

**Ролика не существовало ни разу — ни нашим графом, ни чужим API.** Все планки
качества откалиброваны на пустоте. Пока это так, любой срок до прода — прогноз.

---

## РЯД 1. Без этого не начинается ничего (деньги и доступ)

| # | блокер | замерено чем | кто закрывает |
|---|---|---|---|
| **1** | **баланс fal.ai = 0.0** | `rest.fal.ai/billing/user_balance` -> `0.0`; любой POST -> 403 «User is locked. Exhausted balance» | владелец |
| **2** | **баланс Kling пуст** | code 1102 «Account balance not enough» ПОСЛЕ полной валидации схемы | владелец |
| 3 | **внешний HTTP-хост под драйвинг для Kling** | `video_url` принимает только ссылку; base64 отвергнут («Video URL is invalid»), а на фото тот же base64 проходит — контроль различает | владелец |
| 4 | **хостер serverless-ComfyUI не выбран и не оплачен** | кандидаты найдены поиском, ни один не вызван | владелец |

Ключи при этом ВАЛИДНЫ: испорченный `FAL_KEY` даёт 401 «No user found»,
настоящий — 403 lock. То есть упёрлись именно в деньги.

## РЯД 2. Техника — открывается сразу после ряда 1

| # | блокер | почему не закрыт | цена закрытия |
|---|---|---|---|
| 5 | **эталона нет**: как выглядит тот же движок на нашем материале | денег нет | $0.17 |
| 6 | **схема fal.ai не подтверждена** | резолвится только первый сегмент: `fal-ai/wan` есть, хвост `v2.2-14b/animate/replace` не проверяем; OpenAPI живёт на `fal.ai`, закрытом политикой | первый же вызов |
| 7 | **встанет ли `ComfyUI-WanVideoWrapper` у хостера** | кастомный пак, ни у кого не собран нами | ~$1 |
| 8 | **холодный старт** | `MEASURED_FIXED_S = 21.89` с загрузки GGUF 10.71 ГиБ; при посекундной оплате платится на каждом холодном запросе, +33% к счёту | один прогон |
| 9 | **время прохода на реальном железе** | `MEASURED_PASS_S = 12.28` снято с ЧУЖОЙ RTX 4090D. Вилка выигрыша против fal.ai от **26x до 1.7x** — это разные бизнесы | один прогон |
| 10 | **наследуется ли фигура** | судит оператор на валидации темплейта (решение владельца), но валидировать нечего: выхода нет. Тестовые фото готовы | входит в п. 5 |
| 11 | **доезжает ли стиль** | канал не проверен ни текстом, ни картинкой (`WanVideoClipVisionEncode`) | ~$0.35 |
| 12 | **липсинк наследованием** | губы должны идти за драйвингом, звук с его дорожки — на выходе не проверено ни разу | входит в п. 5 |
| 13 | **что модель делает с резкими переходами** | в `driving_selfie.mp4` кадры 75, 84, 97, 117, 220; рез это или мах телефоном — не смогли отличить | входит в п. 5 |
| 14 | **смысл `character_orientation` 0/1 у Kling** | сервис требует поле, но значения не объясняет; угадывать нельзя (Ц10) | два вызова |
| 15 | **разные соглашения о длине** | fal: 40..120 кратно 4; наше: 4k+1. 101 их правилу не удовлетворяет | арифметика, не деньги |

## РЯД 3. Решения владельца — кода и денег не требуют

| # | вопрос | почему это блокер |
|---|---|---|
| 16 | **юридическая чистка трендовых драйвингов** | продуктовый довод «юзеру не надо чистить исходники» верен ровно настолько, насколько чистим их мы. Процедуры с записью источника и основания нет |
| 17 | **стилевой референс не получен** | без него стилевая ось витрины не существует; требования выданы |
| 18 | **«рендерим типы телосложения» — A или B** | A (демо-превью витрины) возможно и стоит $0.34 на темплейт; B (готовые варианты по фото юзера) невозможно: лицо неизвестно до заказа |
| 19 | **брать ли страховочный второй ролик fal.ai** ($0.17) | старый референс расходится с драйвингом по шести осям; без страховки плохой эталон спишется на любое из шести, и стоп-условие сработает нечестно |
| 20 | **двусмысленность счёта fal.ai** | «секунды по 16 к/с» читается двояко, вилка +50% ($0.168 против $0.253). Снимается первым счётом |
| 21 | **на чём валидировать фигуру** — на чужом эталоне или на нашем графе | выводы разные; различить можно только прогнав обоих на одних трёх фото |

---

## СНЯТО за 22.08 — чтобы не пересчитывать заново

| было блокером | чем снято |
|---|---|
| домены закрыты (403 CONNECT) | **перемерено: открылись.** `queue.fal.run`, `rest.fal.ai`, `v3.fal.media` -> 404 (ответ сервиса), обе базы Kling -> 200, контроль `pypi.org` 200. Закрыт только сайт `fal.ai` |
| схема Kling неизвестна целиком | **вытащена из ответов сервиса**: Bearer напрямую (JWT не нужен), `/v1/videos/motion-control`, `model_name` ∈ {kling-v2-6, kling-v3}, `image_url`, `video_url`, `mode`, `character_orientation` |
| боевого драйвинга нет в репозитории | лежат оба: `driving_yogaball.mp4` (1 168 395 Б), `driving_selfie.mp4` (962 049 Б) |
| команда реза даёт 103 кадра вместо 101 | заменена прибором `fork_bench_input`, проверено на боевом файле |
| `MIN_FACE_PX = 100` выбрасывает 100% кадров | на `driving_selfie.mp4` лицо **234..369 px, медиана 259, ниже планки 0 из 31**. Для селфи-класса материала планка не мешает вовсе |
| живого фото под замер личности нет | `assets/fork_ref_gym.png`: анфас, ровный свет, один человек |
| тестовых фото под фигуру нет | три готовы через `nanobanana-2`, ArcFace 0.2579 / 0.2828 / 0.3133 при планке 0.35 |
| нужен прибор пропорций силуэта | **отменён решением владельца**: судит оператор на валидации темплейта |
| петли и мост | **убраны из продуктовой логики**, код оставлен под отдельный темплейт |

---

## Кратчайший путь в прод

1. Пополнить fal (**$1 хватит на всё из ряда 2, кроме п. 7**).
2. Один эталонный ролик -> закрывает пункты 5, 6, 10, 12, 13 и половину 20.
3. Развернуть граф у хостера -> закрывает 7, 8, 9 и делает цену числом.
4. Дать стилевой референс -> закрывает 17, открывает 11.
5. Остальное — решения, а не работа.

**Всё, что до п. 1, сделано. Всё, что после, без п. 1 не начинается.**

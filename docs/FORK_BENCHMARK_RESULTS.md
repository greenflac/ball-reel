# Сравнение с рынком: результаты

Задание — `docs/FORK_BENCHMARK_MANUAL.md`. Здесь только то, что **сделано и
замерено**, и ровно так же ясно — что сделать НЕ УДАЛОСЬ и почему.

Две смены, обе 22.08.2026. Первая упёрлась в закрытые домены. Вторая застала
домены открытыми — и упёрлась в то, что **у обоих вендоров на счету ноль.**

---

## НЕПРОВЕРЕНО — наверх (Ц4)

1. **Ни одного ролика ни у fal.ai, ни у Kling так и не заказано.** Главная
   цель задания НЕ ДОСТИГНУТА. Все числа ArcFace на чужих роликах, вся
   латентность и все счета остаются неснятыми, и ниже это не сглаживается.
2. **Цены вендоров** — из мануала, то есть из открытых прайсов на 20.08.2026,
   снятых поиском. Проверить их не вышло и этой сменой: страницы вендоров
   живут на `fal.ai`, который закрыт политикой. Меняются вендором в любой день.
3. **Имя эндпоинта fal.ai и имена его полей** (`video_url`, `image_url`,
   `resolution`) НЕ подтверждены. Ниже показано, что попытка их подтвердить
   ЧУТЬ НЕ ДАЛА ЛОЖНОГО ПОДТВЕРЖДЕНИЯ, и что его отменило.
4. **Смысл значений `character_orientation` у Kling** (0 и 1) не подтверждён,
   хотя оба значения приняты сервисом.
5. **Расчётная вилка нашей цены** (0.202 / 0.604 / 2.012 ₽/с) — РАСЧЁТ по
   чужой карте, не замер. Своего ролика по-прежнему не существует.

---

## Стоп-условие мануала §10 НЕ СРАБОТАЛО И НЕ МОГЛО

Мануал велит остановиться, если эталон fal.ai даёт ролик, который владелец
бракует глазами. **До этой развилки не дошли: ролика нет, браковать нечего.**
Это исход «не смогли проверить», а не «прошло» и не «провалилось».

---

## 1. Приёмка: три блокера, один снят, два держатся

Прибор `fork_bench/fork_bench_preflight.py` печатает это за секунды —
дословный вывод прогона второй смены:

    [годно             ] домены         все 5 хостов открыты; контроль pypi.org 200
    [годно             ] ключи          оба ключа заданы
    [не годно          ] баланс fal.ai  баланс fal.ai = 0.0
    [не годно          ] баланс Kling   code 1102 «Account balance not enough»
    [не годно          ] вход           assets/driving_yogaball.mp4 нет на диске

    исход: не годно
    проверено 5, блокирует 3, не смогли 0

### Блокер А: домены. СНЯТ между сменами — перемерено, а не предположено

Первая смена замерила `CONNECT tunnel failed, 403` на всех хостах прогона.
Вторая перемерила тем же способом и получила ДРУГОЕ:

| хост | первая смена | вторая смена | что значит |
|---|---|---|---|
| `queue.fal.run` | 403 CONNECT | **404** | туннель работает, хост открыт |
| `rest.fal.ai` | 403 CONNECT | **404** | открыт |
| `v3.fal.media` | — | **404** | открыт |
| `api.klingai.com` | 403 CONNECT | **200** | открыт |
| `api-singapore.klingai.com` | 403 CONNECT | **200** | открыт |
| `fal.ai` (сайт) | 403 CONNECT | **соединения нет** | ЗАКРЫТ по-прежнему |
| `pypi.org` | 200 | **200** | негативный контроль (И5): сеть цела |

404 на корне — это ОТВЕТ сервиса, а не отказ прокси: корень просто не
обслуживается. Отличие от 403 CONNECT принципиально, и негативный контроль
`pypi.org` подтверждает, что прибор меряет политику, а не свою поломку.

**Что осталось закрытым и зачем было нужно:** `fal.ai` — на нём живёт
OpenAPI-схема эндпоинтов. Из-за этого пункт 3 «НЕПРОВЕРЕНО» закрыть не вышло
(подробно ниже). Политику не обходили ничем (Ц3).

### Блокер Б: боевого драйвинга по-прежнему НЕТ

    $ ls -la assets/
    README.md  legacy_hero_NOT_FOR_PRODUCTION.png  photoref_profile.jpg
    $ find / -name 'driving_yogaball*'     -> ничего
    $ git log --all -- assets/driving_yogaball.mp4  -> ни одного коммита

Причина та же: `*.mp4` в `.gitignore:8`. Просьба первой смены (`git add -f`)
не исполнена. `assets/README.md` по-прежнему утверждает, что файл «положен в
репозиторий 20.08.2026 по решению владельца» — **документ говорит одно,
`git ls-files` другое, и верить надо свидетельству (Е2).**

Единственный mp4 в репозитории — `demo/clip.mp4`, 672 097 Б, 1.33 с. Он для
сравнения НЕ ГОДИТСЯ и в качестве драйвинга не использовался: подмена входа
уничтожает смысл сравнения. Он послужил только негативным контролем в пробах,
где содержимое неважно.

### Блокер В: НА СЧЕТУ У ОБОИХ ВЕНДОРОВ НОЛЬ. Новый, замерен, выше прочих

Это находка второй смены, и она объясняет, почему открытие доменов ничего не
дало само по себе.

**fal.ai — баланс снят прямо, числом:**

    GET  https://rest.fal.ai/billing/user_balance     HTTP 200, тело: 0.0
    POST https://queue.fal.run/<любой эндпоинт>       HTTP 403
         {"detail": "User is locked. Reason: Exhausted balance.
                     Top up your balance at fal.ai/dashboard/billing."}
    POST https://rest.fal.ai/storage/auth/token       HTTP 403, тот же lock

Негативный контроль эндпоинта баланса (И5): `rest.fal.ai/users/me` отвечает
404 — значит 200 выше не «сервис отвечает 200 на что угодно».

Негативный контроль ключа: с испорченным `FAL_KEY` приходит
`401 «No user found for Key ID and Secret»`, с настоящим — 403 lock.
**То есть `FAL_KEY` ВАЛИДЕН, и отказ именно про деньги, а не про ключ.**

Отдельно важно: lock запирает и ЗАГРУЗКУ входа. Без пополнения нельзя даже
выложить файл, не то что запустить модель.

**Kling — баланс снят отказом боевого метода:**

    POST /v1/videos/motion-control с полностью валидной схемой
    -> HTTP 429  {"code": 1102, "message": "Account balance not enough"}

Отдельного эндпоинта баланса у Kling не нашлось: `/account/costs` отвечает
`{"code":0,"msg":"success"}` с пустыми данными. Зато отказ 1102 однозначен, и
он приходит ПОСЛЕ того, как схема прошла валидацию целиком — то есть упёрлись
именно в деньги, а не в поля.

---

## 2. Kling: схема ВЫТАЩЕНА ЦЕЛИКОМ из ответов сервиса

Первая смена писала: «HTTP-схема Kling неизвестна целиком: ни база, ни путь,
ни способ авторизации не подтверждены ничем». **Все три пункта закрыты.**

### 2.1. Авторизация: подозрение первой смены ОПРОВЕРГНУТО

Первая смена числила первым подозреваемым форму ключа: `KLING_KEY` — 57
символов, двоеточий 0, «на пару AccessKey/SecretKey не похож».

ЗАМЕРЕНО: `Authorization: Bearer <KLING_KEY>` принимается сервисом.

    GET /v1/videos/text2video, Bearer <KLING_KEY>   200 {"code":0,"message":"SUCCEED"}
    то же без заголовка вовсе                       401 {"code":1001,"Authorization is empty"}
    то же с мусорным ASCII-ключом                   {"code":1002,"token was expected
                                                     to have 3 parts, but got 0"}

Прибор различает «ключа нет», «ключ не тот» и «ключ принят» — три исхода, а не
два. **Никакой JWT собирать не надо.**

Оговорка, чтобы наблюдение не выдавалось за большее: сообщение про «3 parts»
показывает, что у Kling есть и JWT-ветка. Наш ключ (точек 0) в неё не попадает
и принимается напрямую. ПОЧЕМУ сервис принимает оба вида — НЕПРОВЕРЕНО;
ЧТО принимает наш — доказано ответом.

### 2.2. Путь эндпоинта: подтверждён, с негативным контролем

GET списка задач оказался бесплатным детектором существования метода.

| путь | ответ | |
|---|---|---|
| `/v1/videos/motion-control` | **200 code 0 SUCCEED** | ЕСТЬ |
| `/v1/videos/motion_control` | 404 | нет |
| `/v1/videos/motioncontrol` | 404 | нет |
| `/v2/videos/motion-control` | 404 | нет |
| `/v1/videos/ZZZ_negative_control_ZZZ` | 404 | нет, контроль (И5) |
| `/v1/videos/text2video`, `image2video`, `multi-image2video`, `video-extend`, `lip-sync`, `effects` | 200 | есть |

Обе базы (`api.klingai.com` и `api-singapore.klingai.com`) отвечают одинаково.

### 2.3. Поля: каждое имя и допустимые значения названы САМИМ СЕРВИСОМ

Схема вытащена лестницей отказов — пустое тело, сервис называет недостающее
поле, добавляем, называет следующее. Ни одно имя не взято из памяти (Ц10).

| поле | как подтверждено | значения |
|---|---|---|
| `model_name` | «model_name must be 'kling-v2-6' or 'kling-v3'» | **сервис назвал сам**; `kling-v1` отвергнут |
| `image_url` | «imageUrl: must not be blank» | URL **или base64** |
| `video_url` | «videoUrl: must not be blank» | **только URL** |
| `mode` | «video mode must be specified» | `std` принят; прочие не проверялись |
| `character_orientation` | «character orientation must be specified» | приняты `0` и `1`; `2`, `3` и все слова отвергнуты |

Имена-кандидаты `orientation`, `char_orientation`, `character_direction`
отвергнуты сервисом — то есть проба различает верное имя и похожее.

### 2.4. Асимметрия загрузки: фото байтами можно, видео — нельзя

Замерено отдельно, и это меняет план:

| что послали | ответ |
|---|---|
| `image_url` = base64 реального фото | прошло до проверки баланса |
| `image_url` = мусор | «File is not in a valid base64 format» |
| `video_url` = base64 реального mp4 (672 КБ) | «Video URL is invalid» |
| `video_url` = мусор | «Video URL is invalid» |

Негативный контроль различает случаи для фото и НЕ различает для видео —
значит base64 в `video_url` не поддерживается, а не «не долетел».

**Следствие: для Kling нужен внешний HTTP-хост под драйвинг.** CDN fal.ai на
эту роль не годится, пока его аккаунт заперт балансом. Это открытый вопрос,
которого до этой смены в списке не было.

---

## 3. fal.ai: подтвердить схему НЕ УДАЛОСЬ, и негативный контроль спас от вранья

Это место стоит прочесть целиком: здесь чуть не появилось ложное
подтверждение, и отменил его именно негативный контроль (И5).

**Соблазнительное наблюдение.** Пустой POST на `fal-ai/wan/v2.2-14b/animate/replace`
даёт 403 lock, а на `fal-ai/nosuchvendor/nosuchmodel/nosuchmode` — 404
«Application "nosuchvendor" not found». Читается как «имя эндпоинта
существует, проверка баланса идёт после резолва».

**Негативный контроль это опроверг:**

    fal-ai/wan/v2.2-14b/animate/replace      403 lock
    fal-ai/wan/v2.2-14b/animate/replaceXX    403 lock   <- ВЫДУМАННЫЙ, тоже 403
    fal-ai/wan/v9.9-99b/animate/replace      403 lock   <- ВЫДУМАННЫЙ, тоже 403
    fal-ai/flux/dev                          403 lock
    fal-ai/nosuchvendor/...                  404

Без ключа сервис говорит прямо: `«Cannot access application "fal-ai/wan"»`.

**Резолвится ТОЛЬКО первый сегмент.** Подтверждено существование приложения
`fal-ai/wan` — и НИЧЕГО про хвост `v2.2-14b/animate/replace`, ничего про поля.
Без негативного контроля в отчёт уехало бы «имя эндпоинта подтверждено», и это
было бы неправдой.

**Обходных путей к схеме не нашлось:** без ключа 401, с ключом 403 lock,
`queue.fal.run/<ep>/openapi.json` — 405, три пути на `rest.fal.ai` — 404, а
публичный OpenAPI живёт на `fal.ai`, закрытом политикой. Обходить её нельзя (Ц3).

**Значит пункт задания «снять фактическую схему fal.ai» закрывается первым же
вызовом после пополнения баланса, и не раньше.**

---

## 4. Таблица «цена за секунду»

**Все цены вендоров — НЕПРОВЕРЕНО (Ц4):** прайсы 20.08.2026 из мануала, ни
одного счёта не выставлено. Наши числа — РАСЧЁТ, не замер: своего ролика нет.

Кусок: **101 кадр при 24 к/с = 4.2083 с.** Курс 95 ₽/$. Секунда карты
0.0135 ₽ при 35 000 ₽/мес.

| | цена за секунду | цена куска 4.208 с | что это за число |
|---|---|---|---|
| fal.ai 480p | $0.040 = 3.80 ₽ | **$0.168** | НЕПРОВЕРЕНО, прайс |
| fal.ai 720p | $0.080 = 7.60 ₽ | **$0.337** | НЕПРОВЕРЕНО, прайс |
| Kling Motion Control | $0.126 = 11.97 ₽ | **$0.530** | НЕПРОВЕРЕНО, прайс, ДРУГАЯ модель |
| наша, если A16 = 4090D | 0.202 ₽ | 0.85 ₽ | РАСЧЁТ по чужой карте |
| наша, если втрое медленнее | 0.604 ₽ | 2.54 ₽ | РАСЧЁТ |
| наша, если вдесятеро | 2.012 ₽ | 8.47 ₽ | РАСЧЁТ |

**Разрыв против того же движка (fal.ai 480p): 18.8x / 6.3x / 1.9x.**
Сходится с вилкой мануала «19x, 6x, 2x» — это проверка арифметики, а НЕ
подтверждение чисел: обе стороны считаны из одного источника.

### Двусмысленность в счёте fal.ai — НЕ СНЯТА

Мануал пишет: «секунды считаются по 16 к/с». Прочтений два:

| прочтение | 480p | 720p |
|---|---|---|
| A. платим за секунды выхода (4.2083 с) | $0.168 | $0.337 |
| B. платим за кадры/16 (101/16 = 6.3125 с) | **$0.253** | **$0.505** |

Разница **+50%**. Снимается первым же счётом, а счёта не было: баланс ноль.
**Вся колонка fal.ai остаётся вилкой, а не числом.**

---

## 5. Планка `MIN_FACE_PX = 100` — замер первой смены, решение не принято

Не заметено, повторено здесь, потому что решения по нему всё ещё нет.

Ролик из боевого фотореференса в НАШЕЙ геометрии выхода 480x832, лицо 95 px:

    отсев выключен:  медиана 0.0232 | в баре 24 из 24 | НЕ СМОГЛИ 0  -> ГОДНО
    отсев 100 px:    медиана None   | в баре  0 из  0 | НЕ СМОГЛИ 24 -> НЕ СМОГЛИ

**Один и тот же ролик: без отсева «тот же человек» с запасом в 15 раз; с
отсевом сто процентов кадров выброшено.** Планка не смягчает вердикт — она
уничтожает выборку. Это решение продукта, а не константа, и подкручивать её
молча нельзя.

ЧЕГО ЭТОТ ЗАМЕР НЕ ГОВОРИТ: он снят на уменьшенных копиях одной фотографии,
где нет ни смаза, ни смены позы, ни генерации. Он ограничивает вклад ОДНОГО
слагаемого и не оправдывает снятие планки.

---

## 6. Референс снят СО СПИНЫ — и Kling спрашивает об этом полем

Находка первой смены (глазами, П3): `assets/photoref_profile.jpg` — женщина
СО СПИНЫ, голова повёрнута через плечо. Драйвинг начинается ЛИЦОМ В КАМЕРУ.
Ширина плеч в проекции 0.010 против 0.288.

Следствие остаётся в силе: **если эталон fal.ai выйдет плохим, отличить
«модель не тянет задачу» от «референс снят не с той стороны» будет НЕЛЬЗЯ** —
два объяснения сольются в одно наблюдение, и стоп-условие §10 сработает
нечестно.

**Новое этой смены:** у Kling есть обязательное поле `character_orientation`,
и оно принимает ровно два значения — 0 и 1. Что каждое значит, сервис не
сказал, и угадывать это в код нельзя (Ц10). Но сам факт, что вендор ТРЕБУЕТ
указать ориентацию персонажа, — независимое подтверждение, что ракурс
референса здесь считается значимым параметром, а не мелочью.

Страховка за $0.17 (второй ролик у fal.ai с кадром драйвинга в роли
референса) по-прежнему разделяет два объяснения. Решение владельца.

---

## 7. Что считается доказанным, а что нет

**ДОКАЗАНО прогоном (вторая смена):**

- домены прогона ОТКРЫЛИСЬ: `queue.fal.run`, `rest.fal.ai`, `v3.fal.media`,
  обе базы Kling отвечают; `fal.ai` (сайт, OpenAPI) закрыт; контроль
  `pypi.org` 200;
- **баланс fal.ai = 0.0**, снят числом с `rest.fal.ai/billing/user_balance`;
  lock запирает и прогон, и загрузку входа;
- **баланс Kling пуст**: code 1102 после полной валидации схемы;
- `FAL_KEY` валиден (испорченный даёт 401 «No user found», настоящий — 403 lock);
- `KLING_KEY` — **готовый Bearer**, JWT собирать не надо (code 0 SUCCEED);
  подозрение первой смены опровергнуто;
- путь Kling `/v1/videos/motion-control` существует, четыре выдуманных — 404;
- полная схема Kling: `model_name` ∈ {`kling-v2-6`,`kling-v3`}, `image_url`,
  `video_url`, `mode`=`std`, `character_orientation` ∈ {0,1};
- у Kling `image_url` принимает base64, `video_url` — только ссылку;
- у fal.ai резолвится только первый сегмент пути: подтверждено приложение
  `fal-ai/wan`, НЕ подтверждён хвост `v2.2-14b/animate/replace`;
- боевого драйвинга нет ни в репозитории, ни в контейнере, ни в истории git.

**ДОКАЗАНО ранее (первая смена), не перепроверялось:**

- прибор ArcFace работает: 0.7137 воспроизведено до четвёртого знака, 0.0 на
  самом себе;
- на геометрии 480x832 лицо 95 px, планка 100 px выбрасывает 100% кадров;
- команда реза из мануала даёт 103 кадра (65..167) вместо 101 (65..165), и при
  резе за концом файла молча отдаёт файл целиком;
- `fal_client.upload_file` существует.

**НЕ ДОКАЗАНО НИЧЕГО ИЗ ГЛАВНОГО:**

- сколько стоит секунда у fal.ai и Kling на нашем входе — счетов не было;
- сколько ждать от отправки до готовности — не отправляли;
- как выглядит ролик того же движка на нашем материале — **эталона
  по-прежнему нет, и все планки качества откалиброваны на пустоте**;
- ляжет ли годный ролик ниже 0.35 — мерить нечего;
- верна ли схема запроса fal.ai;
- верны ли цены вендоров;
- что означают 0 и 1 в `character_orientation` у Kling.

**Ни один ролик не был просмотрен глазами (П3), потому что ни одного ролика
не появилось.**

---

## 8. Что нужно от владельца, по убыванию цены вопроса

1. **Пополнить баланс у обоих вендоров.** Это блокер номер один, и он один
   стоит дешевле всего остального: по прайсу два ролика fal.ai (480p и 720p)
   плюс страховочный второй референс — около $0.68, Kling — $0.53. Пока счёт
   нулевой, открытые домены и подтверждённые схемы не дают ничего.
2. **Положить `assets/driving_yogaball.mp4` в репозиторий** через
   `git add -f` (иначе `*.mp4` съест его снова) — либо отдать файл иначе.
   Без него «один вход на всех» недостижим, а подменять драйвинг нельзя.
3. **Дать HTTP-хост под драйвинг для Kling** — `video_url` принимает только
   ссылку, base64 отвергается (замерено). Для fal.ai хост не нужен:
   `upload_file` закрывает вопрос, как только снимется lock.
4. **Решить, брать ли второй ролик fal.ai** с кадром драйвинга в роли
   референса ($0.17). Без него плохой эталон нельзя будет объяснить
   однозначно, и стоп-условие §10 сработает нечестно.
5. **Решить по планке `MIN_FACE_PX = 100`.** На выбранном плане она отвечает
   «не смогли измерить» на 100% кадров: либо кроп-прогон (окно 462x800 уже
   посчитано, даёт 105 px), либо пересмотр планки замером.

---

## 8б. Побочно замерено: ffmpeg ставится, и рез за концом больше не врёт

В контейнере снова не было ни `ffmpeg`, ни `ffprobe`. `pypi.org` открыт, и
`pip install imageio-ffmpeg` кладёт `ffmpeg` 7.0.2 — то есть резать будет чем,
как только появится драйвинг. `ffprobe` пакет по-прежнему не кладёт, поэтому
заменитель на PyAV (`fork_bench_prober.py`) остаётся нужен.

Заодно проверен старый дефект команды мануала — «при резе за концом ролика
ffmpeg молча отдаёт файл целиком». **На команде с `-frames:v` он НЕ
воспроизводится:** рез из 1.33-секундного `demo/clip.mp4` (короче заказанных
4.21 с) дал файл 261 Б, в котором PyAV не находит ни одного видеопотока, а
прибор ответил `не смогли проверить`, а не `годно`. Против входных 672 097 Б
это ни при каком чтении не «копия». Ещё один довод за `-frames:v` против
команды из мануала.

## 9. Чем пользоваться

`fork_bench/` — пять приборов, все прогнаны, все с тремя исходами:

| файл | что делает | проверен |
|---|---|---|
| `fork_bench_preflight.py` | что блокирует прямо сейчас: домены, ключи, **баланс**, вход | да, мутациями констант в обе стороны |
| `fork_bench_input.py` | точный рез 101 кадра + проверка ПОСЛЕ реза | да: годно / нет исходника / рез за концом |
| `fork_bench_fal.py` | прогон fal.ai; отличает «денег нет» от «схема не та» | до lock'а (третий исход) |
| `fork_bench_kling.py` | прогон Kling по ПОДТВЕРЖДЁННОЙ схеме | до баланса (третий исход) |
| `fork_bench_measure.py` | `fork_video.frames` + `fork_identity.distances`, оба режима отсева | да, на боевом фото |
| `fork_bench_prober.py` | заменитель `ffprobe` на PyAV | да, на трёх входах |

Существующие модули `ball_reel/*` не тронуты: заменитель подан через
объявленную точку внедрения `prober=`, а не правкой модуля (Ц2).
